# GEO-Pulse Ã¢â‚¬â€ Project State
> Maintained by: Orchestrator only
> Format: Update this file whenever a task changes state. Never delete history Ã¢â‚¬â€ add new entries.

---

## Current Phase: Phase 4 Ã¢â‚¬â€ Launch

**Phase goal:** Production deploy, OG share image, email DNS (SPF/DKIM/DMARC), WAF, keep-alive cron, launch security audit.

**Implementation status (2026-03-25):** P4-002 + P4-005 **in repo**. **P4-001** Ã¢Å“â€¦ Ã¢â‚¬â€ production Worker `https://geo-pulse.uzzielt.workers.dev` + **live Stripe checkout return path** verified Ã¢â‚¬â€ see `COMPLETION_LOG.md` *Phase 4 Ã¢â‚¬â€ operator evidence*. **P2-008** Ã¢Å“â€¦ Ã¢â‚¬â€ production paid path verified. **Still to close with evidence in log:** **P4-003** (SPF/DKIM/DMARC), **P4-004** (WAF CVE-2025-29927), **P4-006** (Security sign-off on five blockers). **Current operator blocker:** the new custom domain `getgeopulse.com` is now purchased, but DNS / Resend / custom-domain cutover evidence is still pending, so `P4-003` and final `P4-006` are not closed yet. **Phase 4Ã¢â€ â€™Launch gate** (`agents/ORCHESTRATOR.md`) requires all five security blockers + production smoke Ã¢â‚¬â€ Orchestrator marks when P4-003/004/006 evidence is attached.

**Current UX follow-up (2026-03-26):** User testing exposed a product-truth gap in the core journey: free scan preview, pay-for-full-audit, and email-save flow did not read as clear incremental steps toward one outcome. The core implementation is now in repo: explicit step framing, shared preliminary/final audit loading language, and state-driven paid-report status have replaced the earlier optimistic copy. Remaining UX work is verification and any follow-up polish from real-user testing.

**Current strategic expansion (2026-03-26):** GEO-Pulse will keep the current audit/report product as the customer-facing wedge while a benchmark/measurement layer is planned as a staged internal platform. This work is tracked as a separate initiative and does not change the truth that launch remains blocked on `P4-003`, `P4-004`, and `P4-006`.

**Current architecture hardening stream (2026-03-28):** Because Phase 4 launch closure is externally blocked, GEO-Pulse is also tracking a narrow architecture-hardening stream to keep the platform lean, easier to debug, and better prepared for future browser-level testing. Current truth: this stream does not close launch; it currently covers internal observability, Playwright readiness plus the first open-source Playwright smoke-test foundation, the first thin-route cleanup slice via a shared admin runtime helper for repeated auth/env/service-role setup, the first large-module decomposition slice for the benchmark run-detail route, the second large-module decomposition slice for the report viewer, the third large-module decomposition slice for the benchmark overview route, the fourth large-module decomposition slice for deep-audit report preparation helpers shared between PDF and markdown generation, the fifth large-module decomposition slice for deep-audit crawl pending-state helpers, the sixth large-module decomposition slice for deep-audit email-delivery composition helpers, and the admin-auth maturity plan documenting the future move from env-only founder-stage gating to a table-driven admin allowlist. The Playwright smoke lane remains intentionally narrow and now runs serialized (`workers: 1`, no full-parallel mode) because that is the stable dev-server setting for this repo.

**Current content-machine implementation stream (2026-03-31):** Branch `planning/content-machine-v1` now holds both the original planning docs and the first product implementation slices. Current truth: the repo now contains a first-pass `.agents/product-marketing-context.md`, founder voice draft in `PLAYBOOK/content-machine-inputs/brand-voice.soulmd`, social-research synthesis from external LLM research, `PLAYBOOK/content-machine-v1-blueprint.md`, `PLAYBOOK/blog-llm-readiness-spec.md`, and `PLAYBOOK/content-writing-skill-spec.md`, plus the first canonical schema and admin surface for the content machine: `supabase/migrations/016_content_machine_foundation.sql`, `lib/server/content-admin-data.ts`, and `/dashboard/content`. A second slice now adds the provider-control layer: `supabase/migrations/017_content_distribution_destinations.sql`, `lib/server/content-destination-admin-data.ts`, `app/dashboard/content/actions.ts`, and destination feature flags inside `/dashboard/content`. A third slice now adds local draft import: `lib/server/content-draft-import.ts` plus an admin import action that reads `PLAYBOOK/content-machine-drafts`, derives stable `content_id` values, and upserts those draft assets into canonical `content_items`. A fourth slice now adds the first content-item detail/editor surface at `/dashboard/content/[contentId]`, with basic metadata editing, markdown editing, and per-item delivery visibility. A fifth slice now adds the provider adapter seam: `lib/server/content-destination-adapters.ts`, first Kit and Ghost adapters, provider env support, and a draft-push action from `/dashboard/content/[contentId]` that persists `content_distribution_deliveries` after a successful API push. A sixth slice now resolves effective destination readiness in admin from both feature flags and live env state, and writes content-push lifecycle events into `app_logs` so `/dashboard/logs` shows started, blocked, failed, and succeeded downstream delivery attempts. A seventh slice now adds the first public blog surface: `/blog`, `/blog/[slug]`, and a public content read path over published canonical `content_items`, with markdown-rendered article pages and a built-in free-scan CTA. An eighth slice now adds the first admin-to-blog publish workflow: article-only publish guardrails, canonical blog route derivation, and a dedicated publish action from `/dashboard/content/[contentId]` that closes the basic site-first path from stored draft to public article. A ninth slice now adds first article trust metadata on the canonical site: author name / role / URL fields stored in `content_items.metadata`, surfaced in admin, rendered on `/blog/[slug]`, and emitted as first-pass `Article` JSON-LD. A tenth slice now adds first topic-cluster navigation on the public blog: the blog index groups articles by topic cluster and exposes article/topic menus, while article pages link to related articles and a browse-articles menu so the canonical site is easier to navigate and easier for machines to interpret as a connected content graph. An eleventh slice now adds dedicated topic landing pages at `/blog/topic/[topic]`, and rewires topic labels across the blog to point at those cluster URLs so topical navigation is part of the URL structure rather than only a page-local grouping affordance. A twelfth slice now makes those topic pages more extractable by adding short definition, why-it-matters, and practical-takeaway blocks before the article list, so the cluster URLs carry usable topic context instead of being bare navigational lists. A thirteenth slice now moves that topic-page intro copy into the content system: `/dashboard/content` can seed one published `research_note` record per topic cluster, topic-page fields are editable from the existing content detail view, and `/blog/topic/[topic]` now prefers those canonical records over code-only defaults. A fourteenth slice now adds first in-body internal-link blocks on article pages: `/blog/[slug]` now points readers into the topic hub and sibling canonical articles before the markdown body begins, so cluster navigation is visible inside the article path itself instead of only in sidebars or footer-like related sections. A fifteenth slice now adds first launch-readiness hardening for the blog: `/blog/topic/[topic]` emits structured data for topic hubs, and `/dashboard/content/[contentId]` exposes a lightweight editorial-readiness checklist for article launches. A sixteenth slice now turns that checklist into a real launch gate for canonical articles: publish attempts and manual `status = published` transitions now fail when the required editorial-readiness checks are incomplete. A seventeenth slice now adds a dedicated launch-readiness dashboard at `/dashboard/content/launch`, summarizing whether the current content set actually meets the first-launch threshold and which articles still fail readiness. New planning slices are now frozen too: `A1` adds a docs-style `20 pillars x 5 intents` topic taxonomy contract in `docs/11-topic-taxonomy-v1.md` for a bounded 100-topic program before generation/publish scaling, `A2` adds a markdown frontmatter + media/channel mapping contract in `docs/12-content-frontmatter-media-contract-v1.md` for canonical/blog/newsletter/social consistency, `A3` adds a machine-readable topic registry in `docs/13-topic-registry-v1.json` for batch planning and editorial/generation tracking, and `B1` adds a docs-style blog IA/navigation contract in `docs/14-docs-style-blog-ia-contract-v1.md` covering route ownership, navigation behavior, breadcrumbs, and crawler-friendly hierarchy rules. `B2` is now in repo too: the first bounded docs-style navigation shell is shipped on `/blog`, `/blog/topic/[topic]`, and `/blog/[slug]` with explicit breadcrumb navigation and topic-aware sidebar browsing patterns while preserving existing canonical routes and content rendering flow. `C1` is now in repo too: navigation-related structured data and route metadata are hardened across the same blog routes via breadcrumb JSON-LD aligned to visible breadcrumbs, blog-index collection structured data, and explicit canonical metadata on `/blog`. `C2` is now in repo too: sitemap/indexing guardrails are now split by surface (`/sitemap.xml`, `/blog/sitemap.xml`, `/blog/topic/sitemap.xml`), robots now advertises all sitemap endpoints, and article metadata now supports `noindex` with route-level robots handling and sitemap exclusion for noindex-marked content. `D1` is now in repo too: pre-publish validation now enforces frontmatter/media/indexability checks in admin publish workflows (including topic cluster, author fields, hero media fields, and `noindex` blocking) before canonical article publish. `D2` is now in repo too: publish validation now also enforces explicit LLM-readiness + claim-discipline checks (concrete extractable heading patterns, answer-block density, in-body internal blog links, and bounded overclaim language). `D3` is now in repo too: `/dashboard/content/[contentId]` now surfaces those same shared publish checks as structured operator-facing pass/fail cards grouped by publish contract, LLM readiness, and claim discipline. `D4` is now in repo too: publish validation now also enforces richer semantic checks for claim-to-source alignment, freshness drift on time-sensitive phrasing, and terminology consistency/clarification. `D5` is now in repo too: publish-check snapshots are now persisted in article metadata and surfaced as recent pass/fail history in `/dashboard/content/[contentId]` for operator trend review. The direction remains intentionally site-first and explicitly LLM-searchability-aware so GEO-Pulse does not create a content/distribution machine that fails its own AI-readiness standards on the canonical site. What is still not shipped: a richer editorial workflow, broader provider support, scheduled/public publication workflow, stronger schema/trust metadata, and deeper semantic publish checks and trending beyond the current D5 guardrails (stronger claim-to-source traceability, nuanced freshness policy by topic type, and cross-article regression controls).

**Current blog visual theming queue (2026-04-03):** `E1` is now in repo: the public blog routes (`/blog`, `/blog/topic/[topic]`, and `/blog/[slug]`) now use a black-first theme with white primary text while preserving the docs-style IA behavior and accessibility contrast. `E2` is now in repo too: blog-route header/footer chrome is now dark-theme aware with first contrast cleanup, while non-blog routes keep existing theme behavior. `E3` is now in repo too: final dark-theme parity polish is applied across link/hover/active states and low-contrast copy on blog routes + markdown. `E4` is now in repo too: route-scoped visual QA polish adds focus-visible treatment, selection contrast, and readability smoothing for blog routes. `E5` is now in repo too: screenshot-based cross-device visual QA is implemented and passing via a deterministic blog fixture lane in Playwright. The next optional theming slice is `E6`: tiny bounded spacing/typography/perf micro-polish only if real-usage feedback shows concrete issues.

**Current public-site crawlability / clarity slice (2026-04-12):** The public homepage now uses the discoverability hero message `Make Your Website Discoverable by ChatGPT, Perplexity, and Gemini.`, keeps founder attribution off the hero, emits `SoftwareApplication` + `Organization` + `WebSite` + `FAQPage` JSON-LD, and adds a compact three-step `Audit -> Automate -> Integrate` flow plus a small references block. The startup dashboard overview now includes a compact Slack pointer card that routes operators to the existing connectors hub rather than duplicating Slack controls. Blog article pages now include a BLUF block and a small heading-derived contents list, and topic pages now mirror that structure with topic anchors and a jump list so long-form content is easier to scan and segment. The new public-site slices are intentionally small and do not replace the existing Slack connector dashboard or startup audit history views.
**Current public-site trust follow-up (2026-04-12):** Public pages now carry `GEO-Pulse Editorial` author/schema attribution, blog article meta descriptions are clamped to audit-friendly length, and the privacy page is published and linked from the footer. The intent is to close the latest audit trust gaps without adding extra UI noise or changing the guided homepage flow.
**Current public-site trust refinement (2026-04-12):** The blog index and pricing page now also include compact outbound reference blocks to strengthen trust signals while keeping the UI minimal. The intent remains to satisfy the audit and preserve the same small, premium, low-noise public layout.
**Current startup audit orchestration planning stream (2026-04-14):** GEO-Pulse extends the existing startup workspace system so one startup audit can become a durable execution run with repo-aware planning, role-based multimodel review, approval-gated PR execution, explicit manual follow-up tracking, and long-term improvement history. Current truth: the execution foundation, first dashboard visibility slice, richer execution-aware implementation-task model, strict orchestrator planner-output contract, role-level orchestration service/model routing, repo/DB/risk review artifact contracts, the first planning workflow helper, the approval gate, execution-aware PR linkage, manual operator wait/resume behavior, the dedicated orchestration module in the startup overview, benchmark-ready improvement-history rollups, the founder/admin execution-batch launch path, the first scheduled worker pickup for approved execution batches, and repo reservation / one-active-run guardrails are now in repo (`SAO-002` ... `SAO-018`) and documented in `PLAYBOOK/startup-audit-orchestration-v1.md`. This stream still extends the existing startup workspace / recommendation / implementation-plan / PR workflow spine, preserves rollout guardrails and the suggest-only default for `auto_pr`, and leaves actual GitHub branch / PR creation by an execution worker as the main remaining startup-orchestration slice.

**Current 100-topic execution queue (2026-04-03):** `F1` is now in repo: `/dashboard/content` can seed `batch_1` inventory from `docs/13-topic-registry-v1.json` into canonical `content_items` as article briefs, with idempotent skip behavior when a slug/content-id already exists. `F2` is now in repo too: `/dashboard/content` now surfaces batch coverage in-app (`batch_1`/`batch_2`/`batch_3`) with planned, seeded, ready, published, remaining, and progress percentages. `F3` is now in repo too: `/dashboard/content` now includes admin seed controls for `batch_2` and `batch_3` with the same idempotent safety pattern as `batch_1`. `F4` is now in repo too: `/dashboard/content` now includes a bounded drafting queue for article rows (`brief`/`draft`/`review`) with next-10 bucket views for blog-first execution. `F5` is now in repo too: queue rows now support lightweight owner/week assignment metadata plus in-app filters for session-based planning while newsletter pushes remain optional. `F6` is now in repo too: queue bulk-transition actions (`brief`->`draft`, `draft`->`review`, `review`->`approved`) now exist and respect current owner/week filter scope. `F7` is now in repo too: bounded approved-blog publish-wave controls now exist with filter-aware dry-run preview + execute, while newsletter distribution stays paused. `F8` is next: add post-wave outcome visibility (published count, blocked count, top block reasons) for faster rollout iteration.

**Current content quality trend slice (2026-04-03):** `D6` is now shipped on `/dashboard/content`: publish-check snapshot history is now rolled up into a compact cross-article trend panel with top failure patterns and regression flags so weekly review can catch drift before canonical publish.

**Current distribution-engine implementation stream (2026-04-03):** The repo still contains `PLAYBOOK/distribution-engine-v1-plan.md`, but this stream is no longer planning-only. Current truth: Phase A schema foundation is now in repo via `supabase/migrations/020_distribution_engine_foundation.sql` for generalized accounts, tokens, assets, media, jobs, and attempts. Phase B capability surfaces are also now in repo via `lib/server/distribution-engine-repository.ts`, `lib/server/distribution-engine-admin-data.ts`, and the first admin route at `/dashboard/distribution`. A first writable admin slice now exists too: `app/dashboard/distribution/actions.ts` plus `components/distribution-engine-admin-controls.tsx` can create/update accounts, save token rows, update account connection state, seed assets, create jobs, and manually trigger dispatch. The rollout is explicitly feature-flagged with `DISTRIBUTION_ENGINE_UI_ENABLED` and `DISTRIBUTION_ENGINE_WRITE_ENABLED`, and those flags are enforced server-side as well as in the UI. A first bounded dispatch seam now exists in `lib/server/distribution-job-dispatcher.ts`: it loads due jobs, records `distribution_job_attempts`, resolves linked account and asset rows, and reuses the current content-destination adapter seam for supported `content_item` flows. The next execution slice is also now in repo: `lib/server/distribution-job-schedule.ts` enqueues due jobs into `DISTRIBUTION_QUEUE`, `workers/queue/distribution-job-queue-consumer.ts` processes those jobs with provider-aware retry vs permanent-failure decisions plus DLQ terminal marking, and `workers/cloudflare-entry.ts` routes the dedicated distribution queue beside the existing report queue. Adapter expansion now follows a lean low-maintenance sequence (`1A` through `1F`) and is fully complete for the first text-first lane: `1A` scaffolds/tests, `1B` first `x`/`linkedin` runtime wiring, `1C` token-table credential reads in dispatcher, `1D` provider-aware retry classification, `1E` quick social seed path in admin, and `1F` hardening guardrails (dispatchable job-status defaults plus stricter social seed validation). OAuth lifecycle work is now started for social providers: `2A` adds first provider-native OAuth connect + callback/token persistence for `x` and `linkedin`, `2B` adds first dispatch-time token lifecycle handling, `2C` adds provider-specific retry backoff windows scheduled via `scheduled_for`, `2D` adds account-level backoff controls + retry observability in admin, and `2E` broadens refresh lifecycle support to LinkedIn refresh-token flow when available plus explicit reconnect UX guidance for token-expired LinkedIn accounts. Media foundation work has now started too: `3A` adds an operator media-replace seed form in `/dashboard/distribution`, asset media preview/readiness visibility in the admin table, and runtime dispatch guardrails that block media-required asset types when provider-ready media rows are missing; `3B` adds the first provider-native media publish path for LinkedIn `single_image_post` assets (register/upload/publish flow), `3C` extends that provider-native media runtime to LinkedIn `carousel_post` assets with bounded provider-ready slide validation, `3D` extends that provider-native media runtime to LinkedIn `short_video_post` assets with bounded provider-ready video validation, `3E` extends that provider-native media runtime to LinkedIn `long_video_post` assets (reusing the bounded LinkedIn video upload/publish flow) with provider-ready video validation, and `3F` adds the first non-LinkedIn media runtime path for X `single_image_post` assets (media upload + tweet publish) with bounded provider-ready image validation, while `3G` extends X media runtime to `short_video_post` assets with bounded provider-ready video validation, and `3H` extends X media runtime to `long_video_post` assets (reusing the bounded X video upload/publish flow) with provider-ready video validation; non-wired media provider/type combinations continue to fail with explicit bounded errors. Current limitation remains explicit: newsletter adapters still use env credentials and media publish coverage is still narrow. What is still not shipped: broader media publishing coverage (additional providers + richer provider-specific media behavior) and broader social/video adapter expansion. Strategic truth remains the same: GEO-Pulse stays canonical-first and review-first even as broader distribution capability is added.

**Current benchmark implementation (2026-03-27):** The first benchmark storage foundation is now in repo via migration `012_benchmark_foundation.sql`, the shared benchmark identity + repository seam is implemented in `lib/server/benchmark-domains.ts` and `lib/server/benchmark-repository.ts`, the first benchmark seed path plus runner input contract exist in `scripts/benchmark-seed.ts`, `lib/server/benchmark-seed.ts`, and `lib/server/benchmark-runner-contract.ts`, the first benchmark runner skeleton exists in `lib/server/benchmark-runner.ts` and `scripts/benchmark-runner.ts`, the runner depends on a benchmark-specific execution adapter boundary in `lib/server/benchmark-execution.ts`, the first conservative citation extraction path exists in `lib/server/benchmark-citations.ts`, the first separated metric computation helper exists in `lib/server/benchmark-metrics.ts`, the first admin query layer exists in `lib/server/benchmark-admin-data.ts`, and the first benchmark admin flow is now in repo:
- `/dashboard/benchmarks`
- `/dashboard/benchmarks/[runGroupId]`
- `/dashboard/benchmarks/domains/[domainId]`

Benchmark scale truth stays explicit too: `law_firms` remains the only mature recurring lane, the long-term target remains roughly 1000 benchmarked sites across 10 coherent domains, and the next planned domain-expansion slice is `tech_startups` through a narrow `b2b_software` cohort before any broader startup recurring schedule is enabled.

Current truth: the benchmark admin flow now supports an opt-in live Gemini lane for one configured model via `BENCHMARK_EXECUTION_*` env, while default behavior remains the safe stub adapter. Completed responses flow through citation parsing and metric computation, admins can inspect recent runs, drill into a run group, view one domainÃ¢â‚¬â„¢s benchmark history over time, add a benchmark domain from the admin UI, add a lightweight query set from the admin UI, and launch one benchmark run from the benchmark overview UI. Live provider failures now preserve the Gemini error body in `response_metadata`, and a run group is marked `failed` when every query run fails. First live benchmark proof now exists for a real customer domain with 6 completed queries, 4 extracted citations, and non-zero metrics on `gemini-2.5-flash-lite`; transient `503 UNAVAILABLE` responses now go through a light bounded retry/backoff path before a query run is marked failed. Grounded runs now support a first minimal site-derived grounding builder: when manual evidence is absent, the runner fetches the homepage plus likely about/services pages through the existing validated fetch path and derives a small bounded evidence set for `grounded_site`. Grounded citation rows now preserve exact-page provenance when a parsed citation URL exactly matches a grounded evidence page URL, and unresolved domain-only mentions remain explicitly unresolved rather than guessed. This is still a heuristic builder, not a full crawl, ranked page-selection system, or exact-page citation scorer.

**Operator note (2026-03-25):** Production checkout **`POST /api/checkout`** is rate-limited (**5 attempts per IP per hour**, `Retry-After: 3600` Ã¢â‚¬â€ see `lib/server/rate-limit-kv.ts` `checkCheckoutRateLimit`). Retesting paid checkout may require **waiting up to one hour** or using **another network/IP**. This is **not** a Stripe configuration failure by itself. **Sequencing:** **DA-001** Ã¢Å“â€¦ Ã‚Â· **DA-002** Ã¢Å“â€¦ Ã‚Â· **DA-003** Ã¢Å“â€¦ Ã‚Â· **DA-004** Ã¢Å“â€¦ Ã¢â‚¬â€ queue-scale deep-audit continuation is now implemented and unit-verified (`crawl_pending`, re-queue path, chunk metrics, 1000-page cap, targeted Vitest). **DA-005** Ã¢Å“â€¦ Ã¢â‚¬â€ optional Browser Rendering SPA fallback for paid deep audits, disabled by default unless explicit operator config is provided. **Workflows** are now an optional future enhancement for extreme-scale crawls, not a shipped-path blocker. **Orchestrator:** close **Phase 4 Ã¢â‚¬â€ Launch** first (`agents/ORCHESTRATOR.md` Ã‚Â§ *Phase 4 first Ã¢â‚¬â€ defer remaining Deep Audit*). **Next (Phase 4):** paste **P4-003 / P4-004 / P4-006** evidence in `COMPLETION_LOG.md`; then **Phase 4Ã¢â€ â€™Launch gate**.

**Deep Audit Upgrade (v2):** Tracked in Task Registry (**DA-001Ã¢â‚¬Â¦DA-005**); spec `.cursor/plans/report_depth_and_formats_fa7e556e.plan.md`, narrative [`PLAYBOOK/audit-upgrade.md`](../../PLAYBOOK/audit-upgrade.md). **DA-001** Ã¢Å“â€¦ **DONE** Ã¢â‚¬â€ `005_scan_runs_scan_pages.sql`, queue **v2**, `runDeepAuditCrawl`, multi-page PDF (operator-verified). **DA-002** Ã¢Å“â€¦ **DONE** Ã¢â‚¬â€ `workers/lib/fetch-gate.ts`, `validateEngineFetchUrl`, `robots-and-sitemap.ts`, `crawl-url-utils.ts` (section-aware cap), migration `006_scan_pages_section.sql`; **Security** formal sign-off on fetch path still recommended per `agents/SECURITY_AGENT.md`. **DA-003** Ã¢Å“â€¦ **DONE** Ã¢â‚¬â€ `DeepAuditReportPayload`, `build-deep-audit-markdown.ts`, R2 upload + `reports.pdf_url`, Resend attach vs link policy (`DEEP_AUDIT_ATTACH_MAX_BYTES`). **DA-004** Ã¢Å“â€¦ **DONE** Ã¢â‚¬â€ robots `Crawl-delay` + crawl metrics (`COMPLETION_LOG` DA-004-inc) **+ chunked queue re-invocation** (`crawl_pending` in `scan_runs.config`) **+ chunk-progress guardrails** **+ 1000-page cap** (`MAX_DEEP_AUDIT_PAGE_LIMIT`) **+ accurate coverage chunk reporting** **+ targeted multi-chunk Vitest proof**. **DA-005** Ã¢Å“â€¦ **DONE** Ã¢â‚¬â€ optional Browser Rendering-backed SPA fallback for paid deep audits (`workers/scan-engine/browser-rendering.ts`, deep-audit crawl + queue wiring, env/docs updates). Independent of Phase 4 closure; Orchestrator sets parallel vs sequential execution.

**How to close Phase 4:** Follow **`COMPLETION_LOG.md` Ã¢â€ â€™ *Phase 4 Ã¢â‚¬â€ operator execution order*** Ã¢â‚¬â€ numbered steps + **Stripe Live checkpoint** (after production hostname is fixed). Paste deploy + DNS + WAF + smoke evidence there; Orchestrator marks tasks and Phase 4Ã¢â€ â€™Launch gate per `agents/ORCHESTRATOR.md`.

**Phase 2 Ã¢â‚¬â€ CLOSED (implementation in repo; operator: apply migration `004_payments_guest_email.sql`, verify PDF &lt;60s + webhook in staging).** Checkout (`app/api/checkout`), Stripe webhook + idempotency (`app/api/webhooks/stripe`, `lib/server/stripe/checkout-completed.ts`), queue Ã¢â€ â€™ PDF Ã¢â€ â€™ Resend (`workers/queue/report-queue-consumer.ts`), DLQ replay (`workers/queue/dlq-replay.ts`). Tests: `npm run test`, `lib/server/stripe/checkout-completed.test.ts`.

**Phase 3 Ã¢â‚¬â€ CLOSED (implementation in repo).** Magic link (`app/login`, `app/login/actions.ts`), OAuth callback + guest purchase linking (`app/auth/callback`, `lib/server/link-guest-purchases.ts`), middleware session + CVE header (`middleware.ts`, `lib/supabase/middleware.ts`), dashboard (`app/dashboard`). Gate: sign in Ã¢â€ â€™ `/dashboard` lists `scans` / `reports` for `auth.uid()`; unauthenticated `/dashboard` Ã¢â€ â€™ `/login`.

**Prior phase goal (Phase 2):** Stripe checkout Ã¢â€ â€™ webhook (verified) Ã¢â€ â€™ queue Ã¢â€ â€™ pdf-lib report Ã¢â€ â€™ Resend delivery + idempotency + DLQ.

**Orchestrator sequencing:** Phases **2** and **3** are complete in repo; **Phase 4 Ã¢â‚¬â€ Launch** is in progress. **Deep Audit Upgrade:** **DA-001Ã¢â‚¬â€œ005** Ã¢Å“â€¦ in repo; this does **not** replace Phase 4 tasks or API deferral rules. **API-as-a-Service** (**API-002 Ã¢â‚¬Â¦ API-007**) starts only after **Phase 4Ã¢â€ â€™Launch gate** (see `ORCHESTRATOR.md`). **API-001** (schema) stays done.

**Phase 2Ã¢â€ â€™3 gate** (`agents/ORCHESTRATOR.md`): $1 test payment succeeds Ã‚Â· PDF delivered in &lt;60s Ã‚Â· Stripe webhook never processed without signature verification.

**Phase 3Ã¢â€ â€™4 gate** (`agents/ORCHESTRATOR.md`): Registered user can log in Ã‚Â· past scans visible Ã‚Â· auth middleware blocks unauthenticated routes.

**Operator note (2026-03-24):** $1 payment reported **working end-to-end**. Remaining gate items (PDF SLA, webhook verification, task evidence in `COMPLETION_LOG.md`) still required before advancing phase.

**Phase 1 Ã¢â‚¬â€ CLOSED (2026-03-24).** Free scan path implemented (landing, Turnstile, SSRF-gated fetch, 11 deterministic + 2 Gemini checks, weighted score, results + email gate, `scans`/`leads` via service role, KV rate limits). Evidence: `COMPLETION_LOG.md` *Phase 1 Ã¢â‚¬â€ implementation bundle*.

**Phase 1Ã¢â€ â€™2 gate (manual) Ã¢â‚¬â€ operator verified (2026-03-24):** End-to-end run on `https://techehealthservices.com/`: scan completed, **AI Search Readiness Score** returned, results page + email gate (Ã¢â‚¬Å“You are on the list.Ã¢â‚¬Â). Orchestrator may mark **ACCEPTED** per `ORCHESTRATOR.md` once `leads` row + optional KV rate-limit spot-check are confirmed in Supabase/CF dashboard.

**Phase 0 Ã¢â‚¬â€ CLOSED (2026-03-24).** Gate per `ORCHESTRATOR.md`: `wrangler dev` + Supabase tables/RLS + anon RLS check + type-check Ã¢â‚¬â€ see `COMPLETION_LOG.md` section *Phase 0 Ã¢â‚¬â€ P0-002 / P0-003 / P0-004 / P0-005 / P0-006 evidence bundle*.

**Phase 0 exit criteria (archived):**
- [x] `npm install` completes without errors
- [x] `wrangler dev` starts the dev server
- [x] Supabase project created; schema from `001_initial_schema.sql` (+ `002_api_keys.sql`) applied on remote `geo_pulse`
- [x] Core 6 tables + API tables: RLS enabled; `leads` verified via **anon** PostgREST (200 + `[]`)
- [x] `.cursor/rules/` files present in repo
- [x] `.dev.vars` present at repo root (from `.dev.vars.example` pattern)
- [x] `npm run type-check` returns 0 errors

---

## Task Registry

### Phase 0 Ã¢â‚¬â€ Scaffold
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| P0-001 | Scaffold files created | Orchestrator | Ã¢Å“â€¦ DONE | Files exist in repo |
| P0-002 | `npm install` passes | Backend | Ã¢Å“â€¦ DONE | COMPLETION_LOG Phase 0 bundle |
| P0-003 | Supabase project created + migration applied | Database | Ã¢Å“â€¦ DONE | COMPLETION_LOG Supabase audit + Phase 0 bundle |
| P0-004 | RLS verified via anon key | Security | Ã¢Å“â€¦ DONE | COMPLETION_LOG Phase 0 bundle (PostgREST `leads`) |
| P0-005 | `wrangler dev` runs locally | Backend | Ã¢Å“â€¦ DONE | COMPLETION_LOG Phase 0 bundle |
| P0-006 | Type check passes (0 errors) | QA | Ã¢Å“â€¦ DONE | COMPLETION_LOG Phase 0 bundle |

### Phase 1 Ã¢â‚¬â€ Core Scan Engine
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| P1-001 | Landing page + scan form | Frontend | Ã¢Å“â€¦ DONE | `app/page.tsx`, `components/scan-form.tsx`; COMPLETION_LOG P1 |
| P1-002 | Turnstile integration (form + server validation) | Backend | Ã¢Å“â€¦ DONE | `components/*`, `lib/server/turnstile.ts`, `app/api/scan`, `app/api/leads`; COMPLETION_LOG P1 |
| P1-003 | SSRF validator unit tests (all edge cases) | QA | Ã¢Å“â€¦ DONE | `workers/lib/ssrf.test.ts` (vitest); COMPLETION_LOG P1 |
| P1-004 | Scan Worker: fetch + HTMLRewriter parse | Backend | Ã¢Å“â€¦ DONE | `workers/scan-engine/fetch-page.ts` + `parse-signals.ts` (bounded fetch + signal extraction; streaming HTMLRewriter deferred to dedicated Worker hardening) |
| P1-005 | Deterministic checks (11 of 15) implemented | Backend | Ã¢Å“â€¦ DONE | `workers/scan-engine/checks/check-*.ts` (11 files) + `registry.ts` |
| P1-006 | Gemini integration (2 checks: Q&A + extractability) | Backend | Ã¢Å“â€¦ DONE | `workers/providers/gemini.ts`, `check-llm-*.ts` |
| P1-007 | Scoring engine (weighted 100-pt rubric) | Backend | Ã¢Å“â€¦ DONE | `workers/scan-engine/scoring.ts` |
| P1-008 | Results page: score + 3 issues + email gate | Frontend | Ã¢Å“â€¦ DONE | `app/results/[id]`, `components/results-view.tsx`, `score-display.tsx`, `email-gate.tsx` |
| P1-009 | Email capture Ã¢â€ â€™ Supabase leads table | Backend | Ã¢Å“â€¦ DONE | `app/api/leads/route.ts` (service_role insert) |
| P1-010 | Rate limiting (10 req/min/IP + 20/day/email) | Security | Ã¢Å“â€¦ DONE | `lib/server/rate-limit-kv.ts` + `SCAN_CACHE` binding |
| P1-011 | Phase 1 integration test: end-to-end scan | QA | Ã¢Å“â€¦ DONE | `npm run test` (vitest) + `npm run build`; manual e2e recommended before Phase 2 sign-off Ã¢â‚¬â€ COMPLETION_LOG P1 |

### Phase 2 Ã¢â‚¬â€ Payment + PDF + Email
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| P2-001 | Stripe $29 checkout integration | Backend | Ã¢Å“â€¦ DONE | `app/api/checkout/route.ts` |
| P2-002 | Stripe webhook handler (with signature verification) | Backend | Ã¢Å“â€¦ DONE | `app/api/webhooks/stripe/route.ts` |
| P2-003 | Cloudflare Queue: enqueue deep audit on payment | Backend | Ã¢Å“â€¦ DONE | `lib/server/stripe/ensure-deep-audit-job-queued.ts` |
| P2-004 | pdf-lib report generator | Backend | Ã¢Å“â€¦ DONE | `workers/report/build-deep-audit-pdf.ts` |
| P2-005 | Resend: HTML email + PDF attachment delivery | Backend | Ã¢Å“â€¦ DONE | `workers/report/resend-delivery.ts` |
| P2-006 | Dead-letter queue handler + retry logic | Backend | Ã¢Å“â€¦ DONE | `workers/queue/report-queue-consumer.ts`, `workers/queue/dlq-replay.ts` |
| P2-007 | Payment idempotency (Stripe event ID dedup) | Security | Ã¢Å“â€¦ DONE | `lib/server/stripe/checkout-completed.ts` |
| P2-008 | $1 test payment end-to-end test | QA | Ã¢Å“â€¦ DONE | COMPLETION_LOG Phase 4 Ã¢â‚¬â€ operator evidence; live payment `geo-pulse.uzzielt.workers.dev` verified checkout success return path |

### Phase 3 Ã¢â‚¬â€ Auth + Dashboard
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| P3-001 | Supabase magic link auth | Backend | Ã¢Å“â€¦ DONE | `app/login`, `app/login/actions.ts`, `lib/supabase/server.ts` |
| P3-002 | Auth middleware (CVE-2025-29927 protected) | Security | Ã¢Å“â€¦ DONE | `middleware.ts` (session + block `x-middleware-subrequest`); Next.js patched |
| P3-003 | User dashboard: scan history + past reports | Frontend | Ã¢Å“â€¦ DONE | `app/dashboard/page.tsx` |
| P3-004 | Auto-account creation post-payment | Backend | Ã¢Å“â€¦ DONE | `lib/server/link-guest-purchases.ts`, `app/auth/callback/route.ts`, `004_payments_guest_email.sql` |

### Phase 4 Ã¢â‚¬â€ Launch
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| P4-001 | Production deploy to Cloudflare Pages + Workers | Backend | Ã¢Å“â€¦ DONE | COMPLETION_LOG Phase 4 Ã¢â‚¬â€ operator evidence; prod URL + live Stripe + `lib/server/cf-env.ts` `pickEnvString` for Worker env |
| P4-002 | Share-your-score OG image generation | Frontend | Ã¢Å“â€¦ DONE | `app/results/[id]/opengraph-image.tsx`, `lib/server/get-scan-for-public-share.ts`, `page.tsx` `generateMetadata`; COMPLETION_LOG Phase 4 bundle |
| P4-003 | SPF + DKIM + DMARC configured | Security | Ã¢Â¬Å“ PENDING (operator blocked) | `getgeopulse.com` is now purchased; complete once DNS records are added in Cloudflare/Resend and evidence is attached |
| P4-004 | WAF rules enabled (CVE-2025-29927) | Security | Ã¢Â¬Å“ DEFERRED (paid CF) / mitigated | Managed WAF often needs **paid** Cloudflare Ã¢â‚¬â€ **mitigation in repo:** `middleware.ts` + patched Next.js; full WAF when upgraded Ã¢â‚¬â€ `COMPLETION_LOG` P4-004 paid-plan note; **P4-006** Security acknowledges |
| P4-005 | Supabase keep-alive cron configured | Backend | Ã¢Å“â€¦ DONE | `wrangler.jsonc` `triggers.crons`, `workers/cloudflare-entry.ts` `scheduled`; COMPLETION_LOG Phase 4 bundle |
| P4-006 | Launch security audit (all 5 blockers) | Security | Ã¢Â¬Å“ PENDING (waiting on operator blocker) | Repo-side evidence refreshed in COMPLETION_LOG `P4-006 Ã¢â‚¬â€ Launch security audit evidence bundle refresh`; final sign-off still depends on `P4-003` DNS evidence + `P4-004` launch-policy decision |

### Deep Audit Upgrade (multi-page paid audit Ã¢â‚¬â€ Ã¢â‚¬Å“v2Ã¢â‚¬Â)
> **Spec:** `.cursor/plans/report_depth_and_formats_fa7e556e.plan.md` (Ã‚Â§ Target architecture). **Narrative / executive summary:** [`PLAYBOOK/audit-upgrade.md`](../../PLAYBOOK/audit-upgrade.md). **Does not replace** Phase 4 or API deferral rules in `agents/ORCHESTRATOR.md` Ã¢â‚¬â€ Orchestrator sets **start date** and whether this runs **in parallel with** or **after** Phase 4 operator tasks. **Security** sign-off required on fetch-gate / SSRF changes (DA-002).

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| DA-001 | Phase 0: `scan_runs` / `scan_pages` + RLS; payment Ã¢â€ â€™ deep crawl + cap; free scan unchanged | Backend + Database | Ã¢Å“â€¦ DONE | COMPLETION_LOG DA-001 (Ã¢Å“â€¦ ACCEPTED); operator smoke: paid PDF for `https://techehealthservices.com/` Ã¢â‚¬â€ **Pages scanned** + per-page checklist (10 URLs, site aggregate score); migration `005_scan_runs_scan_pages.sql` |
| DA-002 | Phase 1: Central fetch gate (extend `workers/lib/ssrf.ts`); robots + sitemap streaming; section-aware sampling | Backend + Security | Ã¢Å“â€¦ DONE | `workers/lib/fetch-gate.ts`, `validateEngineFetchUrl`, `deep-audit-crawl` + robots/sitemap + `prioritizeUrlsBySection`; `006_scan_pages_section.sql`; `npm run test` 36 passed; COMPLETION_LOG DA-002 Ã¢â‚¬â€ **Security** review recommended |
| DA-003 | Reporting: `DeepAuditReportPayload`; PDF/MD; R2 + email link policy | Backend | Ã¢Å“â€¦ DONE | COMPLETION_LOG DA-003; `workers/report/*`, `report-queue-consumer.ts`, `wrangler.jsonc` `r2_buckets` + `DEEP_AUDIT_R2_PUBLIC_BASE`; CI runs `cf-typegen` |
| DA-004 | Phase 2: Scale (queue continuation, politeness, metrics) | Backend | Ã¢Å“â€¦ DONE | `deep-audit-crawl.ts`, `report-queue-consumer.ts`, `deep-audit-page-limit.ts`, `deep-audit-crawl.test.ts`; COMPLETION_LOG DA-004 incremental entries + DA-004 completion entry |
| DA-005 | Phase 3 (optional): Browser Rendering / SPA crawl | Backend | Ã¢Å“â€¦ DONE | `workers/scan-engine/browser-rendering.ts`, `deep-audit-crawl.ts`, queue/env wiring; COMPLETION_LOG DA-005 |

### Marketing Attribution Microservice (parallel with launch)
> **PRD:** `PLAYBOOK/marketing-attribution-weekly-report.md` (runbook). **Runs in parallel** with Phase 4 / product launch Ã¢â‚¬â€ does not block or replace any Phase 4 tasks. Service code in `services/marketing-attribution/`. Schema in `analytics` (Supabase, service-role only Ã¢â‚¬â€ no anon access). Client-side UTM capture via `gp_anon_id` cookie (middleware) + `gp_utm` sessionStorage (`lib/client/attribution.ts`). Weekly email report on Monday cron via Resend (opt-in: set `MARKETING_REPORT_TO` secret).

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| MA-001 | Foundation: `analytics` schema + `marketing_events` table + Zod contract + ingestion endpoint + idempotent tests | Backend + Database | Ã¢Å“â€¦ DONE | `007_marketing_attribution.sql` applied; `services/marketing-attribution/schema.ts`, `ingest.ts`, `emit.ts`; `app/api/internal/marketing/events/route.ts`; 39 new tests; type-check 0 errors, 89/89 tests |
| MA-002 | Wiring: client UTM/session capture + emit from scan/leads/checkout/webhook | Backend + Frontend | Ã¢Å“â€¦ DONE | `lib/client/attribution.ts`, `components/attribution-init.tsx`, middleware `gp_anon_id` cookie; emits `scan_started`, `scan_completed`, `lead_submitted`, `checkout_started`, `payment_completed`; 89/89 tests |
| MA-003 | Reporting: first-touch/last-touch views + weekly funnel aggregate + runbook | Backend + Database | Ã¢Å“â€¦ DONE | `008_marketing_attribution_views.sql` applied; `analytics.attribution_conversions_v1`, `analytics.channel_funnel_weekly_v1`; `PLAYBOOK/marketing-attribution-weekly-report.md` |
| MA-004 | Weekly email report via Monday cron + Resend | Backend | Ã¢Å“â€¦ DONE | `services/marketing-attribution/weekly-report.ts`; `workers/cloudflare-entry.ts` scheduled handler; opt-in via `MARKETING_REPORT_TO` env var |
| MA-005 | Hardening: queue-backed ingestion + replay + DLQ (optional) | Backend | Ã¢Â¬Å“ DEFERRED | Trigger: event volume > 1000/day or reliability issue |

### Admin & report quality eval (parallel with launch)
> **Does not replace** Phase 4 tasks. **Security:** `/admin/login` password path + `report_eval_runs` RLS Ã¢â‚¬â€ see `COMPLETION_LOG.md` ADM-001 Ã¢â‚¬Â¦ EVAL-004; Security sign-off recommended before treating as production-hardened.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| ADM-001 | Supabase Email password enabled + env docs | Operator + Backend | Ã¢Å“â€¦ DONE | `.env.local.example`, `.dev.vars.example`; Auth dashboard |
| ADM-002 | `/admin/login` + `signInWithPassword` + non-admin sign-out | Backend | Ã¢Å“â€¦ DONE | `app/admin/login/*`; COMPLETION_LOG ADM-001 Ã¢â‚¬Â¦ EVAL-004 |
| ADM-003 | Middleware public `/admin/login`; `/dashboard/*` unchanged | Security | Ã¢Å“â€¦ DONE | `middleware.ts` (no `/admin` auth wall beyond session) |
| EVAL-001 | `reports.markdown_url` + `report_payload_version`; Worker insert | Backend | Ã¢Å“â€¦ DONE | `009_admin_report_eval.sql`; `workers/queue/report-queue-consumer.ts` |
| EVAL-002 | `report_eval_runs` + RLS (no policies; service_role only) | Database | Ã¢Å“â€¦ DONE | `009_admin_report_eval.sql` |
| EVAL-003 | Structural rubric + `npm run eval:smoke` | Backend | Ã¢Å“â€¦ DONE | `lib/server/report-eval-structural.ts`, `scripts/report-eval-smoke.ts` |
| EVAL-004 | `/dashboard/evals` admin UI | Frontend | Ã¢Å“â€¦ DONE | `app/dashboard/evals/page.tsx` |

### UX Loading States (parallel with launch)
> **Does not replace** Phase 4 tasks. Focused on long-wait feedback for user actions that may exceed the fast-path threshold. Strategy: keep inline button pending states for quick requests, and escalate to a centralized delayed overlay only when the wait is materially longer.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| UX-001 | Centralized delayed long-wait loading overlay for scan / checkout / save / auth / report flows | Frontend | Ã¢Å“â€¦ DONE | `components/long-wait-provider.tsx`, `lib/client/loading-journeys.ts`; COMPLETION_LOG UX-001 |
| UX-002 | Define canonical user journey for scan Ã¢â€ â€™ preview Ã¢â€ â€™ pay / save Ã¢â€ â€™ report delivery with one visible step model | Product + Frontend | Ã¢Å“â€¦ DONE | `components/results-view.tsx`; COMPLETION_LOG UX-002 Ã¢â‚¬Â¦ UX-006 |
| UX-003 | Replace query-string-only checkout success messaging with state-driven payment / report status on results | Frontend + Backend | Ã¢Å“â€¦ DONE | `components/results-view.tsx`, `app/results/[id]/page.tsx`; COMPLETION_LOG UX-002 Ã¢â‚¬Â¦ UX-006 |
| UX-004 | Unify preliminary drafting and final report generation under one shared loading-story pattern | Frontend | Ã¢Å“â€¦ DONE | `lib/client/loading-journeys.ts`, `components/results-view.tsx`; COMPLETION_LOG UX-002 Ã¢â‚¬Â¦ UX-006 |
| UX-005 | Clarify branching on results page: free email-save path vs paid full-audit path, with explicit next-step framing | Frontend + Product | Ã¢Å“â€¦ DONE | `components/results-view.tsx`, `components/deep-audit-checkout.tsx`, `components/email-gate.tsx`; COMPLETION_LOG UX-002 Ã¢â‚¬Â¦ UX-006 |
| UX-006 | Add delivered / in-progress / action-needed states driven by real payment / report persistence instead of optimistic copy | Frontend + Backend | Ã¢Å“â€¦ DONE | `components/results-view.tsx`; COMPLETION_LOG UX-002 Ã¢â‚¬Â¦ UX-006 |
| UX-007 | End-to-end UX verification for guest and signed-in flows; verify shared journey-state behavior | QA + Product | Ã¢Å“â€¦ DONE | `lib/client/results-journey.test.ts`, `app/api/scans/[id]/route.ts`, `lib/server/get-scan-for-public-share.ts`; COMPLETION_LOG UX-007 |
| UX-008 | Tighten the results-to-report action layer so share snapshot is real, delivered-report copy stays truthful, and report view falls back cleanly when only PDF is available | Frontend + Product | Ã¢Å“â€¦ DONE | `components/results-view.tsx`, `components/score-display.tsx`, `components/report-viewer.tsx`, `tests/e2e/smoke.spec.ts`; COMPLETION_LOG UX-008 |

### v2 Enhancement Plan Ã¢â‚¬â€ Scoring Foundation + Check Expansion
> **Plan reference:** Task ledger only; prior `.cursor/plans/geopulse_v2_enhancement_audit_36a0ecee.plan.md` pointer is stale / not present in repo as of 2026-03-26. **Tier 1** (Scoring Foundation) completes the v2 status enum, category mapping, per-category scores, UI, and report integration. **Tier 2** adds new checks. **Does not replace** Phase 4 or API deferral rules.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| T1-1 | Extend `CheckResult` with v2 6-value status enum | Backend | Ã¢Å“â€¦ DONE | `workers/lib/interfaces/audit.ts` Ã¢â‚¬â€ `CheckStatus` type |
| T1-2 | Add `category` field to `AuditCheck`; map 17 checks Ã¢â€ â€™ 5 v2 categories | Backend | Ã¢Å“â€¦ DONE | All 17 check files + `registry.ts` + `audit.ts` `CheckCategory` |
| T1-3 | Compute per-category 0-100 scores in `scoring.ts` | Backend | Ã¢Å“â€¦ DONE | `computeCategoryScores`, `WeightedResult.category`; 96/96 tests |
| T1-4 | Surface 5 category scores in results UI | Frontend | Ã¢Å“â€¦ DONE | `score-display.tsx` Ã¢â‚¬â€ 5-pillar grid replaces 4-pillar; `results-view.tsx` threads `categoryScores` |
| T1-5 | Category breakdown in PDF + Markdown reports | Backend | Ã¢Å“â€¦ DONE | `build-deep-audit-markdown.ts`, `build-deep-audit-pdf.ts` Ã¢â‚¬â€ category table; `DeepAuditReportPayload.categoryScores` |
| T2-1 | New check: Schema.org @type validation | Backend | Ã¢Å“â€¦ DONE | `workers/scan-engine/checks/check-schema-types.ts`, `registry.ts` |
| T2-2 | New check: Image alt text presence | Backend | Ã¢Å“â€¦ DONE | `workers/scan-engine/checks/check-alt-text.ts`, `registry.ts` |
| T2-3 | New check: Content freshness signals | Backend | Ã¢Å“â€¦ DONE | `workers/scan-engine/checks/check-freshness.ts`, `registry.ts` |
| T2-4 | New check: External authority links | Backend | Ã¢Å“â€¦ DONE | `workers/scan-engine/checks/check-external-links.ts`, `registry.ts` |
| T2-5 | New check: Security headers | Backend | Ã¢Å“â€¦ DONE | `workers/scan-engine/checks/check-security-headers.ts`, `registry.ts` |
| T2-6 | Surface LLM confidence in issue display | Frontend | Ã¢Å“â€¦ DONE | `components/score-display.tsx`, `workers/scan-engine/run-scan.ts` |
| T3-6 | In-browser rendered markdown report view | Frontend | Ã¢Å“â€¦ DONE | `components/report-viewer.tsx`, `app/results/[id]/report/page.tsx` |
| T3-7 | Dynamic interactive report view | Frontend | Ã¢Å“â€¦ DONE | `components/report-viewer.tsx`; interactive summary + section chips + collapsible report sections; type-check + build |

### Audit Closure Ã¢â‚¬â€ Report Integrity + Product Truth
> **Source:** 2026-03-26 implementation audit. These tasks convert audit findings into explicit, bite-size follow-up work. They do **not** override Phase 4 launch sequencing; Orchestrator decides execution order.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| AU-001 | Report payload: separate `highlightedIssues` from full evaluated sitewide issue set | Backend | Ã¢Å“â€¦ DONE | COMPLETION_LOG AU-001 Ã¢â‚¬Â¦ AU-005 / AU-007 |
| AU-002 | PDF + Markdown: render true full-check breakdown instead of top-issues-only | Backend | Ã¢Å“â€¦ DONE | COMPLETION_LOG AU-001 Ã¢â‚¬Â¦ AU-005 / AU-007 |
| AU-003 | Preserve v2 statuses (`PASS` / `FAIL` / `BLOCKED` / `NOT_EVALUATED` / `LOW_CONFIDENCE` / `WARNING`) in report UI/PDF/MD | Backend + Frontend | Ã¢Å“â€¦ DONE | COMPLETION_LOG AU-001 Ã¢â‚¬Â¦ AU-005 / AU-007 |
| AU-004 | Deep audit scan persistence: keep `categoryScores` and report payload metadata consistent in `full_results_json` | Backend | Ã¢Å“â€¦ DONE | COMPLETION_LOG AU-001 Ã¢â‚¬Â¦ AU-005 / AU-007 |
| AU-005 | Add coverage summary, blocked/error counts, and crawl notes to customer-facing report | Backend | Ã¢Å“â€¦ DONE | COMPLETION_LOG AU-001 Ã¢â‚¬Â¦ AU-005 / AU-007 |
| AU-006 | Add technical appendix section (robots, schema findings, headers) to Markdown/PDF | Backend | Ã¢Å“â€¦ DONE | COMPLETION_LOG AU-006 / AU-008 |
| AU-007 | Product truth pass: align landing/report/checkout copy with actual implemented checks and deliverables | Architect + Frontend | Ã¢Å“â€¦ DONE | COMPLETION_LOG AU-001 Ã¢â‚¬Â¦ AU-005 / AU-007 |
| AU-008 | Security/docs truth pass: reconcile SSRF documentation with actual runtime protections and limitations | Security + Architect | Ã¢Å“â€¦ DONE | COMPLETION_LOG AU-006 / AU-008 |
| AU-009 | Replace structural-only report eval with content-integrity assertions (check count, statuses, appendix, page mapping) | QA + Backend | Ã¢Å“â€¦ DONE | COMPLETION_LOG AU-009 / AU-010 |
| AU-010 | Add golden-report fixtures covering blocked, low-confidence, and multi-page cases | QA | Ã¢Å“â€¦ DONE | COMPLETION_LOG AU-009 / AU-010 |

### Retrieval / Evaluation Backlog
> **Status:** Retrieval / eval foundation exists in repo (`RE-001` Ã¢â‚¬Â¦ `RE-007`), including deterministic retrieval simulation, a local `promptfoo` harness, and a documented `ragas` no-go decision. `ragas` runtime integration, benchmark engine, and prompt-cluster analysis are still not implemented as of 2026-03-26.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| RE-001 | Define retrieval-eval scope: prompts, sampled pages, expected outputs, and non-goals for MVP vs v2 | Architect | Ã¢Å“â€¦ DONE | `PLAYBOOK/retrieval-eval-foundation.md`; COMPLETION_LOG RE-001 / RE-002 |
| RE-002 | Add retrieval-ready data model for eval runs, prompt sets, passages, and scored answers | Architect + Database | Ã¢Å“â€¦ DONE | `supabase/migrations/010_retrieval_eval_foundation.sql`; COMPLETION_LOG RE-001 / RE-002 |
| RE-003 | Build minimal retrieval simulation harness over scanned pages/passages (no external benchmark claims yet) | Backend | Ã¢Å“â€¦ DONE | `lib/server/retrieval-eval.ts`, COMPLETION_LOG RE-003 |
| RE-004 | Add benchmark-percentile computation design or explicitly remove percentile claims until implemented | Architect + Backend | Ã¢Å“â€¦ DONE | `PLAYBOOK/benchmark-percentile-design.md`, doc/contract truth pass, COMPLETION_LOG RE-004 |
| RE-005 | Add `promptfoo` harness skeleton for prompt regression tests against representative report/retrieval cases | QA + Backend | Ã¢Å“â€¦ DONE | `eval/promptfoo/*`, `scripts/run-promptfoo.cjs`, `package.json`, COMPLETION_LOG RE-005 |
| RE-006 | Add first `promptfoo` suites for executive summary quality, fix specificity, and status preservation | QA | Ã¢Å“â€¦ DONE | `eval/promptfoo/promptfooconfig.report.yaml`, COMPLETION_LOG RE-006 |
| RE-007 | Evaluate `ragas` fit for retrieval faithfulness / answer relevance; produce go/no-go note before implementation | Architect + QA | Ã¢Å“â€¦ DONE | `PLAYBOOK/ragas-fit-evaluation.md`, COMPLETION_LOG RE-007 |
| RE-008 | If approved, add `ragas`-based offline eval pipeline for retrieval runs and answer faithfulness | QA + Backend | Ã¢Â¬Å“ PENDING | Ã¢â‚¬â€ |
| RE-009 | Add prompt-cluster / demand-layer research backlog item with clear dependency on retrieval foundation | Architect | Ã¢Â¬Å“ PENDING | Ã¢â‚¬â€ |
| RE-010 | Add citation / share-of-voice benchmarking backlog item with external-source methodology and limits | Architect + Backend | Ã¢Â¬Å“ PENDING | Ã¢â‚¬â€ |
| RE-011 | Add stable eval-run identity for site-level history (`framework`, `domain`, `site_url`, prompt-set metadata) | Backend + Database | Ã¢Å“â€¦ DONE | `011_eval_run_metadata.sql`; COMPLETION_LOG RE-011 Ã¢â‚¬Â¦ RE-015 |
| RE-012 | Persist Promptfoo runs into Supabase for admin analytics | Backend | Ã¢Å“â€¦ DONE | `scripts/promptfoo-eval-write.ts`, `lib/server/promptfoo-results.ts`; COMPLETION_LOG RE-011 Ã¢â‚¬Â¦ RE-015 |
| RE-013 | Upgrade `/dashboard/evals` into site/framework trend analytics across report + retrieval runs | Frontend | Ã¢Å“â€¦ DONE | `app/dashboard/evals/page.tsx`; COMPLETION_LOG RE-011 Ã¢â‚¬Â¦ RE-015 |
| RE-014 | Add Promptfoo result parsing helpers + metrics needed for trend cards and run tables | Backend + QA | Ã¢Å“â€¦ DONE | `lib/server/promptfoo-results.ts`, `lib/server/promptfoo-results.test.ts`; COMPLETION_LOG RE-011 Ã¢â‚¬Â¦ RE-015 |
| RE-015 | Add admin/internal RAGAS lane placeholder without shipping a runtime RAGAS pipeline | Architect + Frontend | Ã¢Å“â€¦ DONE | `/dashboard/evals` framework filter + empty-state note; COMPLETION_LOG RE-011 Ã¢â‚¬Â¦ RE-015 |
| RE-016 | If approved, add `ragas`-based offline writer + persistence into retrieval analytics | QA + Backend | Ã¢Â¬Å“ PENDING | Ã¢â‚¬â€ |
| RE-017 | Add deterministic retrieval-eval writer that persists aggregate run + prompt / passage / answer rows | Backend + QA | Ã¢Å“â€¦ DONE | `scripts/retrieval-eval-write.ts`, `lib/server/retrieval-eval-writer.ts`; COMPLETION_LOG RE-017 |
| RE-018 | Add retrieval-run admin drilldown and make landing header auth-aware (`Dashboard` only when logged in) | Frontend + Backend | Ã¢Å“â€¦ DONE | `app/dashboard/evals/retrieval/[id]/page.tsx`, `components/site-header.tsx`; COMPLETION_LOG RE-018 |

### API-as-a-Service Layer (deferred Ã¢â‚¬â€ start after Phase 4 launch readiness)
> **Phase 3Ã¢â€ â€™4 gate** is satisfied (auth + dashboard in repo). **API-002 Ã¢â‚¬Â¦ API-007** remain deferred until **Orchestrator** clears **Phase 4 Ã¢â‚¬â€ Launch** (at minimum **P4-001** production deploy + security evidence per `ORCHESTRATOR.md` Phase 4Ã¢â€ â€™Launch gate). **API-001** remains complete.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| API-001 | API key schema + migration | Database | Ã¢Å“â€¦ DONE | `002_api_keys.sql` on remote; COMPLETION_LOG Supabase audit |
| API-002 | API key issuance + validation Worker | API | Ã¢Â¬Å“ DEFERRED | Starts after Phase 4 launch gate (Orchestrator) |
| API-003 | OpenAPI spec v1 | API + Architect | Ã¢Â¬Å“ DEFERRED | Starts after Phase 4 launch gate |
| API-004 | Rate limiting per API key | API + Security | Ã¢Â¬Å“ DEFERRED | Starts after Phase 4 launch gate |
| API-005 | API billing tiers (free/pro/enterprise) | API | Ã¢Â¬Å“ DEFERRED | Starts after Phase 4 launch gate |
| API-006 | Webhook delivery for async results | API | Ã¢Â¬Å“ DEFERRED | Starts after Phase 4 launch gate |
| API-007 | API documentation site | API + Frontend | Ã¢Â¬Å“ DEFERRED | Starts after Phase 4 launch gate |

### Measurement Platform (parallel planning / staged internal foundation)
> **Vision source:** `PLAYBOOK/measurement-platform-roadmap.md` + `PLAYBOOK/v3 aretefacts/*`. This initiative keeps the current audit/report business intact while adding a future measurement layer for AI visibility. It may proceed as planning and tightly scoped internal foundation work while Phase 4 is externally blocked, but it does **not** replace launch closure or change API deferral truth.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| BM-001 | Freeze architectural split: current app vs future measurement layer; record non-goals and staged rollout | Architect | Ã¢Å“â€¦ DONE | `PLAYBOOK/measurement-platform-roadmap.md` |
| BM-002 | Define first benchmark schema set (`benchmark_domains`, `benchmark_query_sets`, `benchmark_queries`, `benchmark_run_groups`, `query_runs`, `query_citations`, `benchmark_domain_metrics`) | Architect + Database | Ã¢Å“â€¦ DONE | `PLAYBOOK/measurement-schema-v1.md` |
| BM-003 | Add LiteLLM integration plan and provider abstraction boundaries | Backend + Architect | Ã¢Å“â€¦ DONE | `PLAYBOOK/litellm-integration-plan.md` |
| BM-004 | Add Langfuse integration plan for traces, datasets, and eval experiments | Backend + Architect | Ã¢Å“â€¦ DONE | `PLAYBOOK/langfuse-integration-plan.md` |
| BM-005 | Design internal benchmark runner for one domain / one query set / one model lane | Backend | Ã¢Å“â€¦ DONE | `PLAYBOOK/benchmark-runner-v1.md` |
| BM-006 | Define citation extraction v1 contract and metric computation v1 (`citation_rate`, `query_coverage`, `share_of_voice`) | Architect + Backend | Ã¢Å“â€¦ DONE | `PLAYBOOK/citation-and-metrics-v1.md` |
| BM-007 | Design internal admin benchmark views and run controls before any customer-facing benchmark UI | Frontend + Product | Ã¢Å“â€¦ DONE | `PLAYBOOK/benchmark-admin-ui-v1.md` |
| BM-008 | Define phased scale path: 20Ã¢â‚¬â€œ50 domains Ã¢â€ â€™ 100Ã¢â‚¬â€œ200 Ã¢â€ â€™ 500Ã¢â‚¬â€œ1000 with isolation from customer flows | Architect + Ops | Ã¢Å“â€¦ DONE | `PLAYBOOK/benchmark-scale-path.md` |
| BM-009 | Add first benchmark foundation migration for internal domains, query sets, run groups, query runs, citations, and aggregate metrics | Backend + Database | Ã¢Å“â€¦ DONE | `supabase/migrations/012_benchmark_foundation.sql` |
| BM-010 | Add benchmark-domain normalization helpers and first typed DB access layer | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-domains.ts`, `lib/server/benchmark-repository.ts` |
| BM-011 | Build first benchmark query-set/domain seeding path and runner input contract | Backend | Ã¢Å“â€¦ DONE | `scripts/benchmark-seed.ts`, `lib/server/benchmark-seed.ts`, `lib/server/benchmark-runner-contract.ts` |
| BM-012 | Build first benchmark runner skeleton for one domain / one query set / one model lane without model execution yet | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-runner.ts`, `scripts/benchmark-runner.ts` |
| BM-013 | Add provider-facing benchmark model response contract and stub execution adapter boundary | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-execution.ts` |
| BM-014 | Add first benchmark citation extraction contract implementation and write `query_citations` rows from normalized responses | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-citations.ts` |
| BM-015 | Add first real benchmark metric computation helper and separate it from runner orchestration | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-metrics.ts` |
| BM-016 | Add benchmark-run admin query layer for run groups, query runs, citations, and domain history | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-admin-data.ts` |
| BM-017 | Build `/dashboard/benchmarks` overview page with filters, summary cards, and recent run groups | Frontend + Backend | Ã¢Å“â€¦ DONE | `app/dashboard/benchmarks/page.tsx` |
| BM-018 | Build `/dashboard/benchmarks/[runGroupId]` detail page for one benchmark run group | Frontend + Backend | Ã¢Å“â€¦ DONE | `app/dashboard/benchmarks/[runGroupId]/page.tsx` |
| BM-019 | Build `/dashboard/benchmarks/domains/[domainId]` history page for one benchmark domain | Frontend + Backend | Ã¢Å“â€¦ DONE | `app/dashboard/benchmarks/domains/[domainId]/page.tsx` |
| BM-020 | Add admin benchmark run trigger flow from the UI for one domain / one query set / one model lane | Frontend + Backend | Ã¢Å“â€¦ DONE | `app/dashboard/benchmarks/actions.ts`, `components/benchmark-trigger-form.tsx` |
| BM-021 | Add first non-stub benchmark execution adapter path for a single configured model lane | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-execution.ts`, `app/dashboard/benchmarks/actions.ts`, `scripts/benchmark-runner.ts` |
| BM-022 | Add benchmark-domain onboarding from the admin UI and align the default trigger lane with the configured live benchmark lane | Frontend + Backend | Ã¢Å“â€¦ DONE | `components/benchmark-domain-form.tsx`, `app/dashboard/benchmarks/page.tsx`, `app/dashboard/benchmarks/actions.ts` |
| BM-023 | Add benchmark query-set onboarding from the admin UI for lightweight real-domain testing | Frontend + Backend | Ã¢Å“â€¦ DONE | `components/benchmark-query-set-form.tsx`, `app/dashboard/benchmarks/actions.ts`, `app/dashboard/benchmarks/page.tsx` |
| BM-024 | Add light retry/backoff handling for temporary benchmark-provider overload (`503 UNAVAILABLE`) before marking a query run failed | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-execution.ts` |
| BM-025 | Define grounded benchmark v2 methodology separating `ungrounded_inference` from `grounded_site` interpretation without skipping BM-024 sequencing | Architect + Backend | Ã¢Å“â€¦ DONE | `PLAYBOOK/benchmark-grounding-v2.md` |
| BM-026 | Add the first grounded benchmark implementation slice: explicit run mode, grounding-context seam, and safe prompt-path gating without breaking ungrounded runs | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-runner.ts`, `lib/server/benchmark-execution.ts`; COMPLETION_LOG BM-026 |
| BM-027 | Improve grounded benchmark citation recovery by prompting explicit target-domain mention and validating alias-based citation parsing | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-citations.test.ts`; COMPLETION_LOG BM-027 |
| BM-028 | Define the next grounded benchmark design step: structured page-level provenance (`page_url`, `page_type`, `excerpt`) so grounded answers can be tied to exact source pages rather than a loose evidence blob | Architect + Backend | Ã¢Å“â€¦ DONE | `PLAYBOOK/benchmark-grounding-v2.md`, `PLAYBOOK/measurement-platform-roadmap.md`; COMPLETION_LOG BM-028 |
| BM-029 | Adopt the synthesized grounded-site production prompt from cross-model prompt research and replace the earlier benchmark prompt wording | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-grounding.test.ts`; COMPLETION_LOG BM-029 |
| BM-030 | Add the first structured grounding-provenance implementation slice: normalize page-level evidence, persist a run-time grounding snapshot, and expose it in benchmark run detail without breaking older evidence blobs | Backend + Frontend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-runner.ts`, `app/dashboard/benchmarks/[runGroupId]/page.tsx`; COMPLETION_LOG BM-030 |
| BM-031 | Add the first minimal site-derived grounding-context builder so grounded runs can derive evidence from homepage/about/services pages when manual evidence is absent | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-runner.ts`; COMPLETION_LOG BM-031 |
| BM-032 | Add the first exact-url grounded citation provenance slice so citation rows preserve matched source-page evidence without guessing unresolved mentions | Backend + Frontend | Ã¢Å“â€¦ DONE | `supabase/migrations/014_benchmark_citation_grounding_provenance.sql`, `lib/server/benchmark-citations.ts`, `lib/server/benchmark-runner.ts`, `components/benchmark-run-detail-view.tsx`; COMPLETION_LOG BM-032 |
| BM-033 | Define the next grounded-provenance sequence so future work stays narrow: ranked page selection, excerpt-level evidence matching, citation-quality scoring, and scale work must remain separate slices with explicit non-goals | Architect + Backend | Ã¢Å“â€¦ DONE | `PLAYBOOK/benchmark-grounding-v2.md`, `PLAYBOOK/measurement-platform-roadmap.md`; COMPLETION_LOG BM-033 |
| BM-034 | Add a bounded ranked grounding-candidate selector for grounded runs so the builder prefers the strongest few same-origin pages instead of only homepage/about/services heuristics | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-grounding.test.ts`; COMPLETION_LOG BM-034 |
| BM-035 | Persist richer grounding-page metadata (`fetch_order`, `selection_reason`, page title/status) for grounded evidence without changing the current benchmark admin flow shape | Backend + Database | Ã¢Å“â€¦ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-run-detail.ts`, `components/benchmark-run-detail-view.tsx`; COMPLETION_LOG BM-035 |
| BM-036 | Add a second provenance matcher that can resolve citations to grounded evidence by normalized page equivalence beyond exact URL string match while keeping unresolved cases explicit | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-citations.ts`, `lib/server/benchmark-citations.test.ts`; COMPLETION_LOG BM-036 |
| BM-037 | Add excerpt-level claim-to-evidence matching for grounded runs as internal metadata only, without turning it into a customer-facing score | Backend + QA | Ã¢Å“â€¦ DONE | `lib/server/benchmark-citations.ts`, `lib/server/benchmark-runner.ts`, `lib/server/benchmark-citations.test.ts`; COMPLETION_LOG BM-037 |
| BM-038 | Define and implement the first exact-page citation-quality metric slice for grounded runs, separated from citation presence and share-of-voice | Architect + Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-metrics.ts`, `lib/server/benchmark-runner.ts`, `components/benchmark-run-detail-view.tsx`; COMPLETION_LOG BM-038 |
| BM-039 | Add benchmark comparison views for the same domain/query set across `ungrounded_inference` and `grounded_site` so methodology gains are inspectable over time | Frontend + Backend | Ã¢Å“â€¦ DONE | `app/dashboard/benchmarks/domains/[domainId]/page.tsx`, `lib/server/benchmark-admin-data.ts`, `lib/server/benchmark-admin-data.test.ts`; COMPLETION_LOG BM-039 |
| BM-040 | Add recurring internal benchmark scheduling and run-label/version discipline so benchmark history is comparable without using the customer request path | Backend + Ops | Ã¢Å“â€¦ DONE | `lib/server/benchmark-schedule.ts`, `workers/cloudflare-entry.ts`, `lib/server/benchmark-runner.ts`; COMPLETION_LOG BM-040 |
| BM-041 | Define competitor/cohort benchmark methodology, guardrails, and evidence limits before any broader comparative benchmark claims or cohort UI are implemented | Architect + Product | Ã¢Å“â€¦ DONE | `PLAYBOOK/benchmark-competitor-cohort-methodology-v1.md`; COMPLETION_LOG BM-041 |
| BM-042 | Add the first competitor/cohort benchmark storage and admin slice for a narrow internal cohort only, after BM-041 freezes methodology | Backend + Frontend | Ã¢Å“â€¦ DONE | `supabase/migrations/015_benchmark_cohorts.sql`, `app/dashboard/benchmarks/domains/[domainId]/page.tsx`; COMPLETION_LOG BM-042 |
| BM-043 | Add the first benchmark query/response/citation lineage view so one run can be inspected as a real benchmark graph rather than disconnected tables | Backend + Frontend | Ã¢Å“â€¦ DONE | `components/benchmark-run-detail-view.tsx`, `lib/server/benchmark-run-detail.ts`; COMPLETION_LOG BM-043 |
| BM-044 | Add the first multi-model benchmark lane beyond the current single configured live lane while keeping provider abstraction and admin controls simple | Backend | Ã¢Å“â€¦ DONE | `lib/server/benchmark-execution.ts`, `app/dashboard/benchmarks/page.tsx`, `docs/06-environment-and-secrets.md`; COMPLETION_LOG BM-044 |
| BM-045 | Add benchmark-scale operator hardening for 100Ã¢â‚¬â€œ200 domain internal runs with explicit isolation, retry policy, and failure visibility before any 500Ã¢â‚¬â€œ1000 site push | Backend + Ops | Ã¢Å“â€¦ DONE | `lib/server/benchmark-schedule.ts`, `lib/server/benchmark-schedule.test.ts`, `docs/06-environment-and-secrets.md`; COMPLETION_LOG BM-045 |
| BM-046 | Revisit the 500Ã¢â‚¬â€œ1000 site benchmark operations path only after BM-045 evidence exists and operator cost/failure tradeoffs are documented | Architect + Ops | Ã¢Å“â€¦ DONE | `PLAYBOOK/benchmark-operations-decision-v1.md`, `PLAYBOOK/benchmark-scale-path.md`; COMPLETION_LOG BM-046 |

| BM-047 | Freeze the first `tech_startups` benchmark methodology as a narrow `b2b_software` cohort before seeding or recurring collection begins | Architect + Product | DONE | `PLAYBOOK/benchmark-tech-startups-b2b-software-methodology-v1.md` |
| BM-048 | Add the first frozen startup benchmark query-set fixture for the `tech_startups` `b2b_software` cohort without widening scheduler scope | Backend + Product | DONE | `eval/fixtures/benchmark-tech-startups-b2b-software-v1-query-set.json` |
| BM-049 | Seed the first 15-25 domain `tech_startups` target cohort and keep it separate from the mature `law_firms` recurring lane | Backend + Ops | DONE | `eval/fixtures/benchmark-tech-startups-b2b-software-v1-domains.json`, `lib/server/benchmark-domain-cohort-seed.ts`, `scripts/benchmark-seed-domain-cohort.ts`, `lib/server/benchmark-domain-cohort-seed.test.ts` |
| BM-050 | Run one narrow startup benchmark proof window and inspect grounded vs ungrounded failures before any recurring startup schedule is enabled | Backend + Ops | DONE | Startup proof run groups `837ba287-faae-48e0-a872-44a7c5c88b19`, `470ae655-c32f-4cd3-ba22-3ebee6915a5b`, `d9fb1213-6221-4fe0-882a-e2061477b680`, `2aa7e75f-a83e-4919-884d-328cfc0b5d04`, `950eeb83-de14-4e5d-836f-949bca117b8d`, `7da423e5-9800-4c12-bb9e-bac2b7cac418`, `e6c0d67d-9dcb-4983-8bda-4fa59a45ac67`, `b8101ca5-f8cb-465d-a414-46fdb63ad37b`; targeted diagnostic + metrics review |
| BM-051 | Freeze the first recurring `tech_startups` pilot as a four-domain `b2b_software` frame on startup query-set `v2`, using the existing schedule path only | Architect + Ops | DONE | `PLAYBOOK/benchmark-tech-startups-collection-pilot-v1.md`, `PLAYBOOK/benchmark-tech-startups-b2b-software-methodology-v1.md`, `docs/01-current-state.md` |
| BM-052 | Add a dedicated startup recurring-pilot domain fixture that enables schedule selection only for the four `v2`-proofed startup domains | Backend + Ops | DONE | `eval/fixtures/benchmark-tech-startups-b2b-software-v2-pilot-domains.json` |
| BM-053 | Seed the four-domain startup recurring-pilot fixture and run one explicit scheduled startup pilot window before widening beyond the proof lane | Backend + Ops | DONE | Pilot cohort seeded from `eval/fixtures/benchmark-tech-startups-b2b-software-v2-pilot-domains.json`; preview + run-now + summary + outliers completed for `2026-04-17T00`; run groups `c5e287ce-30a4-4205-b614-1974449c0381`, `2d4241b6-636c-4f67-b356-62a7eb91204f`, `8fa220db-c5ad-4572-9312-a232d5badf3e`, `d8b75466-ef44-4852-94f4-804cdc412565`, `d7c18256-e53d-444a-9f9b-b6c9de98225b`, `2b39d469-35d4-4f5c-afc4-d236abd1c53a`, `95b44f5a-dec1-42ec-bc65-3dbef19477d8`, `32f9c258-ec67-448d-9aac-e5aae3f65c31` |
| BM-054 | Collect at least three comparable startup pilot windows and use the recurrence helper before deciding whether the startup lane should widen beyond the four-domain allowlist | Backend + Ops | DONE | Windows `2026-04-17T00`, `2026-04-17T12`, `2026-04-18T00`; recurrence review completed via `benchmark:schedule:recurrence`; one-off `replicate.com` grounded dip at `83%` on `2026-04-17T12` but no recurring winners or laggards |
| BM-055 | Inspect the startup pilot's one-off grounded regressions, starting with `replicate.com` on `2026-04-17T12`, before widening the startup recurring lane | Backend + Ops | DONE | Grounded run `bb5a1e80-a5d5-4f33-a9ed-bfe275811325` reviewed against adjacent grounded runs `32f9c258-ec67-448d-9aac-e5aae3f65c31` and `2b4457b9-48a3-4695-a229-615c5acaabbd`; the dip was one missing measured-domain citation on `product-differentiators`, with all three runs still domain-only and no grounding matcher/provenance failure |
| BM-056 | Freeze the post-`BM-055` startup widening decision: either hold the lane at the four-domain pilot or approve one next bounded `b2b_software` cohort expansion with explicit guardrails | Architect + Ops | DONE | Decision freeze: hold at the four-domain startup pilot for now. Reason: `BM-055` cleared the `replicate.com` dip as one-off citation omission, but the three-window pilot still shows neutral recurrence and no stable grounded advantage beyond the existing bounded lane |
| BM-057 | If `BM-056` approves widening, define and seed the next bounded startup cohort fixture without enabling the full 18-domain startup set or changing the scheduler contract | Backend + Ops | PENDING | New bounded startup cohort fixture + seed proof kept separate from the four-domain pilot and mature `law_firms` lane |
| BM-058 | If `BM-057` ships, run one widened startup proof window through the existing summary/outlier flow and confirm the larger cohort stays low-touch before any recurring widening | Backend + Ops | PENDING | Preview + run-now + summary + outlier evidence for one widened startup window, with explicit comparison back to the four-domain pilot frame |

### Startup dashboard + centralized entitlements (planning + staged implementation)
> New stream is now tracked with a concrete implementation plan in `docs/15-startup-dashboard-entitlements-plan.md` and strategy notes in `PLAYBOOK/startup-dashboard-entitlements-v1.md`. This stream does not mark Phase 4 launch closure and must remain feature-flagged + centrally gated.

| TASK ID | Description | Owner | Status | Evidence |
|---|---|---|---|---|
| SD-001 | Service catalog + entitlement schema foundation (`services`, `bundles`, `bundle_services`, workspace/client overrides, model policy scope) | Backend + Database | Ã¢Å“â€¦ DONE | `supabase/migrations/021_service_entitlements_foundation.sql`, `lib/server/service-entitlements-contract.ts`, COMPLETION_LOG 2026-04-04 |
| SD-002 | Unified entitlement resolver with deterministic precedence and tests | Backend | Ã¢Å“â€¦ DONE | `lib/server/service-entitlements.ts`, `lib/server/service-entitlements.test.ts`, `lib/server/agency-access.ts`, COMPLETION_LOG 2026-04-04 |
| SD-003 | Admin service control center (free/paid/trial/off, bundle toggles, audit logs) | Frontend + Backend | Ã¢Å“â€¦ DONE | `/dashboard/services`, `app/dashboard/services/actions.ts`, `components/service-control-admin-view.tsx`, COMPLETION_LOG 2026-04-04 |
| SD-004 | Startup workspace model (founder/team roles) on shared tenancy base | Backend + Database | Ã¢Å“â€¦ DONE | `supabase/migrations/022_startup_workspace_foundation.sql`, `/dashboard/startups`, `lib/server/startup-dashboard-data.ts`, COMPLETION_LOG 2026-04-04 |
| SD-005 | Startup dashboard shell (dark theme, startup-specific IA) | Frontend | Ã¢Å“â€¦ DONE | `/dashboard/startup`, `lib/server/startup-dashboard-shell.ts`, COMPLETION_LOG 2026-04-04 |
| SD-006 | Action-only tracking graphs + linked task lists (trend, burn-down, implementation funnel) | Frontend + Backend | Ã¢Å“â€¦ DONE | `lib/server/startup-tracking-metrics.ts`, `/dashboard/startup`, COMPLETION_LOG 2026-04-04 |
| SD-007 | Recommendation lifecycle model (`suggested` -> `validated`) | Backend + Database | Ã¢Å“â€¦ DONE | `supabase/migrations/023_startup_recommendation_lifecycle.sql`, `lib/server/startup-recommendation-lifecycle.ts`, COMPLETION_LOG 2026-04-04 |
| SD-008 | Markdown-audit to implementation-plan generator by team lane | Backend | Ã¢Å“â€¦ DONE | `supabase/migrations/024_startup_implementation_plan.sql`, `lib/server/startup-implementation-plan.ts`, COMPLETION_LOG 2026-04-04 |
| SD-009 | GitHub App integration foundation (install, repo allowlist, token lifecycle) | Backend | Ã¢Å“â€¦ DONE | `supabase/migrations/025_startup_github_integration_foundation.sql`, `lib/server/startup-github-integration.ts`, COMPLETION_LOG 2026-04-04 |
| SD-010 | Agent PR workflow (approved recommendation -> PR -> status sync) | Backend | Ã¢Å“â€¦ DONE | `supabase/migrations/026_startup_agent_pr_workflow.sql`, `lib/server/startup-agent-pr-workflow.ts`, COMPLETION_LOG 2026-04-04 |
| SD-011 | Multi-model policy routing by service/bundle/workspace with budget guardrails | Backend | Ã¢Å“â€¦ DONE | `supabase/migrations/027_startup_model_policy_scope.sql`, `lib/server/startup-model-policy.ts`, COMPLETION_LOG 2026-04-04 |
| SD-012 | Stripe bundle mapping + add-on linkage to entitlement model | Backend | Ã¢Å“â€¦ DONE | `supabase/migrations/028_service_billing_mappings.sql`, `lib/server/service-billing-guard.ts`, COMPLETION_LOG 2026-04-04 |
| SD-013 | UI gating refactor to centralized entitlement hooks across startup + agency surfaces | Frontend + Backend | Ã¢Å“â€¦ DONE | `lib/server/startup-service-gates.ts`, `app/dashboard/startup/page.tsx`, COMPLETION_LOG 2026-04-04 |
| SD-014 | Observability + auditability for entitlement, model routing, and agent execution transitions | Backend + Ops | Ã¢Å“â€¦ DONE | `lib/server/startup-admin-data.ts`, `components/startup-admin-control-view.tsx`, COMPLETION_LOG 2026-04-04 |
| SD-015 | Beta rollout guardrails (`startup_dashboard`, `github_agent`, `auto_pr`) with suggest-only default | Backend + Ops | Ã¢Å“â€¦ DONE | `lib/server/startup-rollout-flags.ts`, `app/dashboard/startups/actions.ts`, COMPLETION_LOG 2026-04-04 |

### Startup Slack delivery MVP (planning + staged implementation)
> Keep this stream minimal, feature-flagged, and aligned to centralized entitlement controls. Contract is frozen in `docs/17-startup-slack-integration-mvp-plan.md`.

| TASK ID | Description | Owner | Status | Evidence |
|---|---|---|---|---|
| SL-001 | Freeze Slack MVP contract (event types, payload shape, gating defaults, rollout posture) | Product + Backend | Ã¢Å“â€¦ DONE | `docs/17-startup-slack-integration-mvp-plan.md`, `PLAYBOOK/startup-slack-integration-mvp-v1.md`, COMPLETION_LOG 2026-04-04 |
| SL-002 | Add centralized Slack service keys + startup rollout flags (`slack_agent`, `slack_auto_post`) | Backend | Ã¢Å“â€¦ DONE | `supabase/migrations/029_startup_slack_service_keys.sql`, `lib/server/startup-rollout-flags.ts`, COMPLETION_LOG 2026-04-04 |
| SL-003 | Add minimal Slack schema (installations, destinations, delivery events) | Backend + Database | âœ… DONE | `supabase/migrations/030_startup_slack_integration_foundation.sql`, COMPLETION_LOG 2026-04-04 |
| SL-004 | Implement Slack OAuth connect/disconnect foundation | Backend | âœ… DONE | `supabase/migrations/031_startup_slack_install_sessions.sql`, `lib/server/startup-slack-integration.ts`, `app/api/startup/slack/callback/route.ts`, `app/dashboard/startup/actions.ts`, `app/dashboard/startup/page.tsx`, COMPLETION_LOG 2026-04-04 |
| SL-005 | Add simple startup Slack destination UI (list + add destination) | Frontend + Backend | âœ… DONE | `app/dashboard/startup/page.tsx`, `app/dashboard/startup/actions.ts`, `lib/server/startup-slack-integration.ts`, COMPLETION_LOG 2026-04-04 |
| SL-006 | Add manual send-to-Slack action from startup report/audit context | Backend + Frontend | âœ… DONE | `app/dashboard/startup/actions.ts`, `app/dashboard/startup/page.tsx`, `lib/server/startup-slack-integration.ts`, COMPLETION_LOG 2026-04-04 |
| SL-007 | Implement normalized Slack message formatter from frozen payload contract | Backend | âœ… DONE | `lib/server/startup-slack-message.ts`, `app/dashboard/startup/actions.ts`, COMPLETION_LOG 2026-04-04 |
| SL-008 | Persist Slack delivery attempt status and failure reasons | Backend | âœ… DONE | `lib/server/startup-slack-integration.ts`, `app/dashboard/startup/actions.ts`, `app/dashboard/startup/page.tsx`, COMPLETION_LOG 2026-04-04 |
| SL-009 | Add optional `slack_auto_post` workspace toggle (default off) | Backend + Frontend | âœ… DONE | `app/dashboard/startup/actions.ts`, `app/dashboard/startup/page.tsx`, `lib/server/startup-rollout-flags.ts`, COMPLETION_LOG 2026-04-04 |
| SL-010 | Wire Slack service keys into `/dashboard/services` centralized controls | Frontend + Backend | âœ… DONE | `supabase/migrations/032_slack_service_bundle_mappings.sql`, `app/dashboard/services/actions.ts`, COMPLETION_LOG 2026-04-04 |
| SL-011 | Add focused tests for Slack gates, formatter, and delivery logging | QA + Backend | âœ… DONE | `lib/server/startup-slack-integration.test.ts`, `lib/server/startup-slack-message.test.ts`, `lib/server/startup-service-gates.test.ts`, `lib/server/startup-rollout-flags.test.ts`, COMPLETION_LOG 2026-04-04 |
| SL-012 | Pilot rollout on Alie and capture operator evidence | Ops + Product | âœ… DONE | `docs/18-startup-slack-pilot-rollout-runbook.md`, COMPLETION_LOG 2026-04-04 |
| SL-013 | Add scheduled startup Slack autopost cadence trigger (find due workspaces, enqueue deep-audit queue jobs) | Backend | âœ… DONE | `lib/server/startup-slack-schedule.ts`, `workers/cloudflare-entry.ts`, `lib/server/startup-slack-schedule.test.ts`, COMPLETION_LOG 2026-04-04 |
| SL-014 | Auto-post Slack `new_audit_ready` delivery from report completion path for eligible startup workspaces | Backend | âœ… DONE | `workers/queue/report-queue-consumer.ts`, `lib/server/startup-slack-integration.ts`, COMPLETION_LOG 2026-04-04 |

### Startup audit orchestration stream (SAO-001 ... SAO-018)
> Frozen planning stream in `PLAYBOOK/startup-audit-orchestration-v1.md`. This stream extends the existing startup workspace backbone so one audit can produce a durable execution record, repo-aware plan, approval-gated PR flow, and benchmark-ready improvement history. It stays feature-flagged, startup-workspace scoped, multimodel by role, and explicit that Slack is additive rather than the source of truth.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| SAO-001 | Freeze startup audit orchestration v1 scope, milestones, and task registry across playbook + project memory | Orchestrator | DONE | `PLAYBOOK/startup-audit-orchestration-v1.md`, `agents/memory/PROJECT_STATE.md`, `agents/ORCHESTRATOR.md`, `agents/memory/COMPLETION_LOG.md` |
| SAO-002 | Add `startup_audit_executions` + `startup_audit_execution_events` schema with startup-member RLS | Backend + Database | DONE | `supabase/migrations/039_startup_audit_execution_foundation.sql`, COMPLETION_LOG 2026-04-13 |
| SAO-003 | Add typed execution contracts, transition validation, and server helpers | Backend | DONE | `lib/server/startup-audit-execution.ts`, `lib/server/startup-audit-execution.test.ts`, COMPLETION_LOG 2026-04-13 |
| SAO-004 | Surface execution history and detail in startup dashboard | Frontend + Backend | DONE | `lib/server/startup-dashboard-data.ts`, `app/dashboard/startup/components/startup-audits-tab.tsx`, `app/dashboard/startup/components/startup-audits-table-client.tsx`, `lib/server/startup-dashboard-data.test.ts`, COMPLETION_LOG 2026-04-13 |
| SAO-005 | Extend plan-task model with execution mode, dependencies, evidence, and manual-action fields | Backend + Database | DONE | `supabase/migrations/040_startup_implementation_task_execution_fields.sql`, `lib/server/startup-implementation-plan.ts`, `lib/server/startup-implementation-plan.test.ts`, `tests/e2e/startup-dashboard-tabs.spec.ts`, COMPLETION_LOG 2026-04-13 |
| SAO-006 | Freeze orchestrator planner output contract and persistence shape | Architect + Backend | DONE | `lib/server/startup-orchestrator-plan-contract.ts`, `lib/server/startup-implementation-plan.ts`, `lib/server/startup-implementation-plan.test.ts`, COMPLETION_LOG 2026-04-13 |
| SAO-007 | Seed orchestration role service keys and route role-level model policy through centralized admin controls | Backend + Admin | DONE | `supabase/migrations/041_startup_audit_orchestration_service_keys.sql`, `lib/server/service-entitlements-contract.ts`, `lib/server/startup-model-policy.ts`, `lib/server/startup-model-policy.test.ts`, COMPLETION_LOG 2026-04-13 |
| SAO-008 | Add repo-review step contract and artifact persistence | Backend | DONE | `lib/server/startup-orchestrator-repo-review-contract.ts`, `lib/server/startup-audit-execution.ts`, `lib/server/startup-audit-execution.test.ts`, COMPLETION_LOG 2026-04-13 |
| SAO-009 | Add DB-review and risk-review step contracts and persistence | Backend | DONE | `lib/server/startup-orchestrator-db-review-contract.ts`, `lib/server/startup-orchestrator-risk-review-contract.ts`, `lib/server/startup-audit-execution.ts`, `lib/server/startup-audit-execution.test.ts`, COMPLETION_LOG 2026-04-13 |
| SAO-010 | Implement planning workflow: execution -> reviews -> plan_ready | Backend | DONE | `lib/server/startup-audit-planning-workflow.ts`, `lib/server/startup-audit-planning-workflow.test.ts`, COMPLETION_LOG 2026-04-13 |
| SAO-011 | Add approval-gated execution state and founder/admin approval actions | Backend + Frontend | DONE | `lib/server/startup-audit-execution.ts`, `app/dashboard/startup/actions.ts`, `app/dashboard/startup/components/startup-audits-tab.tsx`, `tests/e2e/startup-dashboard-tabs.spec.ts` |
| SAO-012 | Extend PR workflow linkage from recommendation-only runs to execution/task-aware runs | Backend | DONE | `supabase/migrations/042_startup_agent_pr_execution_context.sql`, `lib/server/startup-agent-pr-workflow.ts`, `lib/server/startup-agent-pr-workflow.test.ts` |
| SAO-013 | Add manual operator task tracking and wait/resume behavior | Backend + Frontend | DONE | `lib/server/startup-implementation-plan.ts`, `app/dashboard/startup/actions.ts`, `app/dashboard/startup/components/startup-overview-tab.tsx`, `tests/e2e/startup-dashboard-tabs.spec.ts` |
| SAO-014 | Add orchestration dashboard module for plan, blockers, approvals, and model provenance | Frontend + Backend | DONE | `lib/server/startup-dashboard-data.ts`, `app/dashboard/startup/components/startup-overview-tab.tsx`, `tests/e2e/startup-dashboard-tabs.spec.ts` |
| SAO-015 | Add improvement-history rollups and benchmark-ready execution outcome summaries | Backend + Frontend | DONE | `lib/server/startup-tracking-metrics.ts`, `app/dashboard/startup/components/startup-overview-tab.tsx`, `tests/e2e/startup-dashboard-tabs.spec.ts` |
| SAO-016 | Add founder/admin execution-batch queueing and task-state sync through the PR workflow | Backend + Frontend | DONE | `lib/server/startup-implementation-plan.ts`, `lib/server/startup-agent-pr-workflow.ts`, `app/dashboard/startup/actions.ts`, `app/dashboard/startup/components/startup-overview-tab.tsx`, `lib/server/startup-implementation-plan.test.ts`, `lib/server/startup-agent-pr-workflow.test.ts`, `tests/e2e/startup-dashboard-tabs.spec.ts` |
| SAO-017 | Add scheduled worker pickup for approved execution batches behind rollout + entitlement gates | Backend + Worker | DONE | `lib/server/startup-execution-dispatch-schedule.ts`, `lib/server/startup-execution-dispatch-schedule.test.ts`, `workers/cloudflare-entry.ts`, COMPLETION_LOG 2026-04-14 |
| SAO-018 | Add repo reservation and one-active-run-per-workspace+repo guardrails before real GitHub execution | Backend | DONE | `lib/server/startup-github-guardrails.ts`, `lib/server/startup-github-guardrails.test.ts`, `lib/server/startup-agent-pr-workflow.ts`, `lib/server/startup-agent-pr-workflow.test.ts`, `lib/server/startup-execution-dispatch-schedule.ts`, COMPLETION_LOG 2026-04-14 |

### Production Bug Fix Stream (BF-001 … BF-006)
> **Context:** Three production bugs reported 2026-04-06. Bugs A+B: startup workspace users incorrectly hit Stripe for deep audit — `startup_workspace_id` missing from scan + checkout route selects, no startup bypass path existed. Bug C: no delete path for members or workspace/account objects in admin console.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| BF-001 | Fix `/api/scans/[id]`: add `startup_workspace_id` select + startup membership check + `startup_bypass` checkoutMode | Backend | ✅ DONE | `app/api/scans/[id]/route.ts`, COMPLETION_LOG BF-001 |
| BF-002 | Fix `/api/checkout`: add `startup_workspace_id` select + startup bypass block before Stripe | Backend | ✅ DONE | `app/api/checkout/route.ts`, COMPLETION_LOG BF-002 |
| BF-003 | Add `removeStartupWorkspaceMember` + `deleteStartupWorkspace` server actions | Backend | ✅ DONE | `app/dashboard/startups/actions.ts`, COMPLETION_LOG BF-003 |
| BF-004 | Add `removeAgencyMember` + `deleteAgencyAccount` server actions | Backend | ✅ DONE | `app/dashboard/agencies/actions.ts`, COMPLETION_LOG BF-004 |
| BF-005 | Add remove member + delete workspace UI to `StartupAdminControlView` | Frontend | ✅ DONE | `components/startup-admin-control-view.tsx`, COMPLETION_LOG BF-005 |
| BF-006 | Add remove member + delete account UI to `AgencyAdminControlView` | Frontend | ✅ DONE | `components/agency-admin-control-view.tsx`, COMPLETION_LOG BF-006 |

### Self-Serve Bundle Subscriptions + Onboarding (BILL-001 … BILL-014)
> **Context:** Users need to self-onboard by selecting a bundle on `/pricing`, entering card info, and having their workspace auto-provisioned. Paid tiers (`startup_dev`, `agency_core`, `agency_pro`) use Stripe recurring monthly subscriptions with a configurable free trial (default 7 days, card required upfront). `startup_lite` stays free. Plan: `C:\Users\Carine Tamon\.claude\plans\snoopy-hatching-scroll.md`.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|--------|--------|----------|
| BILL-001 | DB migration: `billing_mode` + `stripe_price_id` + `trial_period_days` on `service_bundles` + new `user_subscriptions` table | Database | ✅ DONE | `supabase/migrations/033_subscription_billing_foundation.sql` |
| BILL-002 | New `/api/billing/subscribe` route — subscription checkout with trial support | Backend | ✅ DONE | `app/api/billing/subscribe/route.ts`, COMPLETION_LOG BILL-002 |
| BILL-003 | Extend `/api/webhooks/stripe` — subscription lifecycle handlers (created/updated/deleted/invoice/trial_will_end) | Backend | ✅ DONE | `lib/server/stripe/subscription-handlers.ts`, `app/api/webhooks/stripe/route.ts`, COMPLETION_LOG BILL-003 |
| BILL-004 | Auto-provision workspace on subscription (trialing OR active) | Backend | ✅ DONE | `lib/server/billing/provision-workspace-for-subscription.ts`, COMPLETION_LOG BILL-004 |
| BILL-005 | Redesign `/pricing` — real bundle cards with trial CTA + pricing display | Frontend | ✅ DONE | `app/pricing/page.tsx`, `components/pricing-bundle-card.tsx`, `components/subscription-status-banner.tsx`, COMPLETION_LOG BILL-005 |
| BILL-006 | Auth callback — new user detection → redirect to `/pricing` | Backend | ✅ DONE | `app/auth/callback/route.ts`, COMPLETION_LOG BILL-006 |
| BILL-007 | Post-subscribe redirect + dashboard onboarding state | Frontend | ✅ DONE | `components/new-subscriber-welcome-banner.tsx`, `app/dashboard/page.tsx`, COMPLETION_LOG BILL-007 |
| BILL-008 | Dashboard `(admin)` layout: redirect to `/dashboard` on failed admin context except service-role misconfiguration (inline error retained for operators) | Frontend | ✅ DONE | `app/dashboard/(admin)/layout.tsx`, `lib/server/admin-runtime.ts`, `lib/server/admin-runtime.test.ts`, COMPLETION_LOG BILL-008 |
| BILL-009 | Middleware fast-path: when `ADMIN_EMAIL` is set, redirect logged-in non-matching session email away from `/admin/*` (layout still enforces `platform_admin_users`) | Security + Backend | ✅ DONE | `middleware.ts`, `lib/supabase/middleware.ts`, `lib/server/legacy-admin-email.ts`, `lib/server/legacy-admin-email.test.ts`, COMPLETION_LOG BILL-009 |
| BILL-010 | Document `users.plan` vs `user_subscriptions.bundle_key` — ADR-009 + current-state | Architect + Product | ✅ DONE | `agents/memory/DECISIONS.md` ADR-009, `docs/01-current-state.md`, COMPLETION_LOG BILL-010 |
| BILL-011 | Align `assignUserPlan` + admin UI with Postgres `plan_type` (`free` \| `pro` \| `agency`) | Backend + Frontend | ✅ DONE | `lib/server/plan-type.ts`, `app/admin/users/actions.ts`, `app/admin/users/[userId]/page.tsx`, `lib/server/plan-type.test.ts`, COMPLETION_LOG BILL-011 |
| BILL-012 | `assignUserPlan` upserts/cancels synthetic `user_subscriptions` row (`admin_comp:<userId>`) so `resolveUserCapabilities` matches admin comp | Backend | ✅ DONE | `lib/server/admin-comp-subscription.ts`, `app/admin/users/actions.ts`, `lib/server/admin-comp-subscription.test.ts`, COMPLETION_LOG BILL-012 |
| BILL-013 | Subscription active but no workspace: shared predicate + dashboard “Create workspace” self-serve action (same provision path as webhook/admin) | Backend + Frontend | ✅ DONE | `lib/server/subscription-provisioning-gap.ts`, `app/dashboard/actions.ts` `provisionMyWorkspaceForSubscription`, `components/subscription-workspace-pending-banner.tsx`, `app/dashboard/page.tsx`, COMPLETION_LOG BILL-013 |
| BILL-014 | RLS SELECT policies for `service_entitlement_overrides` (+ catalog/bundles/bundle_services/model_policies) for `authenticated` | Database | ✅ DONE | `supabase/migrations/037_service_entitlements_rls_policies.sql`, COMPLETION_LOG BILL-014 |

### Billing/onboarding byte-task stream (BT-001 … BT-006)
> Frozen implementation plan in `docs/19-billing-onboarding-implementation-plan.md` and `PLAYBOOK/billing-onboarding-v1.md`. This stream stays docs-first, narrow, and focused on self-serve onboarding, bundle truth, admin readiness, and end-to-end audit access.

| TASK ID | Description | Owner | Status | Evidence |
|---|---|---|---|---|
| BT-001 | Freeze the billing/onboarding stream in project state, playbook, and task registry | Orchestrator | DONE | `PLAYBOOK/billing-onboarding-v1.md`, `docs/19-billing-onboarding-implementation-plan.md`, `agents/memory/PROJECT_STATE.md` |
| BT-002 | Lock canonical product truth for bundle access and full-audit payment behavior | Product + Backend | DONE | `PLAYBOOK/billing-onboarding-v1.md` product truth matrix, `docs/01-current-state.md` |
| BT-003 | Normalize runtime contract for checkout modes and subscription state | Backend + Frontend | DONE | `lib/shared/deep-audit-checkout-mode.ts`, `app/api/scans/[id]/route.ts`, `components/results-view.tsx`, `components/deep-audit-checkout.tsx`, `npm.cmd tsc --noEmit`, `npx.cmd vitest run lib/shared/deep-audit-checkout-mode.test.ts` |
| BT-004 | Align results UI with backend checkout modes so bypass paths never fall through to Stripe | Frontend | DONE | `components/results-view.tsx`, `components/deep-audit-checkout.tsx`, `npm.cmd tsc --noEmit` |
| BT-005 | Add admin bundle readiness checks for billing, entitlements, and mappings | Frontend + Backend | DONE | `lib/server/bundle-readiness.ts`, `lib/server/bundle-readiness.test.ts`, `app/admin/bundles/[bundleKey]/page.tsx`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/bundle-readiness.test.ts` |
| BT-006 | Add end-to-end onboarding coverage from signup through dashboard and audit access | QA + Backend + Frontend | DONE | `lib/server/billing-onboarding-flow.ts`, `lib/server/billing-onboarding-flow.test.ts`, `app/auth/callback/route.ts`, `app/api/billing/subscribe/route.ts`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/billing-onboarding-flow.test.ts` |
| BT-007 | Harden subscription provisioning so webhook retries converge on the same workspace/account | Backend | DONE | `lib/server/billing/provision-workspace-for-subscription.ts`, `lib/server/billing/provision-workspace-for-subscription.test.ts`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/billing/provision-workspace-for-subscription.test.ts` |
| BT-008 | Normalize coarse user.plan from the live subscription set instead of resetting to free on cancel | Backend | DONE | `lib/server/subscription-plan-sync.ts`, `lib/server/subscription-plan-sync.test.ts`, `app/admin/users/actions.ts`, `lib/server/stripe/subscription-handlers.ts`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/subscription-plan-sync.test.ts` |
| BT-009 | Lock the Stripe webhook route dispatch contract for subscription, invoice, and checkout events | QA + Backend | DONE | `app/api/webhooks/stripe/route.test.ts`, `app/api/webhooks/stripe/route.ts`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/api/webhooks/stripe/route.test.ts` |
| BT-010 | Clarify admin bundle/service-control boundaries for self-serve onboarding and audit bypass setup | Frontend + Docs | DONE | `app/admin/bundles/[bundleKey]/page.tsx`, `components/service-control-admin-view.tsx`, `docs/01-current-state.md`, `npx.cmd tsc --noEmit` |
| BT-011 | Converge admin auth to one DB-backed platform-admin allowlist and remove legacy email fallback from runtime gates | Backend + Security + Docs | DONE | `lib/server/require-admin.ts`, `lib/server/admin-runtime.ts`, `app/admin/layout.tsx`, `app/admin/login/actions.ts`, `app/dashboard/layout.tsx`, `middleware.ts`, `lib/server/distribution-oauth-admin-gate.ts`, `npx.cmd tsc --noEmit` |
| BT-012 | Add a hard-delete admin user flow that purges test accounts, related artifacts, billing state, and auth access | Backend + Frontend + QA | DONE | `lib/server/user-deletion.ts`, `lib/server/user-deletion.test.ts`, `app/admin/users/actions.ts`, `app/admin/users/[userId]/page.tsx`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/user-deletion.test.ts` |

| 2026-04-08 | **Billing/onboarding byte-task stream opened.** Added `PLAYBOOK/billing-onboarding-v1.md` and `docs/19-billing-onboarding-implementation-plan.md`; registered `BT-001` … `BT-006` in `PROJECT_STATE.md`. Sequencing freeze: lock product truth first, then runtime contract, UI alignment, admin readiness, and end-to-end onboarding proof. |

| 2026-04-08 | **BT-002 â€” COMPLETE (documentation).** Added the billing playbook product truth matrix and the current-state note that makes bundle behavior explicit: `startup_lite` remains free; `startup_dev` is paid and still uses the startup workspace bypass for full-audit access; `agency_core` and `agency_pro` include deep-audit entitlement while still honoring backend checkout modes and payment guardrails. |
| 2026-04-08 | **BT-004 â€” COMPLETE (implementation + verification).** Results and deep-audit checkout UI now read the bypass state distinctly for startup workspaces and agency clients, so bypass paths do not collapse back into Stripe phrasing. Verification: `npm.cmd tsc --noEmit`. |
| 2026-04-08 | **BT-005 â€” COMPLETE (implementation + verification).** Added `lib/server/bundle-readiness.ts` plus a focused test file and surfaced a new readiness summary on `/admin/bundles/[bundleKey]` covering billing config, Stripe mappings, and entitlement override coverage before a bundle is treated as launch-ready. Verification: `npm.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/bundle-readiness.test.ts`. |
| 2026-04-08 | **BT-006 â€” COMPLETE (implementation + verification).** Added shared onboarding helpers in `lib/server/billing-onboarding-flow.ts`, wired them into auth callback and billing subscribe, and added a focused onboarding-flow test that covers post-signup resume/onboarding redirects, dashboard success URL generation, subscription provisioning state, and audit-access mode normalization. Verification: `npm.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/billing-onboarding-flow.test.ts`. |
| 2026-04-08 | **BT-003 â€” COMPLETE (implementation + verification).** Added a shared deep-audit checkout-mode contract in `lib/shared/deep-audit-checkout-mode.ts` with a focused Vitest file, updated `/api/scans/[id]` to emit the shared `DeepAuditCheckoutMode`, and normalized results/deep-audit checkout consumers so `startup_bypass` and `agency_bypass` are both first-class runtime states. Verification: `npm.cmd tsc --noEmit`, `npx.cmd vitest run lib/shared/deep-audit-checkout-mode.test.ts`. |
| 2026-04-08 | **BT-007 â€” COMPLETE (implementation + verification).** Hardened subscription provisioning so workspace/account creation uses a stable subscription-derived key, and membership writes use composite conflict targets instead of insert-only semantics. Current truth: webhook retries and near-simultaneous deliveries now converge on the same child record instead of creating duplicate workspaces or agency accounts. Verification: `npm.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/billing/provision-workspace-for-subscription.test.ts`. |
| 2026-04-08 | **BT-008 â€” COMPLETE (implementation + verification).** Added `subscription-plan-sync` so coarse `users.plan` now resolves from the live subscription set instead of being forced to `free` during cancellation. Both webhook cancellation/invoice handlers and the admin cancel action now use the shared sync path. Verification: `npm.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/subscription-plan-sync.test.ts`. |
| 2026-04-08 | **BT-009 â€” COMPLETE (implementation + verification).** Added explicit webhook route spec coverage for subscription lifecycle dispatch, invoice lifecycle dispatch, and the subscription-mode checkout skip path. Current truth: the router contract is now locked down alongside the handler-level idempotency work. Verification: `npm.cmd tsc --noEmit`, `npx.cmd vitest run app/api/webhooks/stripe/route.test.ts`. |
| 2026-04-08 | **BT-010 â€” COMPLETE (implementation + docs).** Clarified the operator boundary between bundle-local billing/service settings and the cross-bundle service control center. The bundle page now points admins at `/admin/services` for deep-audit bypass and payment-required overrides instead of implying those rules belong in the bundle editor. The bundle editor now uses a binary included/not included toggle for service rows and derives `free/off` from that choice. Verification: `npm.cmd tsc --noEmit`. |
| 2026-04-08 | **BT-011 â€” COMPLETE (implementation + docs).** Converged admin authorization to a single DB-backed platform-admin allowlist. The runtime no longer uses the legacy `ADMIN_EMAIL` fallback for admin routes, dashboard shell affordances, admin login, or distribution OAuth. Verification: `npm.cmd tsc --noEmit`. |

### Auth/signup byte-task stream (BT-001 ... BT-006)
> Frozen implementation plan in `docs/20-auth-signup-implementation-plan.md` and `PLAYBOOK/auth-signup-v1.md`. This stream stays docs-first, narrow, and focused on one primary signup surface, bundle preservation, redirect continuity, and focused tests.

| TASK ID | Description | Owner | Status | Evidence |
|---|---|---|---|---|
| BT-001 | Simplify the public auth entry into a single sign-up path with a secondary sign-in link beneath it | Frontend | DONE | `app/login/page.tsx`, `app/login/login-form.tsx`, `components/pricing-bundle-card.tsx` |
| BT-002 | Replace magic-link signup with password signup plus password confirmation | Backend + Frontend | TODO | `docs/20-auth-signup-implementation-plan.md` |
| BT-003 | Preserve the selected bundle through signup so checkout resumes on the correct tier | Backend + Frontend | DONE | `app/login/actions.ts`, `app/login/actions.test.ts`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/login/actions.test.ts` |
| BT-004 | Align auth callback and onboarding redirects with the password-based signup flow | Backend | DONE | `app/login/actions.ts`, `app/login/login-form.tsx`, `app/auth/callback/route.ts`, `app/login/actions.test.ts`, `npx.cmd vitest run app/login/actions.test.ts` |
| BT-005 | Add focused tests for signup UI, bundle preservation, and redirect behavior | QA | DONE | `app/login/actions.test.ts`, `app/login/page.test.tsx`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/login/actions.test.ts app/login/page.test.tsx` |
| BT-006 | Update repo docs and current-state notes after each completed slice | Orchestrator | DONE | `PLAYBOOK/auth-signup-v1.md`, `docs/20-auth-signup-implementation-plan.md`, `docs/01-current-state.md`, `agents/memory/PROJECT_STATE.md` |

| 2026-04-09 | **Auth/signup byte-task stream opened.** Added `PLAYBOOK/auth-signup-v1.md` and `docs/20-auth-signup-implementation-plan.md`; registered `BT-001` ... `BT-006` in `PROJECT_STATE.md`. Sequencing freeze: keep one primary sign-up surface, then password rollout, bundle preservation, redirect alignment, and focused tests. |
| 2026-04-09 | **BT-001 - COMPLETE (implementation + documentation).** Simplified the public auth entry into a single signup panel for pricing-tier clicks with a secondary `Or sign in` link beneath it. The signup path no longer presents a side-by-side split on the tier-entry surface. Verification: `npx.cmd tsc --noEmit`. |
| 2026-04-09 | **BT-002 - COMPLETE (implementation + verification).** Replaced the signup flow with password-based account creation plus confirmation on the sign-up panel. The action now creates the account, signs the user in, and resumes the selected pricing bundle toward checkout. Verification: `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/login/actions.test.ts`. |
| 2026-04-09 | **BT-003 - COMPLETE (implementation + verification).** Preserved the selected bundle through the new password-signup flow so the user returns to the correct pricing bundle before checkout. Verification: `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/login/actions.test.ts`. |
| 2026-04-09 | **BT-004 - COMPLETE (implementation + verification).** Password sign-in from the pricing-tier auth path now preserves the selected bundle and resumes the same checkout path via the shared onboarding redirect helper. Verification: `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/login/actions.test.ts`. |
| 2026-04-09 | **BT-005 - COMPLETE (implementation + verification).** Added focused regression coverage for the signup shell and the password signup/sign-in redirect behavior. Verification: `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/login/actions.test.ts app/login/page.test.tsx`. |
| 2026-04-09 | **BT-006 - COMPLETE (documentation).** Updated the auth/signup playbook, implementation plan, current-state doc, and project memory to reflect the shipped password-based signup flow. |

### Checkout + Workspace Provisioning byte-task stream (BT-001 ... BT-007)
> Frozen implementation plan in `docs/21-checkout-workspace-provisioning-implementation-plan.md` and `PLAYBOOK/checkout-workspace-provisioning-v1.md`. This stream stays docs-first, narrow, and focused on one stable checkout wait state plus subscription-derived workspace provisioning visibility and a shared startup-access resolver.

| TASK ID | Description | Owner | Status | Evidence |
|---|---|---|---|---|
| BT-001 | Collapse the pricing-page autosubscribe flow into one stable checkout wait state before Stripe is ready | Frontend | DONE | `components/pricing-bundle-card.tsx`, `lib/client/loading-journeys.ts`, `lib/client/loading-journeys.test.ts`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/client/loading-journeys.test.ts` |
| BT-002 | Show connectors as provisioning-pending when a live startup/agency subscription exists but no workspace/account is linked yet | Backend + Frontend | DONE | `app/dashboard/connectors/lib/load-startup-connectors-context.ts`, `app/dashboard/connectors/page.tsx`, `app/dashboard/connectors/lib/load-startup-connectors-context.test.ts`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/dashboard/connectors/lib/load-startup-connectors-context.test.ts` |
| BT-003 | Update repo docs and current-state notes to record the checkout and workspace-provisioning contract | Orchestrator | DONE | `PLAYBOOK/checkout-workspace-provisioning-v1.md`, `docs/21-checkout-workspace-provisioning-implementation-plan.md`, `docs/01-current-state.md`, `agents/memory/PROJECT_STATE.md` |
| BT-004 | Add a shared startup-access resolver so subscription, workspace, and membership state resolve from one helper | Backend | DONE | `lib/server/startup-access-resolver.ts`, `lib/server/startup-access-resolver.test.ts`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/startup-access-resolver.test.ts` |
| BT-005 | Wire startup dashboard and connectors to the shared startup-access resolver | Backend + Frontend | DONE | `app/dashboard/startup/lib/load-startup-dashboard-context.ts`, `app/dashboard/startup/page.tsx`, `app/dashboard/connectors/lib/load-startup-connectors-context.ts`, `app/dashboard/connectors/page.tsx`, `app/dashboard/startup/lib/load-startup-dashboard-context.test.ts`, `app/dashboard/connectors/lib/load-startup-connectors-context.test.ts`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/startup-access-resolver.test.ts app/dashboard/startup/lib/load-startup-dashboard-context.test.ts app/dashboard/connectors/lib/load-startup-connectors-context.test.ts` |
| BT-006 | Wire new-scan to the shared startup-access resolver and update docs/state | Backend + Frontend + Orchestrator | DONE | `app/dashboard/new-scan/page.tsx`, `docs/01-current-state.md`, `docs/21-checkout-workspace-provisioning-implementation-plan.md`, `PLAYBOOK/checkout-workspace-provisioning-v1.md`, `agents/memory/PROJECT_STATE.md` |
| BT-007 | Wire checkout to the shared startup-access resolver so startup scans bypass Stripe from the same source of truth | Backend + Frontend | DONE | `app/api/checkout/route.ts`, `app/api/checkout/route.test.ts`, `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/api/checkout/route.test.ts` |

| 2026-04-09 | **Checkout + workspace-provisioning byte-task stream opened.** Added `PLAYBOOK/checkout-workspace-provisioning-v1.md` and `docs/21-checkout-workspace-provisioning-implementation-plan.md`; registered `BT-001` ... `BT-003` in `PROJECT_STATE.md`. Sequencing freeze: collapse checkout wait first, then surface subscription-derived provisioning state on connectors, then update docs/state. |
| 2026-04-09 | **BT-002 - COMPLETE (implementation + verification).** Connectors now show a provisioning-pending state when a live startup/agency subscription exists but the workspace/account link has not landed yet, reusing the existing subscription-derived provisioning source of truth instead of a second onboarding path. The pending state now includes a dashboard return path so users can keep moving while provisioning finishes. Verification: `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/dashboard/connectors/lib/load-startup-connectors-context.test.ts`. |
| 2026-04-09 | **BT-001 - COMPLETE (implementation + verification).** Pricing autosubscribe now uses one stable checkout wait state before Stripe is ready, replacing the visible multi-step verification/preparing redirect sequence. Verification: `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/client/loading-journeys.test.ts`. |
| 2026-04-09 | **BT-003 - COMPLETE (documentation).** Updated the checkout/workspace-provisioning playbook, implementation plan, current-state doc, and project memory to reflect the shipped checkout wait state and provisioning-pending connectors state. |
| 2026-04-10 | **BT-004 - COMPLETE (implementation + verification).** Added a shared startup-access resolver that normalizes startup subscription, workspace, and membership state into ready / needs-provisioning / membership-repair / no-subscription outcomes. Verification: `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/startup-access-resolver.test.ts`. |
| 2026-04-10 | **BT-005 - COMPLETE (implementation + verification).** Wired the startup dashboard and connectors pages to the shared startup-access resolver so both surfaces can distinguish provisioning and membership-repair states before deciding whether to show workspace UI. Verification: `npx.cmd tsc --noEmit`, `npx.cmd vitest run lib/server/startup-access-resolver.test.ts app/dashboard/startup/lib/load-startup-dashboard-context.test.ts app/dashboard/connectors/lib/load-startup-connectors-context.test.ts`. |
| 2026-04-10 | **BT-006 - COMPLETE (implementation + documentation).** Wired the new-scan page to the shared startup-access resolver and updated the living docs to record the centralized startup-access contract. Verification: `npx.cmd tsc --noEmit`. |
| 2026-04-10 | **BT-007 - COMPLETE (implementation + documentation).** Wired the checkout route to the shared startup-access resolver so startup scans bypass Stripe from the same source of truth, and updated the living docs to match. Verification: `npx.cmd tsc --noEmit`, `npx.cmd vitest run app/api/checkout/route.test.ts`. |

### Startup Dashboard Tab Redesign (four-tab IA on `/dashboard/startup`)
### Signup-linked workspace provisioning + trial gating byte-task stream (BT-001 ... BT-005)
> Keep this stream narrow: one slice for signup/org-name propagation, one slice for subscription metadata, one slice for provisioning name preference, one slice for trial-aware audit gating, one slice for regression coverage.

| TASK ID | Description | Owner | Status | Evidence |
|---|---|---|---|---|
| BT-001 | Capture organization/workspace name through signup, callback, and pricing resume | Backend + Frontend | DONE | `app/login/actions.ts`, `app/login/login-form.tsx`, `app/login/page.tsx`, `components/pricing-bundle-card.tsx`, `app/auth/callback/route.ts` |
| BT-002 | Carry `organization_name` into Stripe checkout session metadata and subscription metadata | Backend | DONE | `app/api/billing/subscribe/route.ts`, `lib/server/stripe/subscription-handlers.ts` |
| BT-003 | Prefer captured organization name when provisioning startup/agency rows and self-serve fallback | Backend | DONE | `lib/server/billing/provision-workspace-for-subscription.ts`, `app/dashboard/actions.ts` |
| BT-004 | Fix trial-aware full-audit gating so live trialing subscriptions use provisioning state instead of paywalling | Backend + Frontend | PENDING | â€” |
| BT-005 | Add regression tests and docs for organization-name propagation and the remaining trial-gating slice | QA + Orchestrator | DONE | `app/login/actions.test.ts`, `lib/server/billing-onboarding-flow.test.ts`, `lib/server/billing/provision-workspace-for-subscription.test.ts`, `npm.cmd type-check`, `npx.cmd vitest run app/login/actions.test.ts lib/server/billing-onboarding-flow.test.ts lib/server/billing/provision-workspace-for-subscription.test.ts` |
> Restructure the startup dashboard into Overview / Audits / Delivery / Settings via `?tab=` (default `overview`). Plan: `.cursor/plans/startup_dashboard_tab_redesign_997f45c9.plan.md`. Shared types: `app/dashboard/startup/components/startup-tab-types.ts`.

| TASK ID | Description | Owner | Status | Evidence |
|---|---|---|---|---|
| TR-001 | Contract freeze: register TR tasks, shared tab prop types + audit filter types | Frontend | âœ… DONE | `startup-tab-types.ts`, PROJECT_STATE, COMPLETION_LOG TR-001 |
| TR-002 | Tab bar: four links with `?tab=`, preserve `startupWorkspace`, active styling | Frontend | âœ… DONE | `startup-tab-bar.tsx`, `page.tsx`, `parseStartupDashboardTab`, COMPLETION_LOG TR-002 |
| TR-003 | Extract Overview tab (stats, trend, backlog, implementation lane, PR activity) | Frontend | âœ… DONE | `startup-overview-tab.tsx`, `page.tsx`, COMPLETION_LOG TR-003 |
| TR-004 | Extract Settings tab (Slack, GitHub, workspace info) | Frontend | âœ… DONE | `startup-settings-tab.tsx`, `page.tsx`, COMPLETION_LOG TR-004 |
| TR-005 | Extract Delivery tab (send form, delivery metrics, auto-post badge, delivery history) | Frontend | âœ… DONE | `startup-delivery-tab.tsx`, `page.tsx`, COMPLETION_LOG TR-005 |
| TR-006 | Audits tab: filterable history table (presets + custom range, URL params) | Frontend | âœ… DONE | `startup-audits-tab.tsx`, `parseStartupAuditFilterFromSearchParams`, `page.tsx`, COMPLETION_LOG TR-006 |
| TR-007 | Audits tab: compare-two mode with side-by-side score/grade/recommendations delta | Frontend | âœ… DONE | `startup-audits-table-client.tsx`, `StartupAuditRowModel`, `startup-audits-tab.tsx`, COMPLETION_LOG TR-007 |
| TR-008 | Thin `page.tsx`: `?tab=` routing + status-param auto-routing + tab-scoped banners | Frontend | âœ… DONE | `startup-dashboard-status-messages.ts`, `load-startup-dashboard-context.ts`, `startup-dashboard-page-shell.tsx`, `page.tsx`, COMPLETION_LOG TR-008 |
| TR-009 | Cross-tab empty states, line-count guardrails, type-check + E2E proof | Frontend | âœ… DONE | empty CTAs + `data-testid` hooks, `tests/e2e/startup-dashboard-tabs.spec.ts`, COMPLETION_LOG TR-009 |

### Architecture Hardening + Ops Observability
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| AH-001 | Define the architecture hardening backlog with explicit slices for lean-code discipline, observability, and maintenance risk reduction | Architect + Backend | Ã¢Å“â€¦ DONE | `PLAYBOOK/architecture-hardening-backlog.md`; COMPLETION_LOG AH-001 |
| AH-002 | Add persistent structured application logs so internal events can be inspected without backend console access | Backend + Database | Ã¢Å“â€¦ DONE | `supabase/migrations/013_app_logs.sql`, `lib/server/structured-log.ts`; COMPLETION_LOG AH-002 |
| AH-003 | Add `/dashboard/logs` as an admin-only internal logs viewer over persisted structured logs | Frontend + Backend | Ã¢Å“â€¦ DONE | `app/dashboard/logs/page.tsx`, `lib/server/admin-logs-data.ts`; COMPLETION_LOG AH-003 |
| AH-004 | Add Playwright readiness guidance so future browser-level testing can land on stable UI and operator flows | Architect + QA | Ã¢Å“â€¦ DONE | `PLAYBOOK/architecture-hardening-backlog.md`; COMPLETION_LOG AH-004 |
| AH-005 | Add the first thin-route cleanup slice by extracting a small shared admin runtime helper for repeated auth/env/service-role setup | Backend | Ã¢Å“â€¦ DONE | `lib/server/admin-runtime.ts`, admin dashboard pages, `app/dashboard/benchmarks/actions.ts`; COMPLETION_LOG AH-005 |
| AH-006 | Add the first large-module decomposition slice by reducing the benchmark run-detail route to a thin shell and moving detail rendering/helpers into dedicated modules | Backend + Frontend | Ã¢Å“â€¦ DONE | `app/dashboard/benchmarks/[runGroupId]/page.tsx`, `components/benchmark-run-detail-view.tsx`, `lib/server/benchmark-run-detail.ts`; COMPLETION_LOG AH-006 |
| AH-007 | Document the admin-auth maturity path from the current env-based founder-stage gate to a future table-driven admin allowlist / role model without changing runtime auth behavior yet | Architect + Security | Ã¢Å“â€¦ DONE | `PLAYBOOK/architecture-hardening-backlog.md`, `lib/server/require-admin.ts`, `app/admin/login/actions.ts`; COMPLETION_LOG AH-007 |
| AH-008 | Add the first open-source Playwright browser smoke-test foundation and wire it into CI for narrow public-entry-flow coverage | QA + Frontend | Ã¢Å“â€¦ DONE | `playwright.config.ts`, `tests/e2e/smoke.spec.ts`, `.github/workflows/ci.yml`; COMPLETION_LOG AH-008 |
| AH-009 | Add the second large-module decomposition slice by reducing `components/report-viewer.tsx` to a shell and moving helpers/sections into dedicated modules with browser proof | Backend + Frontend | Ã¢Å”â€¦ DONE | `components/report-viewer.tsx`, `components/report-viewer-sections.tsx`, `lib/client/report-viewer.ts`; COMPLETION_LOG AH-009 |

### UX/IA Redesign Stream — Stitch prompt generation
> **Goal:** Unified dashboard shell, one connectors page, separate admin console, WhatNextBanner on every home. Each task = one Stitch prompt + screen. No code changes until all Stitch screens are approved. Plan: `.claude/plans/snoopy-hatching-scroll.md`.
> **Note:** Task IDs use UXIA prefix to avoid collision with prior UX loading-state tasks (UX-001...UX-008).

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| UXIA-001 | Dashboard Shell Stitch prompt (sidebar + header + workspace switcher) | Frontend | ✅ DONE | `components/dashboard-sidebar.tsx`, `app/dashboard/layout.tsx`; COMPLETION_LOG 2026-04-05 |
| UXIA-002 | Dashboard Home — Individual persona | Frontend | ✅ DONE | `app/dashboard/page.tsx`, `components/what-next-banner.tsx`; COMPLETION_LOG 2026-04-05 |
| UXIA-003 | Dashboard Home — Agency persona | Frontend | ✅ DONE | `app/dashboard/page.tsx` agency section; COMPLETION_LOG 2026-04-05 |
| UXIA-004 | Dashboard Home — Startup persona | Frontend | ✅ DONE | `app/dashboard/page.tsx` startup section; COMPLETION_LOG 2026-04-05 |
| UXIA-005 | New Scan page (in-dashboard, context-aware) | Frontend | ✅ DONE | `app/dashboard/new-scan/page.tsx`; COMPLETION_LOG 2026-04-05 |
| UXIA-006 | Connectors Page (GitHub + Slack + future placeholders) | Frontend | ✅ DONE | `app/dashboard/connectors/page.tsx`, sidebar updated; COMPLETION_LOG 2026-04-05 |
| UXIA-007 | Workspace Settings page (metadata, bundle, rollout flags) | Frontend | ✅ DONE | `app/dashboard/workspace/page.tsx`, sidebar updated; COMPLETION_LOG 2026-04-05 |
| UXIA-008 | Admin Console Shell + Home (separate shell, amber tint) | Frontend | ✅ DONE | `app/admin/layout.tsx`, `app/admin/page.tsx`; COMPLETION_LOG 2026-04-05 |
| UXIA-009 | Admin Workspace Management (agencies + startups) | Frontend | ✅ DONE | `app/admin/agencies/page.tsx`, `app/admin/startups/page.tsx`; COMPLETION_LOG 2026-04-05 |
| UXIA-010 | Admin Service Control | Frontend | ✅ DONE | `app/admin/services/page.tsx`; COMPLETION_LOG 2026-04-05 |
| UXIA-011 | Admin Logs | Frontend | ✅ DONE | `app/admin/logs/page.tsx`; COMPLETION_LOG 2026-04-05 |
| UXIA-012 | Scan Results page (redesigned action card + step framing) | Frontend | ✅ DONE | `components/results-view.tsx`; COMPLETION_LOG 2026-04-05 |
| UXIA-MOB | Full mobile responsiveness pass — sidebar hamburger, admin sidebar client extract, connectors grid fix, new-scan context gap fix | Frontend | ✅ DONE | `components/dashboard-sidebar.tsx`, `components/admin-sidebar.tsx`, `app/admin/layout.tsx`, `app/dashboard/connectors/page.tsx`, `app/dashboard/new-scan/page.tsx`; COMPLETION_LOG 2026-04-05 |

---

| AH-010 | Add the third large-module decomposition slice by reducing the benchmark overview route to a thin shell and moving overview rendering/helpers into dedicated modules with existing browser proof retained | Backend + Frontend | Ã¢Å“â€¦ DONE | `app/dashboard/benchmarks/page.tsx`, `components/benchmark-overview-view.tsx`, `lib/server/benchmark-overview.ts`; COMPLETION_LOG AH-010 |

| AH-011 | Add the fourth large-module decomposition slice by extracting shared deep-audit report preparation helpers out of the PDF builder without changing report behavior or adding UI churn | Backend | Ã¢Å“â€¦ DONE | `workers/report/deep-audit-report-helpers.ts`, `workers/report/build-deep-audit-pdf.ts`, `workers/report/build-deep-audit-markdown.ts`; COMPLETION_LOG AH-011 |

| AH-012 | Add the fifth large-module decomposition slice by extracting crawl-pending parsing and chunk-window planning into a dedicated state helper module without changing crawl behavior | Backend | Ã¢Å“â€¦ DONE | `workers/scan-engine/deep-audit-crawl-state.ts`, `workers/scan-engine/deep-audit-crawl.ts`; COMPLETION_LOG AH-012 |
| AH-013 | Add the sixth large-module decomposition slice by extracting shared email-delivery composition helpers out of the Resend path without changing delivery behavior or adding UI churn | Backend | Ã¢Å“â€¦ DONE | `workers/report/resend-delivery-helpers.ts`, `workers/report/resend-delivery.ts`; COMPLETION_LOG AH-013 |

### Report Design Ã¢â‚¬â€ Phase A (audit-defensible narrative upgrade)
> **Goal:** Upgrade the paid report from commodity-SEO-audit framing to a CRO-level intelligence document that a full team can act on Ã¢â‚¬â€ without introducing numeric revenue claims or live competitor data we cannot support. Phase A is docs/design only. No runtime report generation changes. No PDF/UI changes. No rewriter default change.
> **Guardrails:** Keep all new framing audit-defensible. One slice at a time. Lean architecture. Low maintenance.
> **Spec:** `PLAYBOOK/rd-001-team-owner-taxonomy-v1.md` and future RD PLAYBOOK files.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| RD-001 | Team-owner taxonomy: map all 22 implemented checks to 4 owners (Engineering / Content / Brand / Product) | Architect | Ã¢Å“â€¦ DONE | `PLAYBOOK/rd-001-team-owner-taxonomy-v1.md`; COMPLETION_LOG RD-001 |
| RD-002 | Executive Brief contract: headline framing, 3-bullet summary, #1 action, directional exposure language (no numeric estimates) | Architect | Ã¢Å“â€¦ DONE | `PLAYBOOK/rd-002-executive-brief-contract-v1.md`; COMPLETION_LOG RD-002 |
| RD-005 | Section order contract update: Executive Brief Ã¢â€ â€™ Immediate Wins Ã¢â€ â€™ Team Action Map Ã¢â€ â€™ Score Summary Ã¢â€ â€™ Best Practices Ã¢â€ â€™ Coverage Ã¢â€ â€™ Appendix | Architect | Ã¢Å“â€¦ DONE | `PLAYBOOK/rd-005-section-order-contract-v1.md`; COMPLETION_LOG RD-005 |
| RD-006 | Immediate Wins format: implementation-ticket style, not audit-finding style | Architect | Ã¢Å“â€¦ DONE | `PLAYBOOK/rd-006-immediate-wins-format-v1.md`; COMPLETION_LOG RD-006 |
| RD-004 | "What AI-Ready Leaders Do Differently": audit-derived best-practice framing only, no live competitor data | Architect | Ã¢Å“â€¦ DONE | `PLAYBOOK/rd-004-ai-ready-leaders-contract-v1.md`; COMPLETION_LOG RD-004 |
| RD-003 | Directional business exposure language placeholder: template language only, no formula, no numbers; marked pending data layer | Architect | Ã¢Â¬Å“ DEFERRED | Holds until RD-002 through RD-006 narrative is validated on real reports |
| RD-007 | Standalone team-owner mapping artifact: `TeamOwner` type, `TEAM_OWNER_MAP`, `getTeamOwner` helper + test | Backend | Ã¢Å“â€¦ DONE | `workers/report/team-owner-map.ts`, `workers/report/team-owner-map.test.ts`; type-check 0 errors; 5/5 tests pass; COMPLETION_LOG RD-007 |

### GEO Performance Monitoring (GPM-001 … GPM-022)
> New billable service: recurring AI visibility reports (ChatGPT + Gemini + Perplexity) for startup and agency clients. Integrated into existing benchmark runner, service entitlement, workspace, PDF, and delivery infrastructure — not a microservice. All paid bundles get all three platforms. Spec: `PLAYBOOK/geo-performance-monitoring-v1.md`. Build in strict layer order: provider adapters → client config → scheduled runs → report generation → admin UI.

#### Layer 1 — Provider Adapters + Data Foundation
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| GPM-001 | Freeze GEO Performance Monitoring v1 scope, service definition, package model, and task registry | Architect + Product | ✅ DONE | `PLAYBOOK/geo-performance-monitoring-v1.md`, `agents/memory/PROJECT_STATE.md` |
| GPM-002 | Add OpenAI (ChatGPT) provider adapter to `lib/server/benchmark-execution.ts` with env-gated model config | Backend | ✅ DONE | `lib/server/benchmark-execution.ts` `OpenAiCompatibleBenchmarkExecutionAdapter`; `OPENAI_API_KEY`, `OPENAI_MODEL`, `OPENAI_ENDPOINT` env vars; type-check 0 errors; 42/42 tests |
| GPM-003 | Add Perplexity provider adapter to `lib/server/benchmark-execution.ts` reusing OpenAI-compatible request format | Backend | ✅ DONE | `lib/server/benchmark-execution.ts` `OpenAiCompatibleBenchmarkExecutionAdapter` with `providerTag: 'perplexity'`; `MultiProviderBenchmarkExecutionAdapter` for all-three routing; `PERPLEXITY_API_KEY`, `PERPLEXITY_MODEL`, `PERPLEXITY_ENDPOINT` env vars; type-check 0 errors; 42/42 tests |
| GPM-004 | Harden `rankPosition` extraction in `lib/server/benchmark-citations.ts` to consistently populate rank from numbered lists, ordered bullets, and explicit first/second/third language; keep null when order is genuinely ambiguous | Backend | ✅ DONE | `detectResponseStructure` (numbered_list/bullet_list/ordinal_prose/prose), `buildLineSegments`, `getRankAtOffset`; prose → all null; `response_structure` stored in citation metadata; 6 new tests (17 total citations tests passing) |
| GPM-005 | Add per-platform visibility metric split — compute `chatgpt_visibility_pct`, `gemini_visibility_pct`, `perplexity_visibility_pct` separately in `lib/server/benchmark-metrics.ts` and store in `benchmark_domain_metrics.metrics JSONB` | Backend | ✅ DONE | `inferProviderFromModelId` (handles gpt-/openai/o-series, gemini-/models/gemini-/google/, sonar/llama/perplexity/); `visibilityPctByPlatform` on return type; `chatgpt_visibility_pct`, `gemini_visibility_pct`, `perplexity_visibility_pct` in metrics JSONB; 10 new tests (14 total metrics tests passing) |
| GPM-006 | Add competitor co-citation extraction — for each `query_run`, parse all brand/domain mentions in the response beyond the measured domain and persist them as competitor citation rows; design the storage shape at implementation time | Backend | ✅ DONE | `parseCompetitorCitations(responseText, competitorList, measuredCanonicalDomain)` in `benchmark-citations.ts`; handles domain-like competitors (URL then domain fallback) and brand-name competitors (brand_mention); `metadata.is_competitor: true` + `competitor_name` on each citation; rank lookup reuses structure/segments; 8 new tests (25 total citations tests passing) |
| GPM-007 | Add `industry_rank` metric (average `rankPosition` across completed query runs where client was cited) to `lib/server/benchmark-metrics.ts` and store in `benchmark_domain_metrics.metrics JSONB` | Backend | ✅ DONE | Implemented as part of GPM-005 — `industry_rank` computed in `computeBenchmarkMetrics`, stored in `metrics.industry_rank`; null when no ranked citations exist; test coverage included |

#### Layer 2 — Client Configuration
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| GPM-008 | Add `client_benchmark_configs` table migration: `workspace_id`, `benchmark_domain_id`, `topic`, `location`, `query_set_id`, `competitor_list`, `cadence`, `platforms_enabled`, `report_email`; add workspace→benchmark_domain FK | Backend + Database | ✅ DONE | `supabase/migrations/043_client_benchmark_configs.sql`; `ClientBenchmarkConfigRow` type + 6 repo functions (insert/get/listByStartup/listByAgency/update/delete) in `benchmark-repository.ts`; `'geo_performance_monitoring'` added to `SERVICE_KEYS`; admin CRUD at `app/admin/geo-performance/` (actions.ts + page.tsx); 442 tests passing, tsc 0 errors |
| GPM-009 | Add typed server contracts and helpers for client benchmark configuration — CRUD, validation, and entitlement-aware resolver | Backend | ✅ DONE | `lib/server/geo-performance-entitlements.ts`: `ResolvedGeoPerformanceEntitlement`, `resolveGeoPerformanceCaps(bundleKey)` (pure, testable), `resolveGeoPerformanceEntitlement(args)` (DB-backed, wraps `resolveServiceEntitlement`), `validateClientBenchmarkConfigInput`, `enforceGeoPerformanceLimits`; 23/23 tests, tsc 0 errors |
| GPM-010 | Add admin-side guided prompt set builder: given topic + location, generate and persist a `benchmark_query_set` with intent-typed prompts (high-intent, informational, local, branded) without requiring manual fixture seeding | Backend + Frontend | ✅ DONE | `lib/server/geo-performance-prompt-builder.ts`: `buildGpmQueryPrompt` (pure, testable), `parseGpmQueryResponse` (Zod validation + dedup + fence stripping), `generateGpmPrompts` (Anthropic Messages API via fetch, `claude-haiku-4-5-20251001` default), `generateAndSeedGpmQuerySet` (generates + seeds via existing `seedBenchmarkQuerySet`); admin action `generateQuerySetForConfig` in `app/admin/geo-performance/actions.ts`; inline "Generate with Claude" form per config card; `ANTHROPIC_API_KEY` + GPM env vars documented; 18 pure-function tests, tsc 0 errors |
| GPM-011 | Add competitor roster management per client workspace — seed, update, and link competitor list to co-citation extraction path | Backend + Frontend | PENDING | Depends on GPM-009 |

#### Layer 3 — Scheduled Client Runs
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| GPM-012 | Register `geo_performance_monitoring` as a service in the `services` catalog and map it to `startup_dev`, `agency_core`, and `agency_pro` bundles with entitlement defaults (prompts per run, cadence, platforms) | Backend + Database | ✅ DONE | `supabase/migrations/044_geo_performance_service_catalog.sql`: inserts `geo_performance_monitoring` into `service_catalog` (category: analytics, default_access_mode: off, metadata captures cadence_caps/prompt_caps/delivery_surfaces per bundle tier); seeds `service_bundle_services` for all four bundles (startup_lite off, startup_dev/agency_core/agency_pro enabled + paid access_mode); tsc 0 errors |
| GPM-013 | Extend `lib/server/benchmark-schedule.ts` to trigger client-workspace-scoped runs on subscription cadence — resolve due workspaces, check entitlement, enqueue runs | Backend | ✅ DONE (built ahead of GPM-010–012) | `lib/server/geo-performance-schedule.ts`: `resolveGpmWindowDate` (monthly/biweekly/weekly ISO window strings), `buildGpmRunKey` (idempotency via `schedule_run_key` in run group metadata), `executeGpmClientRun` (entitlement gate → per-platform run → competitor co-citation post-processing), `runGpmScheduledSweep` (fetches all client_benchmark_configs, sweeps with GPM_SCHEDULE_ENABLED guard), `resolveGpmPlatformModelMap` (env-configurable model IDs with defaults); 17 pure-function tests, tsc 0 errors |
| GPM-014 | Add `workspace_id` FK to `benchmark_run_groups` so client runs are scoped and queryable per workspace without leaking across accounts | Backend + Database | PENDING | Depends on GPM-013 |

#### Layer 4 — Report Generation + Delivery
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| GPM-015 | Build GEO Performance Report PDF template: summary paragraph, AI visibility cards (per-platform %), industry rank, top prompts table (prompt / avg rank / ChatGPT / Gemini / Perplexity), competitor bar chart, opportunities section | Backend | PENDING | Depends on GPM-005, GPM-007 |
| GPM-016 | Add Claude-generated summary narrative for GEO Performance Reports — brand, topic, location, period, headline visibility number, top win, top opportunity | Backend | PENDING | Depends on GPM-015 |
| GPM-017 | Store generated GEO Performance Report in R2 and persist a report record linked to `workspace_id` and `benchmark_run_group_id` | Backend | PENDING | Depends on GPM-015 |
| GPM-018 | Wire GEO Performance Report delivery via existing report queue worker on run completion — email report PDF to workspace `report_email` via Resend | Backend | PENDING | Depends on GPM-017 |
| GPM-019 | Add optional Slack delivery for eligible workspaces — post report summary card to configured Slack destination using existing `lib/server/startup-slack-integration.ts` path | Backend | PENDING | Depends on GPM-018 |

#### Layer 5 — Admin Configuration Portal
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| GPM-020 | Add admin client benchmark config UI — view and edit topic, location, competitor list, cadence, and platforms per workspace; surface in `/dashboard/startups` and `/dashboard/agencies` workspace detail | Frontend + Backend | PENDING | Depends on GPM-011 |
| GPM-021 | Add package entitlement controls for `geo_performance_monitoring` in `/dashboard/services` — prompts per run, cadence, and report delivery surface per bundle; admin overrideable per workspace | Frontend + Backend | PENDING | Depends on GPM-012 |
| GPM-022 | Add manual run trigger and report preview from admin panel — trigger one GPM run for any configured workspace immediately, preview the generated PDF before first scheduled delivery | Frontend + Backend | PENDING | Depends on GPM-020, GPM-021 |

---

## Active Blockers

- `P4-003` operator blocker: domain purchase / DNS setup paused due to founder credit-card issue Ã¢â‚¬â€ 2026-03-26
- `P4-006` blocked on `P4-003` DNS evidence plus final security sign-off Ã¢â‚¬â€ 2026-03-26
- `P4-004` WAF remains a launch-policy decision (`DEFERRED / mitigated` in repo, but not fully closed operationally)

---

## Architecture Decisions Log (summary)
> Full ADRs in `agents/memory/DECISIONS.md`

| ID | Decision | Status |
|----|----------|--------|
| ADR-001 | Use `@opennextjs/cloudflare`, not deprecated `next-on-pages` | Ã¢Å“â€¦ Final |
| ADR-002 | pdf-lib for PDF generation (not Puppeteer) | Ã¢Å“â€¦ Final |
| ADR-003 | Gemini 2.5 Flash-Lite as LLM (provider is a config var) | Ã¢Å“â€¦ Final |
| ADR-004 | Supabase RLS on all tables; leads = service_role only | Ã¢Å“â€¦ Final |
| ADR-005 | n8n self-hosted on Oracle Cloud Free (not Make.com) | Ã¢Å“â€¦ Final |
| ADR-006 | API-first architecture: GEO-Pulse is an API with a UI on top | Ã¢Å“â€¦ Final |

---

## State history
| Date | Change |
|------|--------|
| 2026-03-24 | Phase 0 scaffold gate closed; **Current Phase Ã¢â€ â€™ Phase 1 Ã¢â‚¬â€ Core Scan Engine**. P0-002Ã¢â‚¬Â¦P0-006 Ã¢Å“â€¦; API-001 Ã¢Å“â€¦ (remote matches `002_api_keys.sql`). |
| 2026-03-24 | **Phase 1 Ã¢â‚¬â€ Core Scan Engine** implementation complete; **Current Phase Ã¢â€ â€™ Phase 2 Ã¢â‚¬â€ Payment + PDF + Email**. P1-001Ã¢â‚¬Â¦P1-011 Ã¢Å“â€¦ (see COMPLETION_LOG). |
| 2026-03-24 | **Phase 1Ã¢â€ â€™2 manual gate:** live scan + results + email capture reported successful (domain: techehealthservices.com). |
| 2026-03-24 | **Sequencing:** API-as-a-Service (**API-002Ã¢â‚¬Â¦007**) deferred until **Phase 2 + Phase 3** complete and **Phase 3Ã¢â€ â€™4 gate** satisfied (`ORCHESTRATOR.md`). Operator: $1 payment e2e reported working; Phase 2 task registry + COMPLETION_LOG evidence still to be closed out. |
| 2026-03-24 | **Phase 2 + Phase 3** implementation landed (auth, dashboard, `guest_email` on payments, middleware). **Current Phase Ã¢â€ â€™ Phase 4 Ã¢â‚¬â€ Launch**. P2-001Ã¢â‚¬Â¦P2-007 Ã¢Å“â€¦; P2-008 operator QA; P3-001Ã¢â‚¬Â¦P3-004 Ã¢Å“â€¦. Run `supabase db push` for `004_payments_guest_email.sql`. |
| 2026-03-24 | **Phase 4 (partial):** P4-002 OG + P4-005 keep-alive cron **Ã¢Å“â€¦ in repo** (`COMPLETION_LOG.md` Phase 4 bundle). P4-001/P4-003/P4-004/P4-006 **pending** operator + Security. API-002Ã¢â‚¬Â¦007 deferral clarified Ã¢â€ â€™ **after Phase 4 launch gate**. |
| 2026-03-24 | **Deep Audit Upgrade** initiative added to Task Registry (**DA-001Ã¢â‚¬Â¦DA-005**). Spec: `.cursor/plans/report_depth_and_formats_fa7e556e.plan.md`; companion: [`PLAYBOOK/audit-upgrade.md`](../../PLAYBOOK/audit-upgrade.md). Sequencing vs Phase 4: Orchestrator call (parallel vs after P4 operator work). |
| 2026-03-24 | **PLAYBOOK:** Renamed `audit uprade.md` Ã¢â€ â€™ [`audit-upgrade.md`](../../PLAYBOOK/audit-upgrade.md); links in this file updated. |
| 2026-03-25 | **Checkout rate limit + DA-001:** Operator note Ã¢â‚¬â€ `POST /api/checkout` capped at **5/IP/hour** (wait or alternate IP). **DA-001** marked **IN_PROGRESS**; parallel with Phase 4 operator work per `agents/ORCHESTRATOR.md`. |
| 2026-03-25 | **DA-001 (implementation):** `scan_runs` / `scan_pages` migration + RLS; paid flow creates `scan_run` and enqueues **v2**; Worker runs capped same-origin crawl, updates `scans` aggregate + PDF multi-page. **Pending:** remote DB migration + Orchestrator acceptance + optional Security note (fetch path unchanged from P1 `fetch-page` until DA-002). |
| 2026-03-25 | **DA-001 Ã¢â‚¬â€ CLOSED (Orchestrator):** Paid deep-audit smoke on **techehealthservices.com** Ã¢â‚¬â€ multi-page PDF (10 URLs, aggregate score, per-page checklist). **DA-002** marked **IN_PROGRESS** (central fetch gate + robots/sitemap + sampling; Security review on fetch/SSRF). |
| 2026-03-25 | **DA-002 Ã¢â‚¬â€ DONE (implementation):** Central fetch gate (`fetchGateText`, stream byte cap), `validateEngineFetchUrl` (http/https for engine only), robots.txt + sitemap.xml discovery, section-aware URL ordering, `scan_pages.section`; free scan still uses HTTPS-only `validateUrl`. **Next:** **DA-003**. |
| 2026-03-25 | **DA-004 (incremental):** `parseRobotsTxt` reads `Crawl-delay` for `*`; `deep-audit-crawl` sleeps between non-seed fetches (cap 10s); `scan_runs.coverage_summary` adds `wall_time_ms`, `pages_errored`, `crawl_delay_ms`; `structuredLog` `deep_audit_crawl_complete`. Full DA-004 (Workflows / 100+ pages) still deferred. |
| 2026-03-25 | **Orchestrator sequencing:** Complete **Phase 4 Ã¢â€ â€™ Launch** (P4-001, P4-003, P4-004, P4-006 + evidence) **before** resuming **DA-004** remainder / **DA-005** Ã¢â‚¬â€ see `agents/ORCHESTRATOR.md` Ã‚Â§ *Phase 4 first Ã¢â‚¬â€ defer remaining Deep Audit until Launch gate*. |
| 2026-03-25 | **Phase 4 Ã¢â‚¬â€ P4-001 + P2-008:** Production **`https://geo-pulse.uzzielt.workers.dev`** Ã¢â‚¬â€ live Stripe checkout success return path verified. **P4-003 / P4-004 / P4-006** still need evidence in `COMPLETION_LOG.md` for full Launch gate. |
| 2026-03-25 | **DA-004 (ops):** `DEEP_AUDIT_DEFAULT_PAGE_LIMIT` plaintext var in `wrangler.jsonc` + `getPaymentApiEnv` Ã¢â‚¬â€ default page cap for new paid `scan_runs`; `lib/server/deep-audit-page-limit.ts` single source for max + parsing. |
| 2026-03-26 | **UCD Report Journey Overhaul (5 phases) Ã¢â‚¬â€ COMPLETE.** Phase 1: 4 differentiating checks (AI crawler access, llms.txt, snippet eligibility, E-E-A-T signals) + `CheckContext` extended + weights rebalanced to 100 across 17 checks. Phase 2: PDF report rebuilt Ã¢â‚¬â€ branded cover, exec summary, score breakdown table, priority action plan, per-page breakdown, branded footer. Phase 3: Delivery email rebuilt Ã¢â‚¬â€ branded header, score badge, top 3 priorities w/ severity, CTA button, inline CSS. Phase 4: User-centered language rewrite across all components. Phase 5: Results page restructured Ã¢â‚¬â€ score hero Ã¢â€ â€™ category health grid (4 pillars) Ã¢â€ â€™ upgrade strip (dark) Ã¢â€ â€™ numbered issues w/ severity chips Ã¢â€ â€™ email gate Ã¢â€ â€™ rescan link. Evidence: type-check 0 errors, 50/50 tests passed, build success. |
| 2026-03-26 | **Marketing Attribution Microservice (MA-001 Ã¢â€ â€™ MA-004) Ã¢â‚¬â€ COMPLETE.** Parallel build: `analytics.marketing_events` (007), Zod contract + idempotent ingestion (39 tests), client UTM/session capture (`gp_anon_id` cookie + `gp_utm` sessionStorage), emits from scan/leads/checkout/webhook, first-touch/last-touch views (008), weekly funnel aggregate, Monday cron email (opt-in `MARKETING_REPORT_TO`). `leads.scan_id` now persisted. MA-005 (queue hardening) deferred. Evidence: type-check 0 errors, 89/89 tests, 0 lint errors. |
| 2026-03-26 | **Admin & report eval (ADM-001 Ã¢â‚¬Â¦ EVAL-004) Ã¢â‚¬â€ implementation in repo.** `/admin/login` password for `ADMIN_EMAIL`, `report_eval_runs` + `reports.markdown_url`, `/dashboard/evals`, `npm run eval:smoke`. See Task Registry **Admin & report quality eval** + `COMPLETION_LOG.md`. **Phase 4** still blocked on P4-003 / P4-006 operator evidence per `ORCHESTRATOR.md`. |
| 2026-03-26 | **P4-004 / P4-006 (tests):** Unit tests for CVE-2025-29927 middleware guard (`lib/server/middleware-cve.ts`, `middleware-cve.test.ts`) Ã¢â‚¬â€ supplements `middleware.ts` belt-and-suspenders check; paste Vitest output into `COMPLETION_LOG` for Security review. |
| 2026-03-26 | **v2 Enhancement Plan Ã¢â‚¬â€ Tier 1 COMPLETE.** T1-1: `CheckStatus` 6-value enum + `CheckCategory` 5-value type in `audit.ts`. T1-2: 17 checks mapped to categories (ai_readiness, extractability, trust). T1-3: `computeCategoryScores` in `scoring.ts` with v2 weighting (BLOCKED/NOT_EVALUATED excluded; LOW_CONFIDENCE = 50%). T1-4: `score-display.tsx` 5-pillar category grid (replaces 4-pillar). T1-5: category breakdown table in Markdown + PDF reports. LLM checks now carry `confidence` + `status: LOW_CONFIDENCE` when confidence is low. Evidence: type-check 0 errors, 96/96 tests pass. |
| 2026-03-26 | **Task ledger reconciliation:** v2 plan pointer in `PROJECT_STATE.md` marked stale (referenced `.cursor/plans/...` file not present in repo). Task statuses corrected for implemented checks/views: **T2-1Ã¢â‚¬Â¦T2-6** and **T3-6** set to **DONE** based on shipped code. Added explicit bite-size backlog sections: **Audit Closure Ã¢â‚¬â€ Report Integrity + Product Truth** (**AU-001Ã¢â‚¬Â¦AU-010**) and **Retrieval / Evaluation Backlog** (**RE-001Ã¢â‚¬Â¦RE-010**) including `promptfoo` and `ragas` as backlog items only, not implemented features. |
| 2026-03-26 | **AU-001 Ã¢â‚¬Â¦ AU-005 / AU-007 Ã¢â‚¬â€ COMPLETE (implementation).** Deep-audit reports now separate `highlightedIssues` from `allIssues`, render the full sitewide breakdown, preserve v2 statuses in Markdown/PDF, keep `full_results_json` aligned with `categoryScores` + report metadata, expose coverage summary in customer-facing report output, and remove inaccurate checkout copy about exact check count. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **AU-009 / AU-010 Ã¢â‚¬â€ COMPLETE (implementation).** Report eval moved from shallow structural checks to content-integrity assertions, and golden fixtures now cover multi-page output plus blocked / low-confidence / not-evaluated statuses. `scripts/report-eval-smoke.ts` now uses rubric version `integrity-v2`. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-001 / RE-002 Ã¢â‚¬â€ COMPLETE (foundation).** Added retrieval-eval scope doc (`PLAYBOOK/retrieval-eval-foundation.md`) and schema scaffold (`010_retrieval_eval_foundation.sql`) for future retrieval simulation, `promptfoo`, and possible `ragas` adoption. No runtime retrieval harness implemented yet. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **AU-006 / AU-008 Ã¢â‚¬â€ COMPLETE (implementation).** Deep-audit reports now include a structured technical appendix in Markdown/PDF, and the SSRF/security docs were updated to match the implemented protection model and Cloudflare Workers runtime limitations. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-003 Ã¢â‚¬â€ COMPLETE (foundation).** Added deterministic retrieval simulation helpers in `lib/server/retrieval-eval.ts` with passage extraction, lexical ranking, and prompt-level metrics. This is an offline harness only; no `promptfoo`, `ragas`, or live engine integration yet. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-004 Ã¢â‚¬â€ COMPLETE (design / truth pass).** Added a benchmark-percentile design note and removed unsupported percentile claims from the current API contract, PRD report promise, OG-image guidance, and audit-upgrade metrics. Percentile output remains deferred until cohorting, snapshots, and guardrails exist. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-005 Ã¢â‚¬â€ COMPLETE (implementation).** Added a repo-local `promptfoo` skeleton with report and retrieval suites, custom local providers, a repo-scoped runner to avoid user-profile writes, package scripts, and `.promptfoo/` ignore. This is a regression harness only; `ragas` remains pending. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-006 / RE-007 Ã¢â‚¬â€ COMPLETE (implementation / evaluation).** Promptfoo report coverage now checks executive summary extraction, fix specificity, and status preservation. Added a formal `ragas` fit note with a current no-go decision until the retrieval dataset and answer-generation layer are mature enough to justify semantic scoring. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-011 Ã¢â‚¬Â¦ RE-015 Ã¢â‚¬â€ COMPLETE (implementation).** Eval analytics now carry stable site/framework metadata via `011_eval_run_metadata.sql`; Promptfoo runs can be written into Supabase with `scripts/promptfoo-eval-write.ts`; `/dashboard/evals` is now a site-centric admin analytics view spanning report + retrieval runs with framework filters, trend views, and an explicit RAGAS placeholder lane. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-017 Ã¢â‚¬â€ COMPLETE (implementation).** Added a deterministic retrieval writer over the existing foundation: fixture-driven site pages + prompts are simulated, persisted into `retrieval_eval_runs`, `retrieval_eval_prompts`, `retrieval_eval_passages`, and `retrieval_eval_answers`, and can now support future admin drilldown. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-018 Ã¢â‚¬â€ COMPLETE (implementation).** Added a retrieval-run admin drilldown page from `/dashboard/evals` and made the landing header session-aware so `Dashboard` only appears when a user is logged in. Signed-in users now see `Dashboard` + `Sign out`; signed-out users see only sign-in actions. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **DA-004 (incremental resume):** Added chunk-progress metrics and continuation guardrails to `crawl_pending` / `coverage_summary`, surfaced chunk metrics in Markdown/PDF coverage output, raised the deep-audit cap to **1000** pages, and aligned final coverage reporting with the configured chunk size. |
| 2026-03-26 | **DA-004 Ã¢â‚¬â€ COMPLETE (user-directed reopen).** Added a targeted `deep-audit-crawl` Vitest that exercises partial requeue, `crawl_pending`, continuation, and final coverage metrics for a multi-chunk run. `npm.cmd run type-check` passed and `npx.cmd vitest run workers/scan-engine/deep-audit-crawl.test.ts` passed (7/7). Workflows is now tracked as optional future scale work, not DA-004 remainder. |
| 2026-03-26 | **UX-001 Ã¢â‚¬â€ COMPLETE (implementation).** Added a centralized delayed long-wait overlay with animated step progression and wired it into scan submit, checkout, email save, login, admin login, results load, and report load. Fast-path buttons still use local pending labels; the overlay only appears after a delay for longer waits. `npm.cmd run type-check` and `npm.cmd run build` passed. |
| 2026-03-26 | **P4-006 repo evidence refreshed.** Added targeted Turnstile and Stripe webhook route tests, then re-ran the Phase 4 security Vitest slice (`middleware-cve`, `turnstile`, `ssrf`, `checkout-completed`, webhook route): 5 files / 25 tests passed. `npm.cmd run type-check` and `npm.cmd run build` passed. `P4-006` remains pending until operator DNS evidence (`P4-003`) and final security sign-off are attached. |
| 2026-03-26 | **Phase 4 operator pause recorded.** Founder could not complete domain purchase because of a credit-card issue. `P4-003` is now explicitly operator-blocked, and final `P4-006` sign-off remains pending on that DNS evidence. Repo-side security/test work is current; revisit once domain purchase can be completed. |
| 2026-03-26 | **UX journey clarity stream opened.** User testing showed that the scan Ã¢â€ â€™ preview Ã¢â€ â€™ pay / save Ã¢â€ â€™ report flow is functionally working but not communicating clear incremental steps. Added `UX-002` ... `UX-007` to track canonical journey design, state-driven status, and unified loading/report messaging while Phase 4 operator work is externally blocked. |
| 2026-03-26 | **UX-002 ... UX-006 Ã¢â‚¬â€ COMPLETE (implementation).** Results now present a single guided audit journey: preview first, full audit second, preview-save as the subtle alternative. Query-string-only checkout success messaging was removed in favor of results-page state derived from `hasPaidReport` / `reportStatus`, and the long-wait copy now frames preliminary and full audit generation as one continuous process. `npm.cmd run type-check` passed; `npm.cmd run build` passed outside sandbox after the usual `spawn EPERM` retry. |
| 2026-03-26 | **UX-007 Ã¢â‚¬â€ COMPLETE (verification).** Added a pure state-matrix test for the results journey (`lib/client/results-journey.test.ts`) covering preview-ready, checkout-cancelled, checkout-returned awaiting confirmation, generating, and delivered states. Verification notes recorded that both guest/public and signed-in results paths feed the same `hasPaidReport` / `reportStatus` shape into `ResultsView`, so the UX state model is shared across both flows. `npm.cmd run type-check` passed; targeted Vitest passed outside sandbox (1 file / 5 tests). |
| 2026-03-26 | **T3-7 Ã¢â‚¬â€ COMPLETE (implementation).** `components/report-viewer.tsx` now renders an interactive report shell above the markdown artifact: score/category summary, top-issue snapshot, mobile section chips, and collapsible report sections split from markdown `##` headings. Route ownership remains in `app/results/[id]/report/page.tsx`; existing PDF + raw markdown delivery path is preserved. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **DA-005 Ã¢â‚¬â€ COMPLETE (implementation).** Added an optional Browser Rendering-backed SPA fallback for paid deep audits: Browser Rendering client + heuristics, deep-audit crawl metrics for render attempts/success/failure, queue/run-config wiring, and security/operator docs. Disabled by default; this is not a full `/crawl` orchestration layer. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **Measurement platform initiative opened.** Strategic direction is now explicit: keep the audit/report product as the wedge and build the benchmark/measurement layer as a staged internal platform on top. Added `PLAYBOOK/measurement-platform-roadmap.md` and `BM-001` ... `BM-008` to the task registry so the work is documented before implementation starts. |
| 2026-03-26 | **BM-002 Ã¢â‚¬â€ COMPLETE (design).** Added `PLAYBOOK/measurement-schema-v1.md` defining the first benchmark schema set: canonical benchmark domains, query sets, benchmark run groups, raw query runs, extracted citations, and computed domain metrics. The spec freezes v1 identity rules, service-role-only access, first metric semantics, and clear out-of-scope items before any migration is written. |
| 2026-03-26 | **BM-003 Ã¢â‚¬â€ COMPLETE (design).** Added `PLAYBOOK/litellm-integration-plan.md`, which keeps the existing `LLMProvider` seam intact, introduces a staged LiteLLM-backed provider strategy, and defines provider-factory/config boundaries so audit/report flows remain stable while the future benchmark layer gains multi-model flexibility. |
| 2026-03-26 | **BM-004 Ã¢â‚¬â€ COMPLETE (design).** Added `PLAYBOOK/langfuse-integration-plan.md`, which scopes Langfuse to benchmark-layer traces, datasets, and experiments while keeping Supabase as the system of record and preserving Promptfoo/deterministic evals as primary foundations. |
| 2026-03-26 | **BM-005 Ã¢â‚¬â€ COMPLETE (design).** Added `PLAYBOOK/benchmark-runner-v1.md`, defining the first internal runner shape: one domain, one query set, one model lane, serial execution, conservative citation extraction, query-level failure tolerance, and structured logging around run-group lifecycle. |
| 2026-03-26 | **BM-006 Ã¢â‚¬â€ COMPLETE (design).** Added `PLAYBOOK/citation-and-metrics-v1.md`, which confirms no existing shared citation service conflicts with the new benchmark layer and defines the v1 citation classes, dedupe/confidence rules, and first metric formulas for `citation_rate`, `query_coverage`, and `share_of_voice`. |
| 2026-03-26 | **BM-007 Ã¢â‚¬â€ COMPLETE (design).** Added `PLAYBOOK/benchmark-admin-ui-v1.md`, which defines the first internal benchmark admin surfaces, keeps them aligned with the repoÃ¢â‚¬â„¢s existing server-rendered admin pattern, and limits run controls to narrow internal operations before any customer-facing benchmark UI exists. |
| 2026-03-27 | **Benchmark milestone Ã¢â‚¬â€ first live benchmark verified.** Real domain (`techehealthservices.com`) + admin-authored query set executed successfully on `gemini-2.5-flash-lite` with 6 completed queries, 4 citations, and non-zero benchmark metrics. The benchmark foundation is now operational; next step is transient-provider retry handling (`BM-024`) rather than more scaffolding. |
| 2026-03-27 | **BM-024 Ã¢â‚¬â€ COMPLETE (implementation).** Added light bounded retry/backoff for transient Gemini overload (`429` / `503`) in `lib/server/benchmark-execution.ts`. Retries are limited to temporary provider failures only, store retry metadata in `response_metadata`, and do not mask hard failures like invalid keys or unsupported models. |
| 2026-03-27 | **BM-025 Ã¢â‚¬â€ COMPLETE (design).** Added `PLAYBOOK/benchmark-grounding-v2.md` to define the next benchmark-methodology evolution: separate `ungrounded_inference` from `grounded_site` interpretation, treat business-type misclassification as a real internal signal, and keep sequencing anchored to `BM-024` retry/backoff before protocol expansion. |
| 2026-03-27 | **BM-026 Ã¢â‚¬â€ COMPLETE (implementation).** Added the first grounded benchmark foundation without disturbing the live ungrounded path: `runMode` is now a first-class runner/admin/CLI contract, benchmark-domain metadata can optionally provide `grounding_context.evidence` for `grounded_site` runs, the execution adapter now builds mode-aware prompts, and grounded runs fail truthfully when evidence is missing instead of silently drifting. Default behavior remains `ungrounded_inference`, so this does not claim grounded benchmark maturity yet. |
| 2026-03-27 | **BM-027 Ã¢â‚¬â€ COMPLETE (implementation).** Tightened the grounded-site prompt so completed answers should mention the exact target domain when the evidence supports the company, giving the conservative citation extractor a deterministic hook. Added explicit alias-based citation coverage in tests so names like `Teche Consulting` can still map back to the measured domain when configured. |
| 2026-03-27 | **BM-028 Ã¢â‚¬â€ COMPLETE (design).** Added the next explicit roadmap step after the current grounded-mode foundation: move from a loose grounded evidence blob to structured page-level provenance with `page_url`, `page_type`, and `excerpt`, so future grounded answers can be judged against exact source pages instead of domain-level evidence only. |
| 2026-03-27 | **BM-029 Ã¢â‚¬â€ COMPLETE (implementation).** Replaced the grounded-site prompt with the synthesized production version derived from external prompt research: stronger `do not use outside knowledge` scoping, explicit anti-inference behavior, explicit paraphrasing guidance, softer natural domain mention, and a cleaner plain-text answer instruction. This keeps the current grounded mode aligned with the benchmark methodology docs before the next live comparison run. |
| 2026-03-27 | **BM-030 Ã¢â‚¬â€ COMPLETE (implementation).** Grounded benchmark runs now normalize either legacy evidence blobs or structured provenance evidence into one internal shape, persist a safe `grounding_evidence` snapshot in run-group metadata, and expose that snapshot on the benchmark run detail page. This is the first inspectable page-provenance slice only; it does not claim a live grounding-context builder or exact-page citation-quality scoring yet. |
| 2026-03-27 | **BM-031 Ã¢â‚¬â€ COMPLETE (implementation).** Grounded benchmark runs now have a first minimal site-derived grounding builder: when manual evidence is absent, the runner fetches the homepage plus likely about/services pages through the existing validated fetch gate, derives bounded excerpts, and records whether grounding came from metadata or the site builder. This does not claim full crawl coverage, ranked page selection, or exact-page citation scoring yet. |
| 2026-03-28 | **BM-032 Ã¢â‚¬â€ COMPLETE (implementation).** Grounded benchmark citations now preserve the first exact-page provenance slice: each grounded evidence record carries a deterministic `evidence_id`, citation rows store matched grounding page fields when the parsed citation URL exactly matches a grounded evidence page URL, and unresolved domain-only mentions remain explicitly unresolved. The admin run-detail view now shows whether each citation matched a grounded source page. This is intentionally conservative and does not claim excerpt-level matching, best-page inference, or exact-page citation scoring beyond exact URL equivalence. |
| 2026-03-28 | **BM-033 Ã¢â‚¬â€ COMPLETE (design).** Froze the next grounded-provenance implementation order before more code is added: first improve bounded page selection and inspectable grounding metadata, then widen provenance matching, then add excerpt-level evidence checks, then define an exact-page citation-quality metric, and only after that expose grounded-vs-ungrounded comparison UI. The non-goals are now explicit: no large benchmark rewrite, no guessed provenance on weak evidence, and no customer-facing benchmark claims from this sequence. |
| 2026-03-28 | **BM-034 Ã¢â‚¬â€ COMPLETE (implementation).** Replaced the earlier homepage/about/services-only grounding candidate picker with a bounded ranked selector over same-origin links. The builder still stays narrow and heuristic, but it now prefers higher-signal pages like about, services, and product/platform pages while skipping low-signal sections like blog/legal/contact when stronger options exist. This does not claim full crawl coverage, semantic best-page selection, or citation-quality scoring yet. |
| 2026-03-28 | **BM-035 Ã¢â‚¬â€ COMPLETE (implementation).** Grounded evidence snapshots now preserve richer page metadata for later provenance work: `fetch_order`, `selection_reason`, explicit page title, and fetch status are carried through the grounding context, serialized into run metadata, parsed back in the run-detail helper, and shown in the existing grounding evidence cards without changing the route structure. This remains internal inspection metadata only, not a customer-facing score or a new schema surface. |
| 2026-03-28 | **BM-036 Ã¢â‚¬â€ COMPLETE (implementation).** Added a second conservative citation-to-grounding matcher: page URLs can now match grounded evidence by normalized page equivalence, not just raw exact-string equality. The matcher strips weak URL differences such as `www`, trailing slash, fragment, default ports, and tracking query params, but it still refuses to guess across different paths or domain-only mentions. This remains provenance support metadata, not a semantic evidence scorer. |
| 2026-03-28 | **BM-037 Ã¢â‚¬â€ COMPLETE (implementation).** Grounded citation rows now carry an internal claim-to-evidence overlap signal in metadata. For matched grounded citations, the runner selects the best claim sentence from the response, compares its token overlap against the matched evidence excerpt, and records a conservative status such as `supported_overlap`, `weak_overlap`, or `no_overlap`. This is internal inspection metadata only and does not create a customer-facing score or semantic fact-checking claim. |
| 2026-04-04 | **Startup dashboard + centralized entitlement stream opened.** Added implementation plan `docs/15-startup-dashboard-entitlements-plan.md`, strategy companion `PLAYBOOK/startup-dashboard-entitlements-v1.md`, and tracked tasks `SD-001` ... `SD-015` in the Task Registry. Sequencing freeze: start with `SD-001` (schema/control plane), keep runtime feature-flagged, and preserve Phase 4 launch-truth separation. |
| 2026-04-04 | **SD-001 Ã¢â‚¬â€ COMPLETE (implementation).** Added centralized entitlement foundation migration `021_service_entitlements_foundation.sql` (service catalog, bundles, bundle-service defaults, scope overrides, model policies, legacy agency-flag compatibility map) with seeded startup/agency bundles and initial service catalog. Added typed contract file + tests in `lib/server/service-entitlements-contract.ts` and `.test.ts`. |
| 2026-04-04 | **SD-002 Ã¢â‚¬â€ COMPLETE (implementation).** Added unified entitlement resolver in `lib/server/service-entitlements.ts` with deterministic precedence (`service_default` -> `bundle_service` -> `global` -> `bundle` -> `agency_account` -> `agency_client` -> `user`) plus tests. Integrated agency entitlement reads through this resolver in `lib/server/agency-access.ts` with legacy fallback for unmigrated environments, and added centralized-path + fallback coverage in tests. |
| 2026-04-04 | **SD-003 Ã¢â‚¬â€ COMPLETE (implementation).** Added centralized admin service control center at `/dashboard/services` with server actions for service defaults, bundle mappings, and scoped overrides (`global`, `bundle_default`, `agency_account`, `agency_client`, `user`). Added structured audit-log events for before/after control-plane changes and exposed the route in admin sidebar navigation. |
| 2026-04-04 | **SD-004 Ã¢â‚¬â€ COMPLETE (implementation).** Added startup workspace tenancy foundation migration `022_startup_workspace_foundation.sql` with founder/team membership roles and startup workspace linkage on scans/reports. Added admin bootstrap page `/dashboard/startups` with workspace/member provisioning actions, wired startup workspace selection into `/dashboard`, and added membership/context tests in `lib/server/startup-dashboard-data.test.ts`. |
| 2026-04-04 | **SD-005 Ã¢â‚¬â€ COMPLETE (implementation).** Added dedicated dark startup route shell at `/dashboard/startup` with module slots for score trend, action backlog, implementation lane, and PR activity. Added helper-driven trend/backlog derivations in `lib/server/startup-dashboard-shell.ts` with tests, and linked startup entry points from existing dashboard navigation. |
| 2026-04-04 | **SD-006 Ã¢â‚¬â€ COMPLETE (implementation).** Replaced startup dashboard placeholders with real actionable tracking modules by adding `lib/server/startup-tracking-metrics.ts` (burn-down, status funnel, 7/14/30 impact windows), wiring those metrics into `/dashboard/startup`, and adding metric-aggregation tests in `lib/server/startup-tracking-metrics.test.ts`. |
| 2026-04-04 | **SD-007 Ã¢â‚¬â€ COMPLETE (implementation).** Added normalized recommendation lifecycle schema in `023_startup_recommendation_lifecycle.sql` (`startup_recommendations`, `startup_recommendation_status_events`, startup-member RLS), implemented lifecycle helpers in `lib/server/startup-recommendation-lifecycle.ts` (transition validation, status summary, markdown-audit ingestion), and wired startup dashboard funnel metrics to recommendation statuses with updated tests. |
| 2026-04-04 | **SD-008 Ã¢â‚¬â€ COMPLETE (implementation).** Added implementation-plan schema in `024_startup_implementation_plan.sql` (`startup_implementation_plans`, `startup_implementation_plan_tasks` with team lanes and confidence/evidence fields), implemented markdown-audit parsing + plan-generation/query helpers in `lib/server/startup-implementation-plan.ts`, and wired `/dashboard/startup` implementation module to render lane cards and task rows from the latest generated plan. |
| 2026-04-04 | **SD-009 Ã¢â‚¬â€ COMPLETE (implementation).** Added GitHub integration foundation schema in `025_startup_github_integration_foundation.sql` (`startup_github_installations`, repo allowlist rows, callback install sessions), implemented helper/runtime logic in `lib/server/startup-github-integration.ts`, added startup connect/disconnect/allowlist actions + callback route, and gated startup GitHub controls through centralized `github_integration` entitlement checks. |
| 2026-04-04 | **SD-010 Ã¢â‚¬â€ COMPLETE (implementation).** Added PR workflow schema in `026_startup_agent_pr_workflow.sql` (`startup_agent_pr_runs`, `startup_agent_pr_run_events`), implemented queue + status transition helpers in `lib/server/startup-agent-pr-workflow.ts`, wired startup dashboard actions for queue/open/merge/fail flow, and synchronized PR statuses back to recommendation lifecycle states (`in_progress` -> `shipped` -> `validated`, `failed` on execution failure). |
| 2026-04-04 | **SD-011 Ã¢â‚¬â€ COMPLETE (implementation).** Added startup workspace scope for centralized model policies in `027_startup_model_policy_scope.sql`, implemented deterministic startup resolver in `lib/server/startup-model-policy.ts` (`service_default` -> `bundle` -> `startup_workspace`) with budget guardrails and fallback-model handling, and wired model-policy metadata into startup plan-generation and PR execution workflow records. |
| 2026-04-04 | **SD-012 Ã¢â‚¬â€ COMPLETE (implementation).** Added centralized Stripe billing mappings in `028_service_billing_mappings.sql`, implemented runtime billing guard checks in `lib/server/service-billing-guard.ts` (with tests), and wired startup GitHub connect + PR queue actions to enforce paid/free mode behavior from entitlement + mapping config only. Startup dashboard status messaging now includes explicit billing-blocked outcomes for these actions. |
| 2026-04-04 | **SD-013 Ã¢â‚¬â€ COMPLETE (implementation).** Centralized startup and agency dashboard UI gating into shared gate adapters: startup actions and startup dashboard now use `lib/server/startup-service-gates.ts` for entitlement + billing gate resolution, and agency dashboard route now uses centralized UI-gate mapping from `lib/server/agency-access.ts` instead of route-local entitlement boolean branching. Added focused tests for startup gate resolution and agency UI-gate mapping behavior. |
| 2026-04-04 | **SD-014 Ã¢â‚¬â€ COMPLETE (implementation).** Added centralized startup observability events across model policy resolution, recommendation lifecycle transitions, GitHub integration lifecycle, PR workflow transitions, implementation-plan generation, and service-gate blocking outcomes. Added a startup workspace timeline read model in `lib/server/startup-admin-data.ts` and surfaced that timeline in `/dashboard/startups` for per-workspace audit visibility, with focused timeline mapping tests. |
| 2026-04-04 | **SD-015 Ã¢â‚¬â€ COMPLETE (implementation).** Added centralized startup rollout guardrails with per-workspace flags (`startup_dashboard`, `github_agent`, `auto_pr`) in `lib/server/startup-rollout-flags.ts` and admin controls in `/dashboard/startups`. Startup dashboard/actions now enforce rollout-disabled states and suggest-only default behavior (`auto_pr=false`), with optional env-level overrides and focused rollout/timeline tests. |
| 2026-03-28 | **BM-038 Ã¢â‚¬â€ COMPLETE (design + implementation).** Added the first exact-page citation-quality metric slice for grounded runs. The metric counts completed runs where the measured-domain citation both matches a grounded page and has a `supported_overlap` claim/evidence signal, then records that as `exact_page_quality_rate` alongside matched/supported run counts. It is explicitly separate from citation presence and share-of-voice, and remains an internal benchmark-quality signal rather than a customer-facing score. |
| 2026-03-28 | **BM-039 Ã¢â‚¬â€ COMPLETE (implementation).** Added the first grounded-vs-ungrounded comparison view to the benchmark domain history page. The existing admin history surface now pairs the latest `grounded_site` and `ungrounded_inference` runs for the same query set and model, shows metric deltas and grounded exact-page quality, and keeps the change read-only and architecture-light by extending `benchmark-admin-data` instead of introducing a new route or benchmark subsystem. |
| 2026-03-28 | **BM-040 Ã¢â‚¬â€ COMPLETE (implementation).** Added the first recurring internal benchmark scheduling slice on the existing Cloudflare Worker cron path. Scheduled sweeps now use an env-configured query-set/model lane, sweep the current benchmark customer domains in a bounded daily window, stamp each run with deterministic schedule metadata and run scope, and skip duplicates by daily schedule key so benchmark history becomes comparable without a separate benchmark subsystem or customer-trigger path. |
| 2026-03-28 | **BM-041 Ã¢â‚¬â€ COMPLETE (design).** Added `PLAYBOOK/benchmark-competitor-cohort-methodology-v1.md` to freeze the first competitor/cohort benchmark frame rules before broader comparative UI exists. The methodology now makes the comparison frame explicit, constrains cohort size and role labeling, keeps claim language conservative, and requires underlying query lineage inspection before comparative outputs are trusted. |
| 2026-03-28 | **BM-042 Ã¢â‚¬â€ COMPLETE (implementation).** Added the first narrow competitor/cohort storage and admin slice: explicit `benchmark_cohorts` plus `benchmark_cohort_members` storage, and a read-only cohort-frame panel on the existing benchmark domain history page. The slice stays small on purpose: one stored query-set/model/run-mode frame per cohort, explicit measured-customer vs competitor roles, and links back to existing run-detail/domain-history inspection instead of a new benchmark subsystem. |
| 2026-03-28 | **BM-043 Ã¢â‚¬â€ COMPLETE (implementation).** Extended the existing benchmark run-detail page with a first lineage inspection slice: each query can now be read as prompt -> response -> extracted citations -> grounded evidence status / claim-overlap metadata. This reuses the current admin route and stored citation metadata instead of adding a new benchmark subsystem or widening persistence. |
| 2026-03-28 | **BM-044 Ã¢â‚¬â€ COMPLETE (implementation).** Extended the existing benchmark execution seam to support more than one live model lane on the same provider without widening the admin controls. `BENCHMARK_EXECUTION_ENABLED_MODELS` now acts as a narrow comma-separated allowlist, the first enabled model remains the default admin lane, and the Gemini adapter can execute any requested lane in that allowlist while still skipping unknown model ids truthfully. |
| 2026-03-28 | **BM-045 Ã¢â‚¬â€ COMPLETE (implementation).** Hardened the current benchmark schedule path for larger internal sweeps without splitting services: scheduled runs are now bounded by explicit per-sweep launch caps, repeated failures stop the sweep early, and run-level plus summary failure visibility is emitted on the existing structured log path. This keeps operator hardening on the current cron/schedule seam rather than introducing a second benchmark runtime. |
| 2026-03-28 | **BM-046 Ã¢â‚¬â€ COMPLETE (decision).** Froze the post-BM-045 operations decision for the 500 to 1000-site benchmark path. The repo truth is now explicit: do not split into a separate benchmark service yet; keep the current repo/runtime until real operating evidence shows customer-path risk, schedule brittleness, cost-boundary pressure, or observability failure. The 500 to 1000-site path remains planned, not implemented. |
| 2026-03-28 | **Benchmark backlog expansion.** Converted the remaining benchmark-methodology and scale gaps into explicit staged follow-up tasks `BM-033` ... `BM-046` so future work stays architecture-safe: ranked page selection, richer grounding metadata, provenance matching, excerpt-level evidence checks, citation-quality scoring, grounded-vs-ungrounded comparison UI, recurring runs, competitor/cohort methodology, lineage inspection, multi-model lanes, and scale hardening are now documented as separate slices rather than vague future work. |
| 2026-04-16 | **Benchmark domain-expansion planning opened.** Added the first startup benchmark planning artifacts for the 1000-site / 10-domain target without widening runtime scope: `PLAYBOOK/benchmark-tech-startups-b2b-software-methodology-v1.md` freezes `tech_startups` as a narrow `b2b_software` cohort, `eval/fixtures/benchmark-tech-startups-b2b-software-v1-query-set.json` freezes the first startup query set, and `BM-047` ... `BM-050` now track the next startup benchmark steps separately from the mature `law_firms` recurring lane. |
| 2026-04-16 | **Startup benchmark proof window - COMPLETE (implementation + operator evidence).** Seeded the first `tech_startups` `b2b_software` query set and 18-domain target cohort, added a small reusable domain-cohort seed seam, then ran the first paired startup proof window on `supabase.com` with `gemini-2.5-flash-lite`: ungrounded run group `837ba287-faae-48e0-a872-44a7c5c88b19` and grounded run group `470ae655-c32f-4cd3-ba22-3ebee6915a5b` both completed all 6 queries with no failures. Initial startup-lane truth: measured-domain citation rate improved from `0.833` to `1.0` under grounding, while citations remained domain-level rather than page-URL citations, so the lane looks viable for further narrow startup inspection but does not yet justify recurring startup scheduling or stronger provenance claims. |
| 2026-04-16 | **Startup benchmark multi-domain follow-up - COMPLETE (operator evidence).** Expanded the narrow startup proof from one domain to four representative `tech_startups` sites across different subverticals: `supabase.com`, `linear.app`, `vercel.com`, and `replicate.com`. All eight proof run groups completed all 6 queries with no failures or skips on `gemini-2.5-flash-lite`. Current startup-lane truth is mixed but useful: grounding improved measured-domain citation rate for `supabase.com`, `vercel.com`, and `replicate.com`, while `linear.app` dropped from `1.0` to `0.667`. Across all four domains, citations still remained domain-level rather than page-URL citations, so the next startup slice should inspect the `linear.app` drop and the startup prompt family before any recurring startup schedule is enabled. |
| 2026-04-16 | **Startup benchmark prompt/evidence correction - COMPLETE (implementation + operator evidence).** Inspected the failing `linear.app` proof pair and confirmed the issue was not the runner: the original startup `v1` query family asked category-wide questions that grounded company-site evidence could not answer honestly, and the grounding evidence itself was polluted by inline script/style text. Added the tightened startup `v2` query-set fixture (`eval/fixtures/benchmark-tech-startups-b2b-software-v2-query-set.json`), cleaned `buildTextSample()` to strip script/style/noscript/template/SVG noise (`workers/scan-engine/parse-signals.ts` plus focused tests), seeded the `v2` query set, and reran grounded `linear.app` (`1170591b-b32f-4727-af0b-20b56f91f0be`) against the earlier grounded `v2` baseline (`54645584-8599-48ee-b3da-0d41e1859497`). Current truth: measured-domain citation stayed at `1.0`, but grounded answer usefulness improved materially because the evidence now surfaces real product/about/customer text instead of CSS/JS noise. The next startup step should therefore use the tighter `v2` query family and cleaner evidence path before considering any recurring startup schedule. |
| 2026-04-16 | **Startup benchmark recurring-pilot freeze - COMPLETE (planning/docs).** Froze the next startup benchmark step as a tiny recurring pilot rather than another methodology rewrite. The repo now has an explicit startup pilot runbook (`PLAYBOOK/benchmark-tech-startups-collection-pilot-v1.md`) and a dedicated four-domain pilot fixture (`eval/fixtures/benchmark-tech-startups-b2b-software-v2-pilot-domains.json`) that enables recurring schedule selection only for the `v2`-proofed startup domains: `linear.app`, `supabase.com`, `vercel.com`, and `replicate.com`. Current truth: the startup lane looks stable enough for a very small recurring pilot on the existing schedule path, but it still does not justify broader startup scheduling or page-level provenance claims. |
| 2026-04-16 | **Startup benchmark recurring pilot window - COMPLETE (operator evidence).** Seeded the dedicated four-domain startup pilot fixture into `benchmark_domains`, then previewed and ran the first explicit startup scheduled window on the existing schedule path with startup-only env overrides and the explicit domain allowlist `linear.app,supabase.com,vercel.com,replicate.com`. Window `2026-04-17T00` launched all `8` runs cleanly with `0` failures: `linear.app` ungrounded `c5e287ce-30a4-4205-b614-1974449c0381` / grounded `2d4241b6-636c-4f67-b356-62a7eb91204f`, `vercel.com` ungrounded `8fa220db-c5ad-4572-9312-a232d5badf3e` / grounded `d8b75466-ef44-4852-94f4-804cdc412565`, `supabase.com` ungrounded `d7c18256-e53d-444a-9f9b-b6c9de98225b` / grounded `2b39d469-35d4-4f5c-afc4-d236abd1c53a`, and `replicate.com` ungrounded `95b44f5a-dec1-42ec-bc65-3dbef19477d8` / grounded `32f9c258-ec67-448d-9aac-e5aae3f65c31`. Current truth: the tiny startup recurring pilot is operationally viable on the existing scheduler path, but its evidence is still domain-level (`100%` citation rate, `0%` exact-page quality) and should collect more comparable windows before any widening. |
| 2026-04-16 | **Startup benchmark three-window pilot review - COMPLETE (operator evidence).** Ran two more comparable startup pilot windows on the exact same four-domain frame: `2026-04-17T12` and `2026-04-18T00`. All `16` additional runs completed cleanly with `0` failures. Multi-window truth is now more precise: `linear.app`, `supabase.com`, and `vercel.com` stayed at `100%` grounded and `100%` ungrounded citation rate across all three windows, while `replicate.com` dipped once to `83%` grounded citation rate on `2026-04-17T12` (`bb5a1e80-a5d5-4f33-a9ed-bfe275811325`) before returning to `100%` on `2026-04-18T00` (`2b4457b9-48a3-4695-a229-615c5acaabbd`). The recurrence helper returned no recurring winners or laggards across `2026-04-17T00`, `2026-04-17T12`, and `2026-04-18T00`. Current truth: the startup pilot is operationally stable, but it is not yet a clear grounded-wins lane, so the next step should inspect the one-off `replicate.com` grounded dip before widening the startup cohort. |
| 2026-04-17 | **Startup benchmark post-pilot follow-up queued.** Registered `BM-056` ... `BM-058` so the post-pilot sequence stays explicit after the `replicate.com` regression review: first freeze the widen-or-hold decision after `BM-055`, then seed one next bounded startup cohort only if widening is approved, then run one widened proof window before any broader recurring startup expansion. Current truth: no broader startup widening is approved yet; the four-domain pilot remains the only recurring startup frame until `BM-055` and the follow-on decision slice are complete. |
| 2026-04-17 | **BM-055 - COMPLETE (operator evidence review).** Reviewed the one-off grounded `replicate.com` dip on `2026-04-17T12` directly against the adjacent clean grounded pilot windows. The regression was isolated to one query, `product-differentiators`, which returned a normal grounded answer but no measured-domain citation on run `bb5a1e80-a5d5-4f33-a9ed-bfe275811325`; the adjacent grounded runs `32f9c258-ec67-448d-9aac-e5aae3f65c31` and `2b4457b9-48a3-4695-a229-615c5acaabbd` both cited `replicate.com` on all six queries. Current truth: this looks like one-off model citation omission within the existing domain-level startup lane, not a scheduler failure, grounding-evidence parse failure, or provenance-matcher regression. The widen-or-hold decision remains a separate `BM-056` slice. |
| 2026-04-17 | **BM-056 - COMPLETE (decision freeze).** Froze the post-`BM-055` startup widening decision as **hold at the current four-domain pilot**. Current truth: the startup pilot is operationally clean and the inspected `replicate.com` dip is not a systems regression, but the lane still shows neutral recurrence, domain-level citation behavior, and no stable grounded advantage that would justify widening the cohort now. `BM-057` and `BM-058` remain conditional follow-on tasks only if future evidence reopens startup expansion. |
| 2026-03-27 | **Architecture hardening stream opened.** Added an explicit architecture-hardening backlog covering lean-code discipline, persistent structured logs, admin log visibility, and Playwright readiness. Implemented the first observability slice: structured logs now persist best-effort to `app_logs`, `/dashboard/logs` shows recent internal logs for admins, and Cloudflare scheduled-task failures now flow through the shared structured logging path. |
| 2026-03-27 | **AH-005 Ã¢â‚¬â€ COMPLETE (implementation).** Added a small shared admin runtime helper for repeated admin auth, env loading, and service-role client setup, then moved the main admin pages plus benchmark server actions onto it. This reduces route-level duplication without introducing a broad request-context framework. |
| 2026-03-27 | **AH-006 Ã¢â‚¬â€ COMPLETE (implementation).** Reduced `app/dashboard/benchmarks/[runGroupId]/page.tsx` to a data-loading shell, moved the run-detail rendering into `components/benchmark-run-detail-view.tsx`, and verified the extracted parsing/formatting helpers in `lib/server/benchmark-run-detail.ts`. This is the first narrow large-module decomposition slice only; it does not claim broader benchmark UI or worker refactors yet. |
| 2026-03-27 | **AH-008 Ã¢â‚¬â€ COMPLETE (implementation).** Added the first open-source Playwright foundation: `@playwright/test`, a repo-local config, Chromium smoke specs for the landing page, missing-Turnstile scan-submit guard, a mocked free-scan happy path through submit/redirect/results render, results-page checkout-return messaging before payment confirmation, both sign-in entry points, unauthenticated benchmark-page redirect behavior, the first authenticated admin dashboard case via a non-production-only E2E session seam, and the first authenticated benchmark overview case via a non-production-only E2E admin-data seam, plus output ignores and CI wiring. This is still a narrow browser-smoke lane only; it does not yet cover benchmark actions after login or full live checkout execution. |
| 2026-03-27 | **AH-009 Ã¢â‚¬â€ COMPLETE (implementation).** Reduced `components/report-viewer.tsx` to a fetch/state shell, moved report-viewer helper logic into `lib/client/report-viewer.ts`, moved heavy section rendering into `components/report-viewer-sections.tsx`, and added both targeted helper tests and report-page browser proof. This is a narrow decomposition slice only; it does not claim broader report pipeline or worker refactors. |


| 2026-03-27 | **AH-010 Ã¢â‚¬â€ COMPLETE (implementation).** Reduced `app/dashboard/benchmarks/page.tsx` to a thin admin/data-loading shell, moved benchmark overview rendering into `components/benchmark-overview-view.tsx`, moved pure overview helpers into `lib/server/benchmark-overview.ts`, and kept the existing authenticated benchmark overview Playwright case as the browser proof for this route. The Playwright lane was also tightened to a serialized one-worker configuration because that is the stable dev-server setting for this repo. |

| 2026-03-27 | **AH-011 ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Â COMPLETE (implementation).** Extracted shared deep-audit report preparation helpers into `workers/report/deep-audit-report-helpers.ts`, updated both the PDF builder and markdown exporter to reuse the shared narrative/parsing seam where safe, and added targeted helper tests. This is a worker-only decomposition slice; it does not change customer routes or require a new UI smoke test. |

| AH-010 | Add the third large-module decomposition slice by reducing the benchmark overview route to a thin shell and moving overview rendering/helpers into dedicated modules with existing browser proof retained | Backend + Frontend | Ã¢Å“â€¦ DONE | `app/dashboard/benchmarks/page.tsx`, `components/benchmark-overview-view.tsx`, `lib/server/benchmark-overview.ts`; COMPLETION_LOG AH-010 |
| AH-011 | Add the fourth large-module decomposition slice by extracting shared deep-audit report preparation helpers out of the PDF builder without changing report behavior or adding UI churn | Backend | Ã¢Å“â€¦ DONE | `workers/report/deep-audit-report-helpers.ts`, `workers/report/build-deep-audit-pdf.ts`, `workers/report/build-deep-audit-markdown.ts`; COMPLETION_LOG AH-011 |

| 2026-03-27 | **AH-012 Ã¢â‚¬â€ COMPLETE (implementation).** Extracted crawl-pending parsing, config merge, browser-render stat merge, and chunk-window planning into `workers/scan-engine/deep-audit-crawl-state.ts`, then updated `workers/scan-engine/deep-audit-crawl.ts` to consume the shared state helpers. This is a worker-only decomposition slice; it does not change customer routes or require a new UI smoke test. |
| 2026-03-27 | **AH-013 Ã¢â‚¬â€ COMPLETE (implementation).** Extracted shared email-delivery composition helpers into `workers/report/resend-delivery-helpers.ts`, updated `workers/report/resend-delivery.ts` to reuse the shared CTA/formatting/encoding seam while preserving its existing type exports for callers, and added targeted helper tests. This is a worker-only decomposition slice; it does not change customer routes or require a new UI smoke test. |
| 2026-03-28 | **AH-007 Ã¢â‚¬â€ COMPLETE (design).** Reviewed the current founder-stage admin auth path in `lib/server/require-admin.ts` and `app/admin/login/actions.ts`, then documented the next-stage maturity plan in `PLAYBOOK/architecture-hardening-backlog.md`: keep the current env-based single-admin-email gate for internal use, but move to a fail-closed table-driven admin allowlist and small explicit role model before broader operator scale. No runtime auth behavior changed in this slice. |

| 2026-03-28 | **UX-008 Ã¢â‚¬â€ COMPLETE (implementation + verification).** Tightened the customer-facing results-to-report action layer after founder review: the score snapshot card now has a real share action with native-share or copy-link fallback plus OG-image preview, delivered-report status only promises direct access when hosted artifacts actually exist, the report viewer falls back cleanly to PDF download when no web report is available, and results-page load states now distinguish missing / expired / private scans more clearly. Verification: `npm.cmd run type-check`, targeted Vitest for `lib/client/results-journey.test.ts`, and targeted Playwright smoke for checkout-return messaging, share snapshot, interactive report render, and PDF-only fallback all passed outside sandbox. |
| 2026-03-29 | **Paid-path traceability slice Ã¢â‚¬â€ COMPLETE (implementation).** Added the missing runtime breadcrumbs on the post-payment path so live founder testing can distinguish webhook success from queue/report-worker failure. Stripe webhook processing now logs a structured success/failure event, queue enqueue now logs both success and failure with scan/run ids, and the report worker now logs start, email-sent, and completed milestones in addition to existing failure logs. Verification: `npm.cmd run type-check` passed and targeted Vitest for `app/api/webhooks/stripe/route.test.ts` plus `lib/server/stripe/checkout-completed.test.ts` passed outside sandbox. |
| 2026-03-29 | **Paid-report recovery slice Ã¢â‚¬â€ COMPLETE (implementation + verification).** Tightened the successful paid-path recovery story: delivered results now explain how to save the report into the dashboard, login explicitly tells the user to use the Stripe checkout email first, dashboard empty state now explains why a paid report may not appear yet, and report-delivery emails now remind the user of the same recovery rule. Verification: `npm.cmd run type-check` passed and targeted Playwright smoke for delivered-results recovery, customer login, and dashboard entry all passed outside sandbox. |
| 2026-03-29 | **Results chronology slice Ã¢â‚¬â€ COMPLETE (implementation + verification).** Added one explicit top-of-page action band to the customer results route so the next step is obvious in each real state: preview users get direct buy/save actions, generating users get a progress/recovery panel, and delivered users get open/download/dashboard recovery actions without hunting below the score section. Verification: `npm.cmd run type-check` passed and targeted Playwright smoke for checkout-return, generating, and delivered states all passed outside sandbox. |

---

## Known Constraints (do not debate these)
- No Vercel (commercial use prohibited on hobby plan)
- No Make.com (15x over free limit)
- No Puppeteer in Workers (V8 isolate, not Node.js)
- No `@cloudflare/next-on-pages` (deprecated)
- Gemini free tier: 1,000 RPD Ã¢â‚¬â€ architect around this
- Workers free tier CPU: 10ms Ã¢â‚¬â€ PDF gen requires paid $5/mo plan
- Supabase 500MB + 2GB bandwidth Ã¢â‚¬â€ monitor, Pro at $25/mo when needed

| 2026-03-29 | **BM collection start slice - COMPLETE (implementation + design).** Added the first narrow recurring benchmark collection path for category-based seed data. `benchmark_seed.csv` can now be imported into `benchmark_domains` through `scripts/benchmark-seed-domains.ts` as explicit schedule-enabled targets, the recurring scheduler can filter by one benchmark vertical plus selected seed priorities, and schedule idempotency now supports 12-hour windows so a twice-daily lane does not self-dedupe. The first operating lane is frozen in `PLAYBOOK/benchmark-collection-operations-v1.md`: `law_firms` priority `1`, one fixed query set version, one fixed model lane, and both `ungrounded_inference` plus `grounded_site`. |

| 2026-03-29 | **Law-firms query-set seed slice - COMPLETE (implementation + design).** Added a repeatable seed path for the first frozen law_firms recurring collection query set. scripts/benchmark-seed-query-set.ts now seeds eval/fixtures/benchmark-law-firms-p1-query-set.json through the existing repository seam, so the first collection lane no longer depends on manual admin query entry. The frozen first query-set identity is law-firms-p1-core 1 with 8 queries. |

| 2026-03-29 | **Benchmark schedule preview slice - COMPLETE (implementation + verification).** Added a dry-run preview path for the recurring benchmark lane. scripts/benchmark-schedule-preview.ts now reads the current BENCHMARK_SCHEDULE_* env, reuses the existing scheduler selection rules, and prints the exact query set, model lane, schedule window, and selected domains before cron is enabled. |

| 2026-03-29 | **Benchmark schedule run-now slice - COMPLETE (implementation + verification).** Added scripts/benchmark-schedule-run-now.ts and 
pm run benchmark:schedule:run-now so the recurring benchmark lane can be executed immediately through the same scheduler path as cron. This keeps the proof step architecture-light: same env, same selection rules, same run metadata, no separate benchmark orchestration path. |

| 2026-03-29 | **Benchmark scheduled-window summary slice - COMPLETE (implementation + verification).** Added a read-only terminal summary for the current scheduled benchmark frame. scripts/benchmark-schedule-summary.ts now uses the existing run-group records plus stored metrics to pair grounded and ungrounded runs per domain and print the current window review without adding a new admin route. |

| 2026-03-29 | **Benchmark outlier-selection slice - COMPLETE (implementation + verification).** Added scripts/benchmark-schedule-outliers.ts so the current scheduled window can be ranked by grounded-minus-ungrounded citation-rate delta. This keeps the next methodology step focused and small: inspect the strongest grounded winners and losers in the existing lineage view before touching scoring or scale. |

| 2026-03-29 | **Benchmark run-diagnostic slice - COMPLETE (implementation + verification).** Added scripts/benchmark-run-diagnostic.ts so selected outlier run-group ids can be summarized from existing stored lineage data before manual inspection. The diagnostic now reports URL-vs-domain citation depth, provenance matches, overlap support, and a first probable-issue label without adding a new benchmark subsystem. |

| 2026-03-29 | **First live benchmark window interpretation slice - COMPLETE (docs).** Froze the current operator meaning of the first law_firms-p1-v1 live window on gemini-2.5-flash-lite: grounded citation-rate deltas are usable internal directional signal, but exact-page quality should not yet be treated as a gating metric for this lane because targeted outlier diagnostics showed mostly domain-level grounded attribution rather than page-level URLs. |

| 2026-03-29 | **Two-window benchmark decision freeze - COMPLETE (docs).** After two comparable law_firms-p1-v1 windows on gemini-2.5-flash-lite, the lane is now explicitly treated as a domain-level grounded attribution lane: grounded citation-rate deltas are usable internal signal, exact_page_quality_rate stays visible but non-gating, and the repo should not prioritize provenance-matcher work or scale-up for this lane without new evidence. |
| 2026-03-29 | **Law-firms fit analysis freeze - COMPLETE (analysis/docs).** Completed a manual fit read of the current 21-domain `law_firms` priority-1 lane and froze it in `PLAYBOOK/law-firms-p1-fit-analysis-v1.md`. Current truth: the broad lane is a mixed cohort, not one coherent law-firm frame. It combines enterprise/business-counsel firms with immigration, divorce, PI, and employment specialists while the frozen query set mixes multiple legal-service intents. The main signal-quality issue is now clearer: many current domain/query pairs are low-fit by design, so future redesign work should treat cohort split and query-set redesign as coupled rather than reaching first for matcher rewrites or more infrastructure. |
| 2026-03-29 | **Law-firms replacement-target freeze - COMPLETE (analysis/docs).** Froze the first narrow replacement target for the `law_firms` benchmark redesign: `business_counsel / biglaw / enterprise`. Current truth: the next precision lane should start from the largest coherent subgroup already present in the live 21-domain slice, and the next query-set rewrite should serve that subgroup only rather than trying to repair the whole mixed law-firms lane at once. |
| 2026-03-29 | **Business-counsel narrow query-set draft freeze - COMPLETE (analysis/docs).** Added the first frozen replacement query-set draft for the `business_counsel / biglaw / enterprise` subgroup at `eval/fixtures/benchmark-law-firms-business-counsel-v1-query-set.json`. Current truth: the replacement frame now has a concrete draft built around enterprise/business-law buying intent, but it remains a draft methodology artifact only until the exact target-domain list is frozen and the team explicitly decides to seed a narrow live lane. |
| 2026-03-29 | **Business-counsel target-domain freeze - COMPLETE (analysis/docs).** Froze the exact target-domain list for the first narrow replacement lane as `law_firms_business_counsel_v1`: 17 domains taken from the current priority-1 live lane, excluding the consumer/specialist outliers (`forthepeople.com`, `cordellcordell.com`, `fragomen.com`, `seyfarth.com`). Current truth: the next narrow live experiment can now be launched as a separate comparable frame without redefining the target subgroup on the fly. |
| 2026-03-29 | **Business-counsel query-set seed path - COMPLETE (implementation/docs).** Added an explicit package command for seeding the first narrow replacement query-set draft: `npm run benchmark:seed:query-set:business-counsel`. Current truth: the draft query-set can now be created as a separate DB record without touching the broad live lane or its schedule configuration. |
| 2026-03-29 | **Business-counsel query-set seeded - COMPLETE (operator evidence/docs).** Seeded the first narrow replacement query-set draft successfully. Current draft query-set record: `9910b5ac-ade6-42be-9dca-9b85c04e4469` with 6 queries. This record now exists independently of the broad live lane and can be used for preview/config work without changing the current recurring `law_firms-p1-v1` schedule. |
| 2026-03-29 | **Benchmark schedule domain-allowlist slice - COMPLETE (implementation/docs).** Extended the existing schedule filter seam with an optional `BENCHMARK_SCHEDULE_DOMAINS` canonical-domain allowlist. Current truth: the team can now preview and later launch a narrow business-counsel lane against the frozen 17-domain cohort without mutating the current broad 21-domain live lane. |
| 2026-03-29 | **First business-counsel narrow-lane result freeze - COMPLETE (operator evidence/docs).** The first live `law-firms-business-counsel-v1` window completed cleanly across all 17 paired domains on `gemini-2.5-flash-lite`. Current truth: narrowing the cohort/query frame improved signal quality versus the broad mixed lane while `exact_page_quality_rate` remained `0%` and non-gating. The business-counsel lane is now the cleaner internal law-firms signal lane, and the next move should be repeated comparable windows rather than immediate prompt/model/evidence-depth changes. |
| 2026-03-29 | **Primary law-firms lens freeze - COMPLETE (operator evidence/docs).** After three comparable `law-firms-business-counsel-v1` windows, the narrow business-counsel lane is now the primary internal law-firms benchmark frame. Current truth: the broad `law-firms-p1-v1` lane remains useful only as a secondary legacy comparison frame, while continued law-firms methodology decisions should use the narrow lane first. |
| 2026-03-29 | **Schedule run-now window override - COMPLETE (implementation/docs).** Added `--window-date YYYY-MM-DDTHH` support to `npm run benchmark:schedule:run-now`. Current truth: the team can now create the next controlled benchmark window on demand without waiting for the UTC slot to advance, while still using the same scheduler path and run metadata shape as cron. |
| 2026-03-29 | **Benchmark recurrence review slice - COMPLETE (implementation/docs).** Added a small terminal-only recurrence helper for the current scheduled benchmark frame. `scripts/benchmark-schedule-recurrence.ts` and `buildBenchmarkScheduleMultiWindowSummary(...)` now summarize recurring winners and laggards across an explicit chosen set of comparable windows using the existing run-group records, without adding a new admin route or persistent aggregation layer. |
| 2026-03-30 | **Layer One report rewriter contract freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-report-rewriter-contract-v1.md` to freeze the required section order and role of each section for future Layer One report rewrites. Current truth: report-quality hardening should start by separating confirmed findings, likely implications, priority actions, optional advanced GEO ideas, and open questions before changing prompts or retrieval inputs. |
| 2026-03-30 | **Layer One evidence-discipline freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-report-evidence-discipline-v1.md` to freeze the claim-boundary rules for future Layer One report rewrites. Current truth: rewritten reports should not invent market statistics, hard-diagnose weak signals, or collapse optional GEO strategy into audit-backed remediation before the actual prompt/runtime slice is updated. |
| 2026-03-30 | **Layer One tone and verbosity freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-report-tone-and-verbosity-v1.md` to freeze the default presentation rules for future Layer One report rewrites. Current truth: the rewrite layer should use plain operational language, open with site-specific findings rather than industry framing, and compress repeated narrative before the prompt/runtime slice is updated. |
| 2026-03-30 | **Layer One recommendation-format freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-report-recommendation-format-v1.md` to freeze the recommendation shape for future Layer One report rewrites. Current truth: the `Priority actions` section should resolve into explicit issue, why-it-matters, action, priority, and confidence fields before the prompt/runtime slice is updated. |
| 2026-03-30 | **Layer One ambiguous-signal wording freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-report-ambiguous-signal-wording-v1.md` to freeze how future Layer One report rewrites should describe real-but-ambiguous findings. Current truth: uncertain signals like `402/403`, low-confidence extraction, partial schema, stale freshness, and mixed page outcomes should use observed signal, bounded implication, and verification-step wording before the prompt/runtime slice is updated. |
| 2026-03-30 | **Layer One rewrite-prompt implementation slice - COMPLETE (implementation/docs).** Added `lib/server/layer-one-report-rewrite-prompt.ts` plus `scripts/layer-one-report-rewrite-prompt.ts` so an existing markdown report can now be turned into the constrained rewrite prompt text for another LLM. Current truth: report hardening now has a real local execution seam, but the shipped customer report generation path remains deterministic. |
| 2026-03-30 | **Layer One real-world rewrite fixture slice - COMPLETE (operator workflow/docs).** Added `docs/review-inputs/reports/cllc-gemini-report.md` as the first real-world external rewrite example for the constrained Layer One prompt seam. Current truth: the rewrite workflow now has one concrete bad-output fixture to test against, but there is still no dedicated UI for this path. |
| 2026-03-30 | **Layer One gold rewrite fixture slice - COMPLETE (fixture/docs).** Added `eval/fixtures/cllc-layer-one-rewrite-gold.md` as the first gold-standard rewritten Layer One report fixture. Current truth: prompt tuning can now compare candidate rewrites against one concrete target output for a real site instead of relying only on high-level writing rules. |
| 2026-03-30 | **Automatic Layer One report eval writing - COMPLETE (implementation/docs).** Added `lib/server/report-eval-writer.ts` and wired the deep-audit report queue consumer to write a deterministic `report_eval_runs` row automatically after report creation under framework `layer_one_report`. Current truth: Layer One report quality can now be tracked in the existing admin eval analytics over time without introducing Promptfoo or RAGAS into the paid report runtime. |
| 2026-03-30 | **Deep-audit markdown delivery fix - COMPLETE (implementation/docs).** Added `app/api/scans/[id]/report-markdown/route.ts` and switched the report viewer to load markdown through the app instead of browser-fetching the raw storage URL. Current truth: delivered reports now have a same-origin markdown render path plus a direct markdown download endpoint, which avoids public-R2/CORS fragility in the `View report` flow. |
| 2026-03-30 | **Layer One internal rewritten-artifact contract freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-internal-rewrite-artifact-v1.md` to freeze the next paid-report improvement boundary. Current truth: any rewritten Layer One report should begin as a second internal artifact stored and evaluated alongside the deterministic markdown first, not as an immediate replacement for the customer-facing paid report. |
| 2026-03-30 | **Layer One internal rewritten-artifact implementation - COMPLETE (implementation/docs).** Added `lib/server/layer-one-report-internal-rewrite.ts`, optional `DEEP_AUDIT_INTERNAL_REWRITE_*` env gates, separate rewritten-markdown R2 storage, and a second best-effort `layer_one_report` eval write in `workers/queue/report-queue-consumer.ts`. Current truth: deep-audit generation can now create an internal rewritten markdown artifact and rewritten eval row without changing the default paid report output. |
| 2026-03-30 | **Layer One report internal comparison access - COMPLETE (implementation/docs).** Added `app/dashboard/evals/report/[id]/page.tsx` and linked `layer_one_report` rows from the existing eval analytics table. Current truth: admins can now open one report-eval run, inspect metadata, and compare deterministic vs rewritten sibling report evals side-by-side for the same `scan_id` before any paid-report default is changed. |
| 2026-03-30 | **Layer One operator judgment seam - COMPLETE (implementation/docs).** Added `app/dashboard/evals/actions.ts` and extended `app/dashboard/evals/report/[id]/page.tsx` with `better / worse / unclear` buttons for rewritten report variants. Current truth: rewritten-report review now has a lightweight persistent operator judgment loop stored in `report_eval_runs.metadata`, so repeated internal comparisons can accumulate evidence before any paid default switch. |
| 2026-03-30 | **Report Design teamOwner propagation slice - COMPLETE (implementation/docs).** Added `teamOwner` to normalized `IssueRow` parsing in `workers/report/deep-audit-report-helpers.ts`, backed by the standalone RD-007 map, and switched the markdown builder to use the shared parser. Current truth: report issue rows now carry owner metadata for later Team Action Map / Immediate Wins work, while customer-facing report structure and content remain unchanged. |
| 2026-03-30 | **Report Design canonical payload teamOwner propagation - COMPLETE (implementation/docs).** Updated `workers/report/deep-audit-report-payload.ts` so the canonical deep-audit payload now preserves normalized issue rows, including `teamOwner`, across highlighted issues, all issues, and page-level issue arrays. Current truth: owner metadata now survives through the canonical report payload, but no owner-aware report sections are rendered yet. |
| 2026-03-30 | **Report Design internal Immediate Wins payload seam - COMPLETE (implementation/docs).** Added `workers/report/immediate-wins.ts` and extended the canonical deep-audit payload with an internal-only `immediateWins` block derived from owner-aware issues. Current truth: the report pipeline now derives prefiltered ticket candidates internally, which now also feed the first rendered markdown `Immediate Wins` section. |
| 2026-03-30 | **Report Design deterministic Immediate Wins markdown slice - COMPLETE (implementation/docs).** Updated `workers/report/build-deep-audit-markdown.ts` so the paid markdown report now renders a first deterministic `Immediate Wins` section from the owner-aware payload seam. Current truth: the report now exposes one owner-aware ticket-style section without changing the broader report order, PDF layout, or web report viewer structure yet. |
| 2026-03-30 | **Report Design per-page markdown cleanup slice - COMPLETE (implementation/docs).** Updated `workers/report/build-deep-audit-markdown.ts` so passed checks in the per-page checklist no longer print `Fix:` lines. Current truth: the markdown report now avoids one obvious source of contradictory/noisy copy while leaving findings, scoring, and overall structure unchanged. |
| 2026-03-30 | **Report Design bounded low-confidence wording slice - COMPLETE (implementation/docs).** Added shared customer-facing finding formatting in `workers/report/deep-audit-report-helpers.ts` and reused it in both markdown and PDF report rendering. Current truth: raw low-confidence transport tokens like `http_403` no longer leak directly into customer-facing report copy, while the underlying audit data remains unchanged. |
| 2026-03-30 | **Report Design metadata-guidance cleanup slice - COMPLETE (implementation/docs).** Corrected broken title-length guidance at the audit-check source in `workers/scan-engine/checks/check-title.ts` and added a targeted regression test. Current truth: customer-facing reports now inherit a readable `10-70` title range instead of corrupted `1070`-style output. |
| 2026-03-30 | **Report Design per-page markdown compression slice - COMPLETE (implementation/docs).** Updated `workers/report/build-deep-audit-markdown.ts` so the per-page checklist now shows only non-passing rows. Current truth: the markdown report is materially less repetitive, while findings, scoring, and the rest of the report structure remain unchanged. |
| 2026-03-30 | **Production domain cutover prep slice - COMPLETE (config/docs).** Updated `wrangler.jsonc` so production `NEXT_PUBLIC_APP_URL` now targets `https://getgeopulse.com/`, and added `PLAYBOOK/getgeopulse-domain-cutover-v1.md` to freeze the operator cutover checklist. Current truth: repo config is now aligned to the new domain, but DNS, Turnstile, Supabase Auth, Stripe, Resend, and WAF closure still require operator actions. |






| 2026-04-04 | **Startup Slack MVP stream opened (SL-001 complete).** Added frozen MVP contract + byte-sized plan in docs/17-startup-slack-integration-mvp-plan.md, plus strategy companion PLAYBOOK/startup-slack-integration-mvp-v1.md, and registered SL-001 ... SL-012 in the task registry. Sequencing freeze: keep Slack MVP minimal, manual-send first, and centralized behind existing entitlement + rollout controls. |
| 2026-04-04 | **SL-002 â€” COMPLETE (implementation).** Added centralized Slack service-key support by extending the runtime service contract (slack_integration, slack_notifications) and adding migration 29_startup_slack_service_keys.sql for catalog seeding. Added startup rollout flags (slack_agent, slack_auto_post) across metadata/env resolver, admin rollout controls, startup timeline summaries, and focused tests (service-entitlements-contract, startup-rollout-flags, startup-admin-data) with npm run type-check passing. |

| 2026-04-04 | **SL-003 â€” COMPLETE (implementation).** Added minimal Slack schema foundation in supabase/migrations/030_startup_slack_integration_foundation.sql with startup workspace installations, channel destinations, and delivery-event logging tables plus startup-member RLS policies and bounded status/event constraints. |
| 2026-04-04 | **SL-004 â€” COMPLETE (implementation).** Added Slack OAuth foundation with callback-session table (031_startup_slack_install_sessions.sql), startup Slack integration helpers (session create/consume, installation upsert/disconnect), `/api/startup/slack/callback` route, startup dashboard connect/disconnect actions, and a minimal Slack connection card/status flow on `/dashboard/startup`. Also extended startup UI gate aggregation to include Slack service keys and added focused tests for Slack integration + updated gate coverage. |
| 2026-04-04 | **SL-005 â€” COMPLETE (implementation).** Added startup Slack destination setup flow with list + add UI on `/dashboard/startup`, plus backend destination upsert/list helpers and server action wiring (`saveStartupSlackDestination`) behind startup rollout and centralized Slack entitlement gates. Added focused destination helper coverage in `startup-slack-integration.test.ts`; type-check + targeted tests passing. |
| 2026-04-04 | **SL-006 â€” COMPLETE (implementation).** Added manual Slack send action from startup report context (`sendStartupReportToSlack`) with rollout + entitlement gate enforcement (`slack_integration` + `slack_notifications`), destination/report validation, and minimal Slack `chat.postMessage` dispatch via connected installation token metadata. Added report-context Slack send form to `/dashboard/startup` and expanded Slack status messaging plus helper coverage (`sendStartupSlackMessage`) in tests. |
| 2026-04-04 | **SL-007 â€” COMPLETE (implementation).** Added normalized Slack message formatter contract module (`startup-slack-message.ts`) with explicit payload shape and deterministic formatting (header, score+delta, top bullets, links), plus formatter tests. Manual send action now builds contract payload from report/scan/recommendations, computes score delta, and includes markdown link only when `markdown_audit_export` gate is enabled. |
| 2026-04-04 | **SL-008 â€” COMPLETE (implementation).** Slack manual send now persists delivery attempts in `startup_slack_delivery_events` with lifecycle status updates (`queued` -> `sent`/`failed`) and explicit failure reasons. Added backend helpers for create/update/list delivery events, wired send action to persist both success and error responses, and surfaced recent delivery attempts in startup Slack UI for immediate operator visibility. Also corrected destination default-column usage to match schema (`is_default`) so destination persistence is production-safe. |
| 2026-04-04 | **SL-009 â€” COMPLETE (implementation).** Added workspace-level `slack_auto_post` toggle in startup dashboard (founder/admin only) with default-off posture. Implemented `updateStartupSlackAutoPostSetting` action and centralized metadata patch helper (`applyStartupRolloutFlagPatch`) so rollout metadata writes are consistent across admin and startup surfaces. Startup/admin rollout flows now share one patch path, improving maintainability and reducing drift risk. |
| 2026-04-04 | **SL-010 â€” COMPLETE (implementation).** Centralized Slack service controls were finalized in `/dashboard/services` by seeding explicit Slack bundle mappings (`032_slack_service_bundle_mappings.sql`) for all current bundles and updating service-control actions to revalidate startup surfaces after service/bundle/override changes. This ensures Slack entitlement changes made in the control center propagate immediately to startup UI/action gates and reduces operator drift between admin controls and startup behavior. |
| 2026-04-04 | **SL-011 â€” COMPLETE (implementation).** Added focused Slack hardening tests across formatter, integration, gates, and rollout handling, including edge-path coverage for missing tokens, inactive destinations, delivery-event normalization, and destination resolution failure paths. Test suite now provides bounded coverage for manual send reliability and contract formatting behavior without expanding runtime complexity. |
| 2026-04-04 | **SL-012 â€” COMPLETE (operator runbook/evidence pack).** Added `docs/18-startup-slack-pilot-rollout-runbook.md` to freeze the Alie pilot rollout sequence, evidence checklist, optional SQL verification snippets, rollback order, and exit criteria. Current truth: Slack MVP implementation slices are complete, and pilot execution is now an operator runbook step rather than an unstructured TODO. |
| 2026-04-04 | **SL-013 â€” COMPLETE (implementation).** Added scheduled startup Slack cadence execution in `lib/server/startup-slack-schedule.ts` and wired Worker cron invocation in `workers/cloudflare-entry.ts`. Current truth: eligible startup workspaces with `slack_auto_post=true` can now be discovered on cadence, have synthetic startup deep-audit scans created, and deep-audit queue jobs enqueued through the existing payment-safe queue path. |
| 2026-04-04 | **SL-014 â€” COMPLETE (implementation).** Added post-report Slack autopost wiring in `workers/queue/report-queue-consumer.ts` so completed startup deep audits can automatically send `new_audit_ready` into the default active destination when rollout, entitlement, billing, and destination gates all pass. Current truth: monthly recurring startup Slack delivery now has an end-to-end backend trigger path (cron enqueue + report-completion auto-send) without adding a new frontend interaction surface. |
| 2026-04-13 | **Startup audit orchestration planning stream opened.** Added `PLAYBOOK/startup-audit-orchestration-v1.md` and registered `SAO-001` ... `SAO-015` in the Task Registry. Sequencing freeze: first add a durable audit-execution record and richer plan-task model, then add role-based multimodel review passes and planning workflow, and only then extend PR execution behind explicit approval and existing rollout gates. |
| 2026-04-04 | **Startup dual-mode theming byte - COMPLETE (implementation + focused browser proof).** Added class-based theme persistence and shared semantic tokens for cross-app light/dark support, wired a global header theme toggle, and migrated startup dashboard + long-wait overlay styling onto tokenized dark/light-safe classes. Added focused startup browser proof in `tests/e2e/startup-theme.spec.ts` (toggle + reload persistence) and aligned E2E auth fixtures in `lib/supabase/e2e-auth.ts` to UUID-safe startup records for stable startup dashboard test execution. |
| 2026-04-04 | **Startup theme parity polish byte - COMPLETE (implementation).** Replaced remaining hardcoded gray status styles in shared score/report components with semantic + dark-safe classes (`components/score-display.tsx`, `components/report-viewer-sections.tsx`) and normalized the startup dashboard primary action buttons to semantic primary tokens in `app/dashboard/startup/page.tsx`. Verification: `npx.cmd tsc --noEmit` passed. |
| 2026-04-04 | **Theme contrast cleanup byte - COMPLETE (implementation).** Replaced remaining non-blog hardcoded contrast classes in key journey/overlay surfaces with semantic theme tokens: updated results-journey status/action styling in `components/results-view.tsx`, converted long-wait overlay badge/progress/backdrop styling in `components/long-wait-provider.tsx`, and normalized non-blog footer brand text to `text-on-background` in `components/site-footer.tsx`. Verification: `npx.cmd tsc --noEmit` passed. |
| 2026-04-04 | **Theme parity browser-smoke byte - COMPLETE (verification).** Added `tests/e2e/theme-parity.spec.ts` to verify light->dark toggle behavior and dark-mode persistence after reload across `/`, `/dashboard`, and `/dashboard/startup` under E2E auth session. Verification: `npx.cmd playwright test tests/e2e/theme-parity.spec.ts` passed; `npx.cmd tsc --noEmit` passed. |
| 2026-04-04 | **Startup theme duplication cleanup byte - COMPLETE (implementation).** Removed redundant duplicate `dark:` token triplets from the startup dashboard root section wrappers in `app/dashboard/startup/page.tsx` where semantic base classes were already identical across modes. Verification: `npx.cmd tsc --noEmit` passed. |
| 2026-04-04 | **Theme verification sweep byte - COMPLETE (verification).** Re-ran focused theme browser proof after duplication cleanup: `tests/e2e/startup-theme.spec.ts` and `tests/e2e/theme-parity.spec.ts` both pass in one run, and `npx.cmd tsc --noEmit` remains clean. Current truth: startup and cross-route light/dark toggle + persistence behavior are stable after the latest token cleanup pass. |
| 2026-04-04 | **Startup surface token-level normalization byte - COMPLETE (implementation + verification).** Normalized mixed startup card/control surface classes by replacing `bg-surface-container-low dark:bg-surface-container` with one consistent semantic token (`bg-surface-container-low`) throughout `app/dashboard/startup/page.tsx`. Verification: `npx.cmd tsc --noEmit` passed; `npx.cmd playwright test tests/e2e/startup-theme.spec.ts tests/e2e/theme-parity.spec.ts` passed. |
| 2026-04-04 | **Final non-blog theme sweep byte - COMPLETE (implementation + verification).** Ran a full non-blog class sweep for hardcoded grayscale/hex token drift, normalized the remaining startup trend chart stroke to semantic token usage (`text-primary` + `currentColor`) in `app/dashboard/startup/page.tsx`, and re-verified with `npx.cmd tsc --noEmit` plus both focused browser specs (`tests/e2e/startup-theme.spec.ts`, `tests/e2e/theme-parity.spec.ts`). Current residual hardcoded colors are intentional in blog-only dark-theme surfaces and OG image rendering constants. |
| 2026-04-05 | **UX/IA redesign stream opened (UXIA-001...UXIA-012).** Full UX audit completed — technical implementation is solid but the UX is fragmented across 3 disconnected dashboard surfaces with no shared shell, buried integrations, and no guided "what's next" logic. Plan frozen at `agents/memory/plans/uxia-redesign.md`. Approach: write 12 Stitch prompts (one per key screen), generate and approve UI screens, then implement. No code changes until Stitch screens are approved. See UXIA task registry below. |
| 2026-04-06 | **Bug fix stream BF-001…BF-006 opened.** Three production bugs identified: (A) startup workspace users hit Stripe for deep audit — `startup_workspace_id` missing from scan route select + no `startup_bypass` checkoutMode path; (B) same missing field in `/api/checkout` causes startup lite users to hit Stripe unconditionally; (C) no delete UI or server actions for workspace members, workspaces, or agency accounts in admin console. Stream registered as BF-001…BF-006. Plan: `agents/memory/plans/snoopy-hatching-scroll.md`. |
| 2026-04-08 | **BILL-008 — COMPLETE (implementation).** `app/dashboard/(admin)/layout.tsx` now `redirect('/dashboard')` when `getAdminPageContext` returns `ok: false` unless the message is the stable service-role misconfiguration string; operators still see an inline error for that case. `ADMIN_PAGE_CONTEXT_MISCONFIGURED_MESSAGE` exported from `lib/server/admin-runtime.ts`; `(admin)/content/page.tsx` aligned. Evidence: `npm run type-check`, `npx vitest run lib/server/admin-runtime.test.ts`. |
| 2026-04-08 | **BILL-009 — COMPLETE (implementation).** When `ADMIN_EMAIL` is set, `middleware.ts` redirects authenticated users whose session email does not match to `/dashboard` before `/admin/*` (except `/admin/login`), preserving refreshed session cookies. `updateSession` now returns `userEmail`. `isLegacyAdminEmailEnvMatch` lives in edge-safe `lib/server/legacy-admin-email.ts`; `isAdminEmail` delegates to it. DB-only platform admins must unset `ADMIN_EMAIL` or align it with their login email. Evidence: `npm run type-check`, `npx vitest run lib/server/legacy-admin-email.test.ts`. |
| 2026-04-08 | **BILL-010 — COMPLETE (documentation).** **ADR-009** added to `agents/memory/DECISIONS.md`: `user_subscriptions` is canonical for Stripe bundle + status; `users.plan` is coarse `plan_type` denormalized via `bundleToPlan`; `assignUserPlan` vs enum gap called out for BILL-011. `docs/01-current-state.md` updated with billing/plan paragraph. |
| 2026-04-08 | **BILL-011 — COMPLETE (implementation).** `lib/server/plan-type.ts` defines `PLAN_TYPE_VALUES` / `isValidPlanType` / `normalizePlanTypeForAdmin`. `assignUserPlan` accepts only `free|pro|agency`. Admin user detail plan dropdown matches. Evidence: `npm run type-check`, `npx vitest run lib/server/plan-type.test.ts`. |
| 2026-04-08 | **BILL-012 — COMPLETE (implementation).** `syncAdminCompSubscription` upserts synthetic `user_subscriptions` row (`admin_comp:<userId>`, `pro`→`startup_dev`, `agency`→`agency_core`) or cancels on `free`; skips if a real Stripe subscription already covers the target bundle. `cancelUserSubscription` calls sync after setting `users.plan` free. Evidence: `npm run type-check`, `npm run test -- --run`. |
| 2026-04-08 | **BILL-013 — COMPLETE (implementation).** `subscriptionNeedsWorkspaceProvisioning` centralizes “live paid bundle + no workspace” checks; dashboard loads `user_subscriptions` and shows `SubscriptionWorkspacePendingBanner` with `provisionMyWorkspaceForSubscription` (service role after ownership check). Admin user detail uses the same predicate for the provision button. Evidence: `npm run type-check`, `npm run test -- --run`. |
| 2026-04-08 | **BILL-014 — COMPLETE (database migration).** Migration `037_service_entitlements_rls_policies.sql`: `authenticated` SELECT on `service_catalog`, `service_bundles`, `service_bundle_services`; scoped SELECT on `service_entitlement_overrides` (global/bundle_default open to signed-in users; agency_account/agency_client/user via membership); `service_model_policies` SELECT by scope including `is_startup_workspace_member` for startup workspace rows. Writes remain service-role only. |

### Production Bug Fix Stream (BF-001 … BF-006)
> **Context:** Three production bugs reported 2026-04-06. Bugs A+B: startup workspace users incorrectly hit Stripe for deep audit (missing startup bypass path in scan + checkout routes). Bug C: no delete path for members or workspace/account objects in admin console.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| BF-001 | Fix `/api/scans/[id]`: add `startup_workspace_id` select + startup membership check + `startup_bypass` checkoutMode | Backend | ✅ DONE | `app/api/scans/[id]/route.ts`; COMPLETION_LOG BF-001 |
| BF-002 | Fix `/api/checkout`: add `startup_workspace_id` select + startup bypass block before Stripe | Backend | ✅ DONE | `app/api/checkout/route.ts`; COMPLETION_LOG BF-002 |
| BF-003 | Add `removeStartupWorkspaceMember` + `deleteStartupWorkspace` server actions to `app/dashboard/startups/actions.ts` | Backend | ❌ PENDING | — |
| BF-004 | Add `removeAgencyMember` + `deleteAgencyAccount` server actions to `app/dashboard/agencies/actions.ts` | Backend | ❌ PENDING | — |
| BF-005 | Add remove member + delete workspace UI to `components/startup-admin-control-view.tsx` | Frontend | ❌ PENDING | — |
| BF-006 | Add remove member + delete account UI to `components/agency-admin-control-view.tsx` | Frontend | ❌ PENDING | — |
