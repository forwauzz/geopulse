# GEO-Pulse — Project State
> Maintained by: Orchestrator only
> Format: Update this file whenever a task changes state. Never delete history — add new entries.

---

## Current Phase: Phase 4 — Launch

**Phase goal:** Production deploy, OG share image, email DNS (SPF/DKIM/DMARC), WAF, keep-alive cron, launch security audit.

**Implementation status (2026-03-25):** P4-002 + P4-005 **in repo**. **P4-001** ✅ — production Worker `https://geo-pulse.uzzielt.workers.dev` + **live Stripe checkout return path** verified — see `COMPLETION_LOG.md` *Phase 4 — operator evidence*. **P2-008** ✅ — production paid path verified. **Still to close with evidence in log:** **P4-003** (SPF/DKIM/DMARC), **P4-004** (WAF CVE-2025-29927), **P4-006** (Security sign-off on five blockers). **Current operator blocker:** the new custom domain `getgeopulse.com` is now purchased, but DNS / Resend / custom-domain cutover evidence is still pending, so `P4-003` and final `P4-006` are not closed yet. **Phase 4→Launch gate** (`agents/ORCHESTRATOR.md`) requires all five security blockers + production smoke — Orchestrator marks when P4-003/004/006 evidence is attached.

**Current UX follow-up (2026-03-26):** User testing exposed a product-truth gap in the core journey: free scan preview, pay-for-full-audit, and email-save flow did not read as clear incremental steps toward one outcome. The core implementation is now in repo: explicit step framing, shared preliminary/final audit loading language, and state-driven paid-report status have replaced the earlier optimistic copy. Remaining UX work is verification and any follow-up polish from real-user testing.

**Current strategic expansion (2026-03-26):** GEO-Pulse will keep the current audit/report product as the customer-facing wedge while a benchmark/measurement layer is planned as a staged internal platform. This work is tracked as a separate initiative and does not change the truth that launch remains blocked on `P4-003`, `P4-004`, and `P4-006`.

**Current architecture hardening stream (2026-03-28):** Because Phase 4 launch closure is externally blocked, GEO-Pulse is also tracking a narrow architecture-hardening stream to keep the platform lean, easier to debug, and better prepared for future browser-level testing. Current truth: this stream does not close launch; it currently covers internal observability, Playwright readiness plus the first open-source Playwright smoke-test foundation, the first thin-route cleanup slice via a shared admin runtime helper for repeated auth/env/service-role setup, the first large-module decomposition slice for the benchmark run-detail route, the second large-module decomposition slice for the report viewer, the third large-module decomposition slice for the benchmark overview route, the fourth large-module decomposition slice for deep-audit report preparation helpers shared between PDF and markdown generation, the fifth large-module decomposition slice for deep-audit crawl pending-state helpers, the sixth large-module decomposition slice for deep-audit email-delivery composition helpers, and the admin-auth maturity plan documenting the future move from env-only founder-stage gating to a table-driven admin allowlist. The Playwright smoke lane remains intentionally narrow and now runs serialized (`workers: 1`, no full-parallel mode) because that is the stable dev-server setting for this repo.

**Current benchmark implementation (2026-03-27):** The first benchmark storage foundation is now in repo via migration `012_benchmark_foundation.sql`, the shared benchmark identity + repository seam is implemented in `lib/server/benchmark-domains.ts` and `lib/server/benchmark-repository.ts`, the first benchmark seed path plus runner input contract exist in `scripts/benchmark-seed.ts`, `lib/server/benchmark-seed.ts`, and `lib/server/benchmark-runner-contract.ts`, the first benchmark runner skeleton exists in `lib/server/benchmark-runner.ts` and `scripts/benchmark-runner.ts`, the runner depends on a benchmark-specific execution adapter boundary in `lib/server/benchmark-execution.ts`, the first conservative citation extraction path exists in `lib/server/benchmark-citations.ts`, the first separated metric computation helper exists in `lib/server/benchmark-metrics.ts`, the first admin query layer exists in `lib/server/benchmark-admin-data.ts`, and the first benchmark admin flow is now in repo:
- `/dashboard/benchmarks`
- `/dashboard/benchmarks/[runGroupId]`
- `/dashboard/benchmarks/domains/[domainId]`

Current truth: the benchmark admin flow now supports an opt-in live Gemini lane for one configured model via `BENCHMARK_EXECUTION_*` env, while default behavior remains the safe stub adapter. Completed responses flow through citation parsing and metric computation, admins can inspect recent runs, drill into a run group, view one domain’s benchmark history over time, add a benchmark domain from the admin UI, add a lightweight query set from the admin UI, and launch one benchmark run from the benchmark overview UI. Live provider failures now preserve the Gemini error body in `response_metadata`, and a run group is marked `failed` when every query run fails. First live benchmark proof now exists for a real customer domain with 6 completed queries, 4 extracted citations, and non-zero metrics on `gemini-2.5-flash-lite`; transient `503 UNAVAILABLE` responses now go through a light bounded retry/backoff path before a query run is marked failed. Grounded runs now support a first minimal site-derived grounding builder: when manual evidence is absent, the runner fetches the homepage plus likely about/services pages through the existing validated fetch path and derives a small bounded evidence set for `grounded_site`. Grounded citation rows now preserve exact-page provenance when a parsed citation URL exactly matches a grounded evidence page URL, and unresolved domain-only mentions remain explicitly unresolved rather than guessed. This is still a heuristic builder, not a full crawl, ranked page-selection system, or exact-page citation scorer.

**Operator note (2026-03-25):** Production checkout **`POST /api/checkout`** is rate-limited (**5 attempts per IP per hour**, `Retry-After: 3600` — see `lib/server/rate-limit-kv.ts` `checkCheckoutRateLimit`). Retesting paid checkout may require **waiting up to one hour** or using **another network/IP**. This is **not** a Stripe configuration failure by itself. **Sequencing:** **DA-001** ✅ · **DA-002** ✅ · **DA-003** ✅ · **DA-004** ✅ — queue-scale deep-audit continuation is now implemented and unit-verified (`crawl_pending`, re-queue path, chunk metrics, 1000-page cap, targeted Vitest). **DA-005** ✅ — optional Browser Rendering SPA fallback for paid deep audits, disabled by default unless explicit operator config is provided. **Workflows** are now an optional future enhancement for extreme-scale crawls, not a shipped-path blocker. **Orchestrator:** close **Phase 4 — Launch** first (`agents/ORCHESTRATOR.md` § *Phase 4 first — defer remaining Deep Audit*). **Next (Phase 4):** paste **P4-003 / P4-004 / P4-006** evidence in `COMPLETION_LOG.md`; then **Phase 4→Launch gate**.

**Deep Audit Upgrade (v2):** Tracked in Task Registry (**DA-001…DA-005**); spec `.cursor/plans/report_depth_and_formats_fa7e556e.plan.md`, narrative [`PLAYBOOK/audit-upgrade.md`](../../PLAYBOOK/audit-upgrade.md). **DA-001** ✅ **DONE** — `005_scan_runs_scan_pages.sql`, queue **v2**, `runDeepAuditCrawl`, multi-page PDF (operator-verified). **DA-002** ✅ **DONE** — `workers/lib/fetch-gate.ts`, `validateEngineFetchUrl`, `robots-and-sitemap.ts`, `crawl-url-utils.ts` (section-aware cap), migration `006_scan_pages_section.sql`; **Security** formal sign-off on fetch path still recommended per `agents/SECURITY_AGENT.md`. **DA-003** ✅ **DONE** — `DeepAuditReportPayload`, `build-deep-audit-markdown.ts`, R2 upload + `reports.pdf_url`, Resend attach vs link policy (`DEEP_AUDIT_ATTACH_MAX_BYTES`). **DA-004** ✅ **DONE** — robots `Crawl-delay` + crawl metrics (`COMPLETION_LOG` DA-004-inc) **+ chunked queue re-invocation** (`crawl_pending` in `scan_runs.config`) **+ chunk-progress guardrails** **+ 1000-page cap** (`MAX_DEEP_AUDIT_PAGE_LIMIT`) **+ accurate coverage chunk reporting** **+ targeted multi-chunk Vitest proof**. **DA-005** ✅ **DONE** — optional Browser Rendering-backed SPA fallback for paid deep audits (`workers/scan-engine/browser-rendering.ts`, deep-audit crawl + queue wiring, env/docs updates). Independent of Phase 4 closure; Orchestrator sets parallel vs sequential execution.

**How to close Phase 4:** Follow **`COMPLETION_LOG.md` → *Phase 4 — operator execution order*** — numbered steps + **Stripe Live checkpoint** (after production hostname is fixed). Paste deploy + DNS + WAF + smoke evidence there; Orchestrator marks tasks and Phase 4→Launch gate per `agents/ORCHESTRATOR.md`.

**Phase 2 — CLOSED (implementation in repo; operator: apply migration `004_payments_guest_email.sql`, verify PDF &lt;60s + webhook in staging).** Checkout (`app/api/checkout`), Stripe webhook + idempotency (`app/api/webhooks/stripe`, `lib/server/stripe/checkout-completed.ts`), queue → PDF → Resend (`workers/queue/report-queue-consumer.ts`), DLQ replay (`workers/queue/dlq-replay.ts`). Tests: `npm run test`, `lib/server/stripe/checkout-completed.test.ts`.

**Phase 3 — CLOSED (implementation in repo).** Magic link (`app/login`, `app/login/actions.ts`), OAuth callback + guest purchase linking (`app/auth/callback`, `lib/server/link-guest-purchases.ts`), middleware session + CVE header (`middleware.ts`, `lib/supabase/middleware.ts`), dashboard (`app/dashboard`). Gate: sign in → `/dashboard` lists `scans` / `reports` for `auth.uid()`; unauthenticated `/dashboard` → `/login`.

**Prior phase goal (Phase 2):** Stripe checkout → webhook (verified) → queue → pdf-lib report → Resend delivery + idempotency + DLQ.

**Orchestrator sequencing:** Phases **2** and **3** are complete in repo; **Phase 4 — Launch** is in progress. **Deep Audit Upgrade:** **DA-001–005** ✅ in repo; this does **not** replace Phase 4 tasks or API deferral rules. **API-as-a-Service** (**API-002 … API-007**) starts only after **Phase 4→Launch gate** (see `ORCHESTRATOR.md`). **API-001** (schema) stays done.

**Phase 2→3 gate** (`agents/ORCHESTRATOR.md`): $1 test payment succeeds · PDF delivered in &lt;60s · Stripe webhook never processed without signature verification.

**Phase 3→4 gate** (`agents/ORCHESTRATOR.md`): Registered user can log in · past scans visible · auth middleware blocks unauthenticated routes.

**Operator note (2026-03-24):** $1 payment reported **working end-to-end**. Remaining gate items (PDF SLA, webhook verification, task evidence in `COMPLETION_LOG.md`) still required before advancing phase.

**Phase 1 — CLOSED (2026-03-24).** Free scan path implemented (landing, Turnstile, SSRF-gated fetch, 11 deterministic + 2 Gemini checks, weighted score, results + email gate, `scans`/`leads` via service role, KV rate limits). Evidence: `COMPLETION_LOG.md` *Phase 1 — implementation bundle*.

**Phase 1→2 gate (manual) — operator verified (2026-03-24):** End-to-end run on `https://techehealthservices.com/`: scan completed, **AI Search Readiness Score** returned, results page + email gate (“You are on the list.”). Orchestrator may mark **ACCEPTED** per `ORCHESTRATOR.md` once `leads` row + optional KV rate-limit spot-check are confirmed in Supabase/CF dashboard.

**Phase 0 — CLOSED (2026-03-24).** Gate per `ORCHESTRATOR.md`: `wrangler dev` + Supabase tables/RLS + anon RLS check + type-check — see `COMPLETION_LOG.md` section *Phase 0 — P0-002 / P0-003 / P0-004 / P0-005 / P0-006 evidence bundle*.

**Phase 0 exit criteria (archived):**
- [x] `npm install` completes without errors
- [x] `wrangler dev` starts the dev server
- [x] Supabase project created; schema from `001_initial_schema.sql` (+ `002_api_keys.sql`) applied on remote `geo_pulse`
- [x] Core 6 tables + API tables: RLS enabled; `leads` verified via **anon** PostgREST (200 + `[]`)
- [x] `.cursor/rules/` files present in repo
- [x] `.dev.vars` present at repo root (from `.dev.vars.example` pattern)
- [x] `npm run type-check` returns 0 errors

---

## Task Registry

### Phase 0 — Scaffold
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| P0-001 | Scaffold files created | Orchestrator | ✅ DONE | Files exist in repo |
| P0-002 | `npm install` passes | Backend | ✅ DONE | COMPLETION_LOG Phase 0 bundle |
| P0-003 | Supabase project created + migration applied | Database | ✅ DONE | COMPLETION_LOG Supabase audit + Phase 0 bundle |
| P0-004 | RLS verified via anon key | Security | ✅ DONE | COMPLETION_LOG Phase 0 bundle (PostgREST `leads`) |
| P0-005 | `wrangler dev` runs locally | Backend | ✅ DONE | COMPLETION_LOG Phase 0 bundle |
| P0-006 | Type check passes (0 errors) | QA | ✅ DONE | COMPLETION_LOG Phase 0 bundle |

### Phase 1 — Core Scan Engine
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| P1-001 | Landing page + scan form | Frontend | ✅ DONE | `app/page.tsx`, `components/scan-form.tsx`; COMPLETION_LOG P1 |
| P1-002 | Turnstile integration (form + server validation) | Backend | ✅ DONE | `components/*`, `lib/server/turnstile.ts`, `app/api/scan`, `app/api/leads`; COMPLETION_LOG P1 |
| P1-003 | SSRF validator unit tests (all edge cases) | QA | ✅ DONE | `workers/lib/ssrf.test.ts` (vitest); COMPLETION_LOG P1 |
| P1-004 | Scan Worker: fetch + HTMLRewriter parse | Backend | ✅ DONE | `workers/scan-engine/fetch-page.ts` + `parse-signals.ts` (bounded fetch + signal extraction; streaming HTMLRewriter deferred to dedicated Worker hardening) |
| P1-005 | Deterministic checks (11 of 15) implemented | Backend | ✅ DONE | `workers/scan-engine/checks/check-*.ts` (11 files) + `registry.ts` |
| P1-006 | Gemini integration (2 checks: Q&A + extractability) | Backend | ✅ DONE | `workers/providers/gemini.ts`, `check-llm-*.ts` |
| P1-007 | Scoring engine (weighted 100-pt rubric) | Backend | ✅ DONE | `workers/scan-engine/scoring.ts` |
| P1-008 | Results page: score + 3 issues + email gate | Frontend | ✅ DONE | `app/results/[id]`, `components/results-view.tsx`, `score-display.tsx`, `email-gate.tsx` |
| P1-009 | Email capture → Supabase leads table | Backend | ✅ DONE | `app/api/leads/route.ts` (service_role insert) |
| P1-010 | Rate limiting (10 req/min/IP + 20/day/email) | Security | ✅ DONE | `lib/server/rate-limit-kv.ts` + `SCAN_CACHE` binding |
| P1-011 | Phase 1 integration test: end-to-end scan | QA | ✅ DONE | `npm run test` (vitest) + `npm run build`; manual e2e recommended before Phase 2 sign-off — COMPLETION_LOG P1 |

### Phase 2 — Payment + PDF + Email
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| P2-001 | Stripe $29 checkout integration | Backend | ✅ DONE | `app/api/checkout/route.ts` |
| P2-002 | Stripe webhook handler (with signature verification) | Backend | ✅ DONE | `app/api/webhooks/stripe/route.ts` |
| P2-003 | Cloudflare Queue: enqueue deep audit on payment | Backend | ✅ DONE | `lib/server/stripe/ensure-deep-audit-job-queued.ts` |
| P2-004 | pdf-lib report generator | Backend | ✅ DONE | `workers/report/build-deep-audit-pdf.ts` |
| P2-005 | Resend: HTML email + PDF attachment delivery | Backend | ✅ DONE | `workers/report/resend-delivery.ts` |
| P2-006 | Dead-letter queue handler + retry logic | Backend | ✅ DONE | `workers/queue/report-queue-consumer.ts`, `workers/queue/dlq-replay.ts` |
| P2-007 | Payment idempotency (Stripe event ID dedup) | Security | ✅ DONE | `lib/server/stripe/checkout-completed.ts` |
| P2-008 | $1 test payment end-to-end test | QA | ✅ DONE | COMPLETION_LOG Phase 4 — operator evidence; live payment `geo-pulse.uzzielt.workers.dev` verified checkout success return path |

### Phase 3 — Auth + Dashboard
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| P3-001 | Supabase magic link auth | Backend | ✅ DONE | `app/login`, `app/login/actions.ts`, `lib/supabase/server.ts` |
| P3-002 | Auth middleware (CVE-2025-29927 protected) | Security | ✅ DONE | `middleware.ts` (session + block `x-middleware-subrequest`); Next.js patched |
| P3-003 | User dashboard: scan history + past reports | Frontend | ✅ DONE | `app/dashboard/page.tsx` |
| P3-004 | Auto-account creation post-payment | Backend | ✅ DONE | `lib/server/link-guest-purchases.ts`, `app/auth/callback/route.ts`, `004_payments_guest_email.sql` |

### Phase 4 — Launch
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| P4-001 | Production deploy to Cloudflare Pages + Workers | Backend | ✅ DONE | COMPLETION_LOG Phase 4 — operator evidence; prod URL + live Stripe + `lib/server/cf-env.ts` `pickEnvString` for Worker env |
| P4-002 | Share-your-score OG image generation | Frontend | ✅ DONE | `app/results/[id]/opengraph-image.tsx`, `lib/server/get-scan-for-public-share.ts`, `page.tsx` `generateMetadata`; COMPLETION_LOG Phase 4 bundle |
| P4-003 | SPF + DKIM + DMARC configured | Security | ⬜ PENDING (operator blocked) | `getgeopulse.com` is now purchased; complete once DNS records are added in Cloudflare/Resend and evidence is attached |
| P4-004 | WAF rules enabled (CVE-2025-29927) | Security | ⬜ DEFERRED (paid CF) / mitigated | Managed WAF often needs **paid** Cloudflare — **mitigation in repo:** `middleware.ts` + patched Next.js; full WAF when upgraded — `COMPLETION_LOG` P4-004 paid-plan note; **P4-006** Security acknowledges |
| P4-005 | Supabase keep-alive cron configured | Backend | ✅ DONE | `wrangler.jsonc` `triggers.crons`, `workers/cloudflare-entry.ts` `scheduled`; COMPLETION_LOG Phase 4 bundle |
| P4-006 | Launch security audit (all 5 blockers) | Security | ⬜ PENDING (waiting on operator blocker) | Repo-side evidence refreshed in COMPLETION_LOG `P4-006 — Launch security audit evidence bundle refresh`; final sign-off still depends on `P4-003` DNS evidence + `P4-004` launch-policy decision |

### Deep Audit Upgrade (multi-page paid audit — “v2”)
> **Spec:** `.cursor/plans/report_depth_and_formats_fa7e556e.plan.md` (§ Target architecture). **Narrative / executive summary:** [`PLAYBOOK/audit-upgrade.md`](../../PLAYBOOK/audit-upgrade.md). **Does not replace** Phase 4 or API deferral rules in `agents/ORCHESTRATOR.md` — Orchestrator sets **start date** and whether this runs **in parallel with** or **after** Phase 4 operator tasks. **Security** sign-off required on fetch-gate / SSRF changes (DA-002).

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| DA-001 | Phase 0: `scan_runs` / `scan_pages` + RLS; payment → deep crawl + cap; free scan unchanged | Backend + Database | ✅ DONE | COMPLETION_LOG DA-001 (✅ ACCEPTED); operator smoke: paid PDF for `https://techehealthservices.com/` — **Pages scanned** + per-page checklist (10 URLs, site aggregate score); migration `005_scan_runs_scan_pages.sql` |
| DA-002 | Phase 1: Central fetch gate (extend `workers/lib/ssrf.ts`); robots + sitemap streaming; section-aware sampling | Backend + Security | ✅ DONE | `workers/lib/fetch-gate.ts`, `validateEngineFetchUrl`, `deep-audit-crawl` + robots/sitemap + `prioritizeUrlsBySection`; `006_scan_pages_section.sql`; `npm run test` 36 passed; COMPLETION_LOG DA-002 — **Security** review recommended |
| DA-003 | Reporting: `DeepAuditReportPayload`; PDF/MD; R2 + email link policy | Backend | ✅ DONE | COMPLETION_LOG DA-003; `workers/report/*`, `report-queue-consumer.ts`, `wrangler.jsonc` `r2_buckets` + `DEEP_AUDIT_R2_PUBLIC_BASE`; CI runs `cf-typegen` |
| DA-004 | Phase 2: Scale (queue continuation, politeness, metrics) | Backend | ✅ DONE | `deep-audit-crawl.ts`, `report-queue-consumer.ts`, `deep-audit-page-limit.ts`, `deep-audit-crawl.test.ts`; COMPLETION_LOG DA-004 incremental entries + DA-004 completion entry |
| DA-005 | Phase 3 (optional): Browser Rendering / SPA crawl | Backend | ✅ DONE | `workers/scan-engine/browser-rendering.ts`, `deep-audit-crawl.ts`, queue/env wiring; COMPLETION_LOG DA-005 |

### Marketing Attribution Microservice (parallel with launch)
> **PRD:** `PLAYBOOK/marketing-attribution-weekly-report.md` (runbook). **Runs in parallel** with Phase 4 / product launch — does not block or replace any Phase 4 tasks. Service code in `services/marketing-attribution/`. Schema in `analytics` (Supabase, service-role only — no anon access). Client-side UTM capture via `gp_anon_id` cookie (middleware) + `gp_utm` sessionStorage (`lib/client/attribution.ts`). Weekly email report on Monday cron via Resend (opt-in: set `MARKETING_REPORT_TO` secret).

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| MA-001 | Foundation: `analytics` schema + `marketing_events` table + Zod contract + ingestion endpoint + idempotent tests | Backend + Database | ✅ DONE | `007_marketing_attribution.sql` applied; `services/marketing-attribution/schema.ts`, `ingest.ts`, `emit.ts`; `app/api/internal/marketing/events/route.ts`; 39 new tests; type-check 0 errors, 89/89 tests |
| MA-002 | Wiring: client UTM/session capture + emit from scan/leads/checkout/webhook | Backend + Frontend | ✅ DONE | `lib/client/attribution.ts`, `components/attribution-init.tsx`, middleware `gp_anon_id` cookie; emits `scan_started`, `scan_completed`, `lead_submitted`, `checkout_started`, `payment_completed`; 89/89 tests |
| MA-003 | Reporting: first-touch/last-touch views + weekly funnel aggregate + runbook | Backend + Database | ✅ DONE | `008_marketing_attribution_views.sql` applied; `analytics.attribution_conversions_v1`, `analytics.channel_funnel_weekly_v1`; `PLAYBOOK/marketing-attribution-weekly-report.md` |
| MA-004 | Weekly email report via Monday cron + Resend | Backend | ✅ DONE | `services/marketing-attribution/weekly-report.ts`; `workers/cloudflare-entry.ts` scheduled handler; opt-in via `MARKETING_REPORT_TO` env var |
| MA-005 | Hardening: queue-backed ingestion + replay + DLQ (optional) | Backend | ⬜ DEFERRED | Trigger: event volume > 1000/day or reliability issue |

### Admin & report quality eval (parallel with launch)
> **Does not replace** Phase 4 tasks. **Security:** `/admin/login` password path + `report_eval_runs` RLS — see `COMPLETION_LOG.md` ADM-001 … EVAL-004; Security sign-off recommended before treating as production-hardened.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| ADM-001 | Supabase Email password enabled + env docs | Operator + Backend | ✅ DONE | `.env.local.example`, `.dev.vars.example`; Auth dashboard |
| ADM-002 | `/admin/login` + `signInWithPassword` + non-admin sign-out | Backend | ✅ DONE | `app/admin/login/*`; COMPLETION_LOG ADM-001 … EVAL-004 |
| ADM-003 | Middleware public `/admin/login`; `/dashboard/*` unchanged | Security | ✅ DONE | `middleware.ts` (no `/admin` auth wall beyond session) |
| EVAL-001 | `reports.markdown_url` + `report_payload_version`; Worker insert | Backend | ✅ DONE | `009_admin_report_eval.sql`; `workers/queue/report-queue-consumer.ts` |
| EVAL-002 | `report_eval_runs` + RLS (no policies; service_role only) | Database | ✅ DONE | `009_admin_report_eval.sql` |
| EVAL-003 | Structural rubric + `npm run eval:smoke` | Backend | ✅ DONE | `lib/server/report-eval-structural.ts`, `scripts/report-eval-smoke.ts` |
| EVAL-004 | `/dashboard/evals` admin UI | Frontend | ✅ DONE | `app/dashboard/evals/page.tsx` |

### UX Loading States (parallel with launch)
> **Does not replace** Phase 4 tasks. Focused on long-wait feedback for user actions that may exceed the fast-path threshold. Strategy: keep inline button pending states for quick requests, and escalate to a centralized delayed overlay only when the wait is materially longer.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| UX-001 | Centralized delayed long-wait loading overlay for scan / checkout / save / auth / report flows | Frontend | ✅ DONE | `components/long-wait-provider.tsx`, `lib/client/loading-journeys.ts`; COMPLETION_LOG UX-001 |
| UX-002 | Define canonical user journey for scan → preview → pay / save → report delivery with one visible step model | Product + Frontend | ✅ DONE | `components/results-view.tsx`; COMPLETION_LOG UX-002 … UX-006 |
| UX-003 | Replace query-string-only checkout success messaging with state-driven payment / report status on results | Frontend + Backend | ✅ DONE | `components/results-view.tsx`, `app/results/[id]/page.tsx`; COMPLETION_LOG UX-002 … UX-006 |
| UX-004 | Unify preliminary drafting and final report generation under one shared loading-story pattern | Frontend | ✅ DONE | `lib/client/loading-journeys.ts`, `components/results-view.tsx`; COMPLETION_LOG UX-002 … UX-006 |
| UX-005 | Clarify branching on results page: free email-save path vs paid full-audit path, with explicit next-step framing | Frontend + Product | ✅ DONE | `components/results-view.tsx`, `components/deep-audit-checkout.tsx`, `components/email-gate.tsx`; COMPLETION_LOG UX-002 … UX-006 |
| UX-006 | Add delivered / in-progress / action-needed states driven by real payment / report persistence instead of optimistic copy | Frontend + Backend | ✅ DONE | `components/results-view.tsx`; COMPLETION_LOG UX-002 … UX-006 |
| UX-007 | End-to-end UX verification for guest and signed-in flows; verify shared journey-state behavior | QA + Product | ✅ DONE | `lib/client/results-journey.test.ts`, `app/api/scans/[id]/route.ts`, `lib/server/get-scan-for-public-share.ts`; COMPLETION_LOG UX-007 |
| UX-008 | Tighten the results-to-report action layer so share snapshot is real, delivered-report copy stays truthful, and report view falls back cleanly when only PDF is available | Frontend + Product | ✅ DONE | `components/results-view.tsx`, `components/score-display.tsx`, `components/report-viewer.tsx`, `tests/e2e/smoke.spec.ts`; COMPLETION_LOG UX-008 |

### v2 Enhancement Plan — Scoring Foundation + Check Expansion
> **Plan reference:** Task ledger only; prior `.cursor/plans/geopulse_v2_enhancement_audit_36a0ecee.plan.md` pointer is stale / not present in repo as of 2026-03-26. **Tier 1** (Scoring Foundation) completes the v2 status enum, category mapping, per-category scores, UI, and report integration. **Tier 2** adds new checks. **Does not replace** Phase 4 or API deferral rules.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| T1-1 | Extend `CheckResult` with v2 6-value status enum | Backend | ✅ DONE | `workers/lib/interfaces/audit.ts` — `CheckStatus` type |
| T1-2 | Add `category` field to `AuditCheck`; map 17 checks → 5 v2 categories | Backend | ✅ DONE | All 17 check files + `registry.ts` + `audit.ts` `CheckCategory` |
| T1-3 | Compute per-category 0-100 scores in `scoring.ts` | Backend | ✅ DONE | `computeCategoryScores`, `WeightedResult.category`; 96/96 tests |
| T1-4 | Surface 5 category scores in results UI | Frontend | ✅ DONE | `score-display.tsx` — 5-pillar grid replaces 4-pillar; `results-view.tsx` threads `categoryScores` |
| T1-5 | Category breakdown in PDF + Markdown reports | Backend | ✅ DONE | `build-deep-audit-markdown.ts`, `build-deep-audit-pdf.ts` — category table; `DeepAuditReportPayload.categoryScores` |
| T2-1 | New check: Schema.org @type validation | Backend | ✅ DONE | `workers/scan-engine/checks/check-schema-types.ts`, `registry.ts` |
| T2-2 | New check: Image alt text presence | Backend | ✅ DONE | `workers/scan-engine/checks/check-alt-text.ts`, `registry.ts` |
| T2-3 | New check: Content freshness signals | Backend | ✅ DONE | `workers/scan-engine/checks/check-freshness.ts`, `registry.ts` |
| T2-4 | New check: External authority links | Backend | ✅ DONE | `workers/scan-engine/checks/check-external-links.ts`, `registry.ts` |
| T2-5 | New check: Security headers | Backend | ✅ DONE | `workers/scan-engine/checks/check-security-headers.ts`, `registry.ts` |
| T2-6 | Surface LLM confidence in issue display | Frontend | ✅ DONE | `components/score-display.tsx`, `workers/scan-engine/run-scan.ts` |
| T3-6 | In-browser rendered markdown report view | Frontend | ✅ DONE | `components/report-viewer.tsx`, `app/results/[id]/report/page.tsx` |
| T3-7 | Dynamic interactive report view | Frontend | ✅ DONE | `components/report-viewer.tsx`; interactive summary + section chips + collapsible report sections; type-check + build |

### Audit Closure — Report Integrity + Product Truth
> **Source:** 2026-03-26 implementation audit. These tasks convert audit findings into explicit, bite-size follow-up work. They do **not** override Phase 4 launch sequencing; Orchestrator decides execution order.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| AU-001 | Report payload: separate `highlightedIssues` from full evaluated sitewide issue set | Backend | ✅ DONE | COMPLETION_LOG AU-001 … AU-005 / AU-007 |
| AU-002 | PDF + Markdown: render true full-check breakdown instead of top-issues-only | Backend | ✅ DONE | COMPLETION_LOG AU-001 … AU-005 / AU-007 |
| AU-003 | Preserve v2 statuses (`PASS` / `FAIL` / `BLOCKED` / `NOT_EVALUATED` / `LOW_CONFIDENCE` / `WARNING`) in report UI/PDF/MD | Backend + Frontend | ✅ DONE | COMPLETION_LOG AU-001 … AU-005 / AU-007 |
| AU-004 | Deep audit scan persistence: keep `categoryScores` and report payload metadata consistent in `full_results_json` | Backend | ✅ DONE | COMPLETION_LOG AU-001 … AU-005 / AU-007 |
| AU-005 | Add coverage summary, blocked/error counts, and crawl notes to customer-facing report | Backend | ✅ DONE | COMPLETION_LOG AU-001 … AU-005 / AU-007 |
| AU-006 | Add technical appendix section (robots, schema findings, headers) to Markdown/PDF | Backend | ✅ DONE | COMPLETION_LOG AU-006 / AU-008 |
| AU-007 | Product truth pass: align landing/report/checkout copy with actual implemented checks and deliverables | Architect + Frontend | ✅ DONE | COMPLETION_LOG AU-001 … AU-005 / AU-007 |
| AU-008 | Security/docs truth pass: reconcile SSRF documentation with actual runtime protections and limitations | Security + Architect | ✅ DONE | COMPLETION_LOG AU-006 / AU-008 |
| AU-009 | Replace structural-only report eval with content-integrity assertions (check count, statuses, appendix, page mapping) | QA + Backend | ✅ DONE | COMPLETION_LOG AU-009 / AU-010 |
| AU-010 | Add golden-report fixtures covering blocked, low-confidence, and multi-page cases | QA | ✅ DONE | COMPLETION_LOG AU-009 / AU-010 |

### Retrieval / Evaluation Backlog
> **Status:** Retrieval / eval foundation exists in repo (`RE-001` … `RE-007`), including deterministic retrieval simulation, a local `promptfoo` harness, and a documented `ragas` no-go decision. `ragas` runtime integration, benchmark engine, and prompt-cluster analysis are still not implemented as of 2026-03-26.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| RE-001 | Define retrieval-eval scope: prompts, sampled pages, expected outputs, and non-goals for MVP vs v2 | Architect | ✅ DONE | `PLAYBOOK/retrieval-eval-foundation.md`; COMPLETION_LOG RE-001 / RE-002 |
| RE-002 | Add retrieval-ready data model for eval runs, prompt sets, passages, and scored answers | Architect + Database | ✅ DONE | `supabase/migrations/010_retrieval_eval_foundation.sql`; COMPLETION_LOG RE-001 / RE-002 |
| RE-003 | Build minimal retrieval simulation harness over scanned pages/passages (no external benchmark claims yet) | Backend | ✅ DONE | `lib/server/retrieval-eval.ts`, COMPLETION_LOG RE-003 |
| RE-004 | Add benchmark-percentile computation design or explicitly remove percentile claims until implemented | Architect + Backend | ✅ DONE | `PLAYBOOK/benchmark-percentile-design.md`, doc/contract truth pass, COMPLETION_LOG RE-004 |
| RE-005 | Add `promptfoo` harness skeleton for prompt regression tests against representative report/retrieval cases | QA + Backend | ✅ DONE | `eval/promptfoo/*`, `scripts/run-promptfoo.cjs`, `package.json`, COMPLETION_LOG RE-005 |
| RE-006 | Add first `promptfoo` suites for executive summary quality, fix specificity, and status preservation | QA | ✅ DONE | `eval/promptfoo/promptfooconfig.report.yaml`, COMPLETION_LOG RE-006 |
| RE-007 | Evaluate `ragas` fit for retrieval faithfulness / answer relevance; produce go/no-go note before implementation | Architect + QA | ✅ DONE | `PLAYBOOK/ragas-fit-evaluation.md`, COMPLETION_LOG RE-007 |
| RE-008 | If approved, add `ragas`-based offline eval pipeline for retrieval runs and answer faithfulness | QA + Backend | ⬜ PENDING | — |
| RE-009 | Add prompt-cluster / demand-layer research backlog item with clear dependency on retrieval foundation | Architect | ⬜ PENDING | — |
| RE-010 | Add citation / share-of-voice benchmarking backlog item with external-source methodology and limits | Architect + Backend | ⬜ PENDING | — |
| RE-011 | Add stable eval-run identity for site-level history (`framework`, `domain`, `site_url`, prompt-set metadata) | Backend + Database | ✅ DONE | `011_eval_run_metadata.sql`; COMPLETION_LOG RE-011 … RE-015 |
| RE-012 | Persist Promptfoo runs into Supabase for admin analytics | Backend | ✅ DONE | `scripts/promptfoo-eval-write.ts`, `lib/server/promptfoo-results.ts`; COMPLETION_LOG RE-011 … RE-015 |
| RE-013 | Upgrade `/dashboard/evals` into site/framework trend analytics across report + retrieval runs | Frontend | ✅ DONE | `app/dashboard/evals/page.tsx`; COMPLETION_LOG RE-011 … RE-015 |
| RE-014 | Add Promptfoo result parsing helpers + metrics needed for trend cards and run tables | Backend + QA | ✅ DONE | `lib/server/promptfoo-results.ts`, `lib/server/promptfoo-results.test.ts`; COMPLETION_LOG RE-011 … RE-015 |
| RE-015 | Add admin/internal RAGAS lane placeholder without shipping a runtime RAGAS pipeline | Architect + Frontend | ✅ DONE | `/dashboard/evals` framework filter + empty-state note; COMPLETION_LOG RE-011 … RE-015 |
| RE-016 | If approved, add `ragas`-based offline writer + persistence into retrieval analytics | QA + Backend | ⬜ PENDING | — |
| RE-017 | Add deterministic retrieval-eval writer that persists aggregate run + prompt / passage / answer rows | Backend + QA | ✅ DONE | `scripts/retrieval-eval-write.ts`, `lib/server/retrieval-eval-writer.ts`; COMPLETION_LOG RE-017 |
| RE-018 | Add retrieval-run admin drilldown and make landing header auth-aware (`Dashboard` only when logged in) | Frontend + Backend | ✅ DONE | `app/dashboard/evals/retrieval/[id]/page.tsx`, `components/site-header.tsx`; COMPLETION_LOG RE-018 |

### API-as-a-Service Layer (deferred — start after Phase 4 launch readiness)
> **Phase 3→4 gate** is satisfied (auth + dashboard in repo). **API-002 … API-007** remain deferred until **Orchestrator** clears **Phase 4 — Launch** (at minimum **P4-001** production deploy + security evidence per `ORCHESTRATOR.md` Phase 4→Launch gate). **API-001** remains complete.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| API-001 | API key schema + migration | Database | ✅ DONE | `002_api_keys.sql` on remote; COMPLETION_LOG Supabase audit |
| API-002 | API key issuance + validation Worker | API | ⬜ DEFERRED | Starts after Phase 4 launch gate (Orchestrator) |
| API-003 | OpenAPI spec v1 | API + Architect | ⬜ DEFERRED | Starts after Phase 4 launch gate |
| API-004 | Rate limiting per API key | API + Security | ⬜ DEFERRED | Starts after Phase 4 launch gate |
| API-005 | API billing tiers (free/pro/enterprise) | API | ⬜ DEFERRED | Starts after Phase 4 launch gate |
| API-006 | Webhook delivery for async results | API | ⬜ DEFERRED | Starts after Phase 4 launch gate |
| API-007 | API documentation site | API + Frontend | ⬜ DEFERRED | Starts after Phase 4 launch gate |

### Measurement Platform (parallel planning / staged internal foundation)
> **Vision source:** `PLAYBOOK/measurement-platform-roadmap.md` + `PLAYBOOK/v3 aretefacts/*`. This initiative keeps the current audit/report business intact while adding a future measurement layer for AI visibility. It may proceed as planning and tightly scoped internal foundation work while Phase 4 is externally blocked, but it does **not** replace launch closure or change API deferral truth.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| BM-001 | Freeze architectural split: current app vs future measurement layer; record non-goals and staged rollout | Architect | ✅ DONE | `PLAYBOOK/measurement-platform-roadmap.md` |
| BM-002 | Define first benchmark schema set (`benchmark_domains`, `benchmark_query_sets`, `benchmark_queries`, `benchmark_run_groups`, `query_runs`, `query_citations`, `benchmark_domain_metrics`) | Architect + Database | ✅ DONE | `PLAYBOOK/measurement-schema-v1.md` |
| BM-003 | Add LiteLLM integration plan and provider abstraction boundaries | Backend + Architect | ✅ DONE | `PLAYBOOK/litellm-integration-plan.md` |
| BM-004 | Add Langfuse integration plan for traces, datasets, and eval experiments | Backend + Architect | ✅ DONE | `PLAYBOOK/langfuse-integration-plan.md` |
| BM-005 | Design internal benchmark runner for one domain / one query set / one model lane | Backend | ✅ DONE | `PLAYBOOK/benchmark-runner-v1.md` |
| BM-006 | Define citation extraction v1 contract and metric computation v1 (`citation_rate`, `query_coverage`, `share_of_voice`) | Architect + Backend | ✅ DONE | `PLAYBOOK/citation-and-metrics-v1.md` |
| BM-007 | Design internal admin benchmark views and run controls before any customer-facing benchmark UI | Frontend + Product | ✅ DONE | `PLAYBOOK/benchmark-admin-ui-v1.md` |
| BM-008 | Define phased scale path: 20–50 domains → 100–200 → 500–1000 with isolation from customer flows | Architect + Ops | ✅ DONE | `PLAYBOOK/benchmark-scale-path.md` |
| BM-009 | Add first benchmark foundation migration for internal domains, query sets, run groups, query runs, citations, and aggregate metrics | Backend + Database | ✅ DONE | `supabase/migrations/012_benchmark_foundation.sql` |
| BM-010 | Add benchmark-domain normalization helpers and first typed DB access layer | Backend | ✅ DONE | `lib/server/benchmark-domains.ts`, `lib/server/benchmark-repository.ts` |
| BM-011 | Build first benchmark query-set/domain seeding path and runner input contract | Backend | ✅ DONE | `scripts/benchmark-seed.ts`, `lib/server/benchmark-seed.ts`, `lib/server/benchmark-runner-contract.ts` |
| BM-012 | Build first benchmark runner skeleton for one domain / one query set / one model lane without model execution yet | Backend | ✅ DONE | `lib/server/benchmark-runner.ts`, `scripts/benchmark-runner.ts` |
| BM-013 | Add provider-facing benchmark model response contract and stub execution adapter boundary | Backend | ✅ DONE | `lib/server/benchmark-execution.ts` |
| BM-014 | Add first benchmark citation extraction contract implementation and write `query_citations` rows from normalized responses | Backend | ✅ DONE | `lib/server/benchmark-citations.ts` |
| BM-015 | Add first real benchmark metric computation helper and separate it from runner orchestration | Backend | ✅ DONE | `lib/server/benchmark-metrics.ts` |
| BM-016 | Add benchmark-run admin query layer for run groups, query runs, citations, and domain history | Backend | ✅ DONE | `lib/server/benchmark-admin-data.ts` |
| BM-017 | Build `/dashboard/benchmarks` overview page with filters, summary cards, and recent run groups | Frontend + Backend | ✅ DONE | `app/dashboard/benchmarks/page.tsx` |
| BM-018 | Build `/dashboard/benchmarks/[runGroupId]` detail page for one benchmark run group | Frontend + Backend | ✅ DONE | `app/dashboard/benchmarks/[runGroupId]/page.tsx` |
| BM-019 | Build `/dashboard/benchmarks/domains/[domainId]` history page for one benchmark domain | Frontend + Backend | ✅ DONE | `app/dashboard/benchmarks/domains/[domainId]/page.tsx` |
| BM-020 | Add admin benchmark run trigger flow from the UI for one domain / one query set / one model lane | Frontend + Backend | ✅ DONE | `app/dashboard/benchmarks/actions.ts`, `components/benchmark-trigger-form.tsx` |
| BM-021 | Add first non-stub benchmark execution adapter path for a single configured model lane | Backend | ✅ DONE | `lib/server/benchmark-execution.ts`, `app/dashboard/benchmarks/actions.ts`, `scripts/benchmark-runner.ts` |
| BM-022 | Add benchmark-domain onboarding from the admin UI and align the default trigger lane with the configured live benchmark lane | Frontend + Backend | ✅ DONE | `components/benchmark-domain-form.tsx`, `app/dashboard/benchmarks/page.tsx`, `app/dashboard/benchmarks/actions.ts` |
| BM-023 | Add benchmark query-set onboarding from the admin UI for lightweight real-domain testing | Frontend + Backend | ✅ DONE | `components/benchmark-query-set-form.tsx`, `app/dashboard/benchmarks/actions.ts`, `app/dashboard/benchmarks/page.tsx` |
| BM-024 | Add light retry/backoff handling for temporary benchmark-provider overload (`503 UNAVAILABLE`) before marking a query run failed | Backend | ✅ DONE | `lib/server/benchmark-execution.ts` |
| BM-025 | Define grounded benchmark v2 methodology separating `ungrounded_inference` from `grounded_site` interpretation without skipping BM-024 sequencing | Architect + Backend | ✅ DONE | `PLAYBOOK/benchmark-grounding-v2.md` |
| BM-026 | Add the first grounded benchmark implementation slice: explicit run mode, grounding-context seam, and safe prompt-path gating without breaking ungrounded runs | Backend | ✅ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-runner.ts`, `lib/server/benchmark-execution.ts`; COMPLETION_LOG BM-026 |
| BM-027 | Improve grounded benchmark citation recovery by prompting explicit target-domain mention and validating alias-based citation parsing | Backend | ✅ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-citations.test.ts`; COMPLETION_LOG BM-027 |
| BM-028 | Define the next grounded benchmark design step: structured page-level provenance (`page_url`, `page_type`, `excerpt`) so grounded answers can be tied to exact source pages rather than a loose evidence blob | Architect + Backend | ✅ DONE | `PLAYBOOK/benchmark-grounding-v2.md`, `PLAYBOOK/measurement-platform-roadmap.md`; COMPLETION_LOG BM-028 |
| BM-029 | Adopt the synthesized grounded-site production prompt from cross-model prompt research and replace the earlier benchmark prompt wording | Backend | ✅ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-grounding.test.ts`; COMPLETION_LOG BM-029 |
| BM-030 | Add the first structured grounding-provenance implementation slice: normalize page-level evidence, persist a run-time grounding snapshot, and expose it in benchmark run detail without breaking older evidence blobs | Backend + Frontend | ✅ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-runner.ts`, `app/dashboard/benchmarks/[runGroupId]/page.tsx`; COMPLETION_LOG BM-030 |
| BM-031 | Add the first minimal site-derived grounding-context builder so grounded runs can derive evidence from homepage/about/services pages when manual evidence is absent | Backend | ✅ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-runner.ts`; COMPLETION_LOG BM-031 |
| BM-032 | Add the first exact-url grounded citation provenance slice so citation rows preserve matched source-page evidence without guessing unresolved mentions | Backend + Frontend | ✅ DONE | `supabase/migrations/014_benchmark_citation_grounding_provenance.sql`, `lib/server/benchmark-citations.ts`, `lib/server/benchmark-runner.ts`, `components/benchmark-run-detail-view.tsx`; COMPLETION_LOG BM-032 |
| BM-033 | Define the next grounded-provenance sequence so future work stays narrow: ranked page selection, excerpt-level evidence matching, citation-quality scoring, and scale work must remain separate slices with explicit non-goals | Architect + Backend | ✅ DONE | `PLAYBOOK/benchmark-grounding-v2.md`, `PLAYBOOK/measurement-platform-roadmap.md`; COMPLETION_LOG BM-033 |
| BM-034 | Add a bounded ranked grounding-candidate selector for grounded runs so the builder prefers the strongest few same-origin pages instead of only homepage/about/services heuristics | Backend | ✅ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-grounding.test.ts`; COMPLETION_LOG BM-034 |
| BM-035 | Persist richer grounding-page metadata (`fetch_order`, `selection_reason`, page title/status) for grounded evidence without changing the current benchmark admin flow shape | Backend + Database | ✅ DONE | `lib/server/benchmark-grounding.ts`, `lib/server/benchmark-run-detail.ts`, `components/benchmark-run-detail-view.tsx`; COMPLETION_LOG BM-035 |
| BM-036 | Add a second provenance matcher that can resolve citations to grounded evidence by normalized page equivalence beyond exact URL string match while keeping unresolved cases explicit | Backend | ✅ DONE | `lib/server/benchmark-citations.ts`, `lib/server/benchmark-citations.test.ts`; COMPLETION_LOG BM-036 |
| BM-037 | Add excerpt-level claim-to-evidence matching for grounded runs as internal metadata only, without turning it into a customer-facing score | Backend + QA | ✅ DONE | `lib/server/benchmark-citations.ts`, `lib/server/benchmark-runner.ts`, `lib/server/benchmark-citations.test.ts`; COMPLETION_LOG BM-037 |
| BM-038 | Define and implement the first exact-page citation-quality metric slice for grounded runs, separated from citation presence and share-of-voice | Architect + Backend | ✅ DONE | `lib/server/benchmark-metrics.ts`, `lib/server/benchmark-runner.ts`, `components/benchmark-run-detail-view.tsx`; COMPLETION_LOG BM-038 |
| BM-039 | Add benchmark comparison views for the same domain/query set across `ungrounded_inference` and `grounded_site` so methodology gains are inspectable over time | Frontend + Backend | ✅ DONE | `app/dashboard/benchmarks/domains/[domainId]/page.tsx`, `lib/server/benchmark-admin-data.ts`, `lib/server/benchmark-admin-data.test.ts`; COMPLETION_LOG BM-039 |
| BM-040 | Add recurring internal benchmark scheduling and run-label/version discipline so benchmark history is comparable without using the customer request path | Backend + Ops | ✅ DONE | `lib/server/benchmark-schedule.ts`, `workers/cloudflare-entry.ts`, `lib/server/benchmark-runner.ts`; COMPLETION_LOG BM-040 |
| BM-041 | Define competitor/cohort benchmark methodology, guardrails, and evidence limits before any broader comparative benchmark claims or cohort UI are implemented | Architect + Product | ✅ DONE | `PLAYBOOK/benchmark-competitor-cohort-methodology-v1.md`; COMPLETION_LOG BM-041 |
| BM-042 | Add the first competitor/cohort benchmark storage and admin slice for a narrow internal cohort only, after BM-041 freezes methodology | Backend + Frontend | ✅ DONE | `supabase/migrations/015_benchmark_cohorts.sql`, `app/dashboard/benchmarks/domains/[domainId]/page.tsx`; COMPLETION_LOG BM-042 |
| BM-043 | Add the first benchmark query/response/citation lineage view so one run can be inspected as a real benchmark graph rather than disconnected tables | Backend + Frontend | ✅ DONE | `components/benchmark-run-detail-view.tsx`, `lib/server/benchmark-run-detail.ts`; COMPLETION_LOG BM-043 |
| BM-044 | Add the first multi-model benchmark lane beyond the current single configured live lane while keeping provider abstraction and admin controls simple | Backend | ✅ DONE | `lib/server/benchmark-execution.ts`, `app/dashboard/benchmarks/page.tsx`, `docs/06-environment-and-secrets.md`; COMPLETION_LOG BM-044 |
| BM-045 | Add benchmark-scale operator hardening for 100–200 domain internal runs with explicit isolation, retry policy, and failure visibility before any 500–1000 site push | Backend + Ops | ✅ DONE | `lib/server/benchmark-schedule.ts`, `lib/server/benchmark-schedule.test.ts`, `docs/06-environment-and-secrets.md`; COMPLETION_LOG BM-045 |
| BM-046 | Revisit the 500–1000 site benchmark operations path only after BM-045 evidence exists and operator cost/failure tradeoffs are documented | Architect + Ops | ✅ DONE | `PLAYBOOK/benchmark-operations-decision-v1.md`, `PLAYBOOK/benchmark-scale-path.md`; COMPLETION_LOG BM-046 |

### Architecture Hardening + Ops Observability
| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| AH-001 | Define the architecture hardening backlog with explicit slices for lean-code discipline, observability, and maintenance risk reduction | Architect + Backend | ✅ DONE | `PLAYBOOK/architecture-hardening-backlog.md`; COMPLETION_LOG AH-001 |
| AH-002 | Add persistent structured application logs so internal events can be inspected without backend console access | Backend + Database | ✅ DONE | `supabase/migrations/013_app_logs.sql`, `lib/server/structured-log.ts`; COMPLETION_LOG AH-002 |
| AH-003 | Add `/dashboard/logs` as an admin-only internal logs viewer over persisted structured logs | Frontend + Backend | ✅ DONE | `app/dashboard/logs/page.tsx`, `lib/server/admin-logs-data.ts`; COMPLETION_LOG AH-003 |
| AH-004 | Add Playwright readiness guidance so future browser-level testing can land on stable UI and operator flows | Architect + QA | ✅ DONE | `PLAYBOOK/architecture-hardening-backlog.md`; COMPLETION_LOG AH-004 |
| AH-005 | Add the first thin-route cleanup slice by extracting a small shared admin runtime helper for repeated auth/env/service-role setup | Backend | ✅ DONE | `lib/server/admin-runtime.ts`, admin dashboard pages, `app/dashboard/benchmarks/actions.ts`; COMPLETION_LOG AH-005 |
| AH-006 | Add the first large-module decomposition slice by reducing the benchmark run-detail route to a thin shell and moving detail rendering/helpers into dedicated modules | Backend + Frontend | ✅ DONE | `app/dashboard/benchmarks/[runGroupId]/page.tsx`, `components/benchmark-run-detail-view.tsx`, `lib/server/benchmark-run-detail.ts`; COMPLETION_LOG AH-006 |
| AH-007 | Document the admin-auth maturity path from the current env-based founder-stage gate to a future table-driven admin allowlist / role model without changing runtime auth behavior yet | Architect + Security | ✅ DONE | `PLAYBOOK/architecture-hardening-backlog.md`, `lib/server/require-admin.ts`, `app/admin/login/actions.ts`; COMPLETION_LOG AH-007 |
| AH-008 | Add the first open-source Playwright browser smoke-test foundation and wire it into CI for narrow public-entry-flow coverage | QA + Frontend | ✅ DONE | `playwright.config.ts`, `tests/e2e/smoke.spec.ts`, `.github/workflows/ci.yml`; COMPLETION_LOG AH-008 |
| AH-009 | Add the second large-module decomposition slice by reducing `components/report-viewer.tsx` to a shell and moving helpers/sections into dedicated modules with browser proof | Backend + Frontend | ✅ DONE | `components/report-viewer.tsx`, `components/report-viewer-sections.tsx`, `lib/client/report-viewer.ts`; COMPLETION_LOG AH-009 |

---

| AH-010 | Add the third large-module decomposition slice by reducing the benchmark overview route to a thin shell and moving overview rendering/helpers into dedicated modules with existing browser proof retained | Backend + Frontend | ✅ DONE | `app/dashboard/benchmarks/page.tsx`, `components/benchmark-overview-view.tsx`, `lib/server/benchmark-overview.ts`; COMPLETION_LOG AH-010 |

| AH-011 | Add the fourth large-module decomposition slice by extracting shared deep-audit report preparation helpers out of the PDF builder without changing report behavior or adding UI churn | Backend | ✅ DONE | `workers/report/deep-audit-report-helpers.ts`, `workers/report/build-deep-audit-pdf.ts`, `workers/report/build-deep-audit-markdown.ts`; COMPLETION_LOG AH-011 |

| AH-012 | Add the fifth large-module decomposition slice by extracting crawl-pending parsing and chunk-window planning into a dedicated state helper module without changing crawl behavior | Backend | ✅ DONE | `workers/scan-engine/deep-audit-crawl-state.ts`, `workers/scan-engine/deep-audit-crawl.ts`; COMPLETION_LOG AH-012 |
| AH-013 | Add the sixth large-module decomposition slice by extracting shared email-delivery composition helpers out of the Resend path without changing delivery behavior or adding UI churn | Backend | ✅ DONE | `workers/report/resend-delivery-helpers.ts`, `workers/report/resend-delivery.ts`; COMPLETION_LOG AH-013 |

### Report Design — Phase A (audit-defensible narrative upgrade)
> **Goal:** Upgrade the paid report from commodity-SEO-audit framing to a CRO-level intelligence document that a full team can act on — without introducing numeric revenue claims or live competitor data we cannot support. Phase A is docs/design only. No runtime report generation changes. No PDF/UI changes. No rewriter default change.
> **Guardrails:** Keep all new framing audit-defensible. One slice at a time. Lean architecture. Low maintenance.
> **Spec:** `PLAYBOOK/rd-001-team-owner-taxonomy-v1.md` and future RD PLAYBOOK files.

| Task ID | Task | Agent | Status | Evidence |
|---------|------|-------|--------|----------|
| RD-001 | Team-owner taxonomy: map all 22 implemented checks to 4 owners (Engineering / Content / Brand / Product) | Architect | ✅ DONE | `PLAYBOOK/rd-001-team-owner-taxonomy-v1.md`; COMPLETION_LOG RD-001 |
| RD-002 | Executive Brief contract: headline framing, 3-bullet summary, #1 action, directional exposure language (no numeric estimates) | Architect | ✅ DONE | `PLAYBOOK/rd-002-executive-brief-contract-v1.md`; COMPLETION_LOG RD-002 |
| RD-005 | Section order contract update: Executive Brief → Immediate Wins → Team Action Map → Score Summary → Best Practices → Coverage → Appendix | Architect | ✅ DONE | `PLAYBOOK/rd-005-section-order-contract-v1.md`; COMPLETION_LOG RD-005 |
| RD-006 | Immediate Wins format: implementation-ticket style, not audit-finding style | Architect | ✅ DONE | `PLAYBOOK/rd-006-immediate-wins-format-v1.md`; COMPLETION_LOG RD-006 |
| RD-004 | "What AI-Ready Leaders Do Differently": audit-derived best-practice framing only, no live competitor data | Architect | ✅ DONE | `PLAYBOOK/rd-004-ai-ready-leaders-contract-v1.md`; COMPLETION_LOG RD-004 |
| RD-003 | Directional business exposure language placeholder: template language only, no formula, no numbers; marked pending data layer | Architect | ⬜ DEFERRED | Holds until RD-002 through RD-006 narrative is validated on real reports |
| RD-007 | Standalone team-owner mapping artifact: `TeamOwner` type, `TEAM_OWNER_MAP`, `getTeamOwner` helper + test | Backend | ✅ DONE | `workers/report/team-owner-map.ts`, `workers/report/team-owner-map.test.ts`; type-check 0 errors; 5/5 tests pass; COMPLETION_LOG RD-007 |

---

## Active Blockers

- `P4-003` operator blocker: domain purchase / DNS setup paused due to founder credit-card issue — 2026-03-26
- `P4-006` blocked on `P4-003` DNS evidence plus final security sign-off — 2026-03-26
- `P4-004` WAF remains a launch-policy decision (`DEFERRED / mitigated` in repo, but not fully closed operationally)

---

## Architecture Decisions Log (summary)
> Full ADRs in `agents/memory/DECISIONS.md`

| ID | Decision | Status |
|----|----------|--------|
| ADR-001 | Use `@opennextjs/cloudflare`, not deprecated `next-on-pages` | ✅ Final |
| ADR-002 | pdf-lib for PDF generation (not Puppeteer) | ✅ Final |
| ADR-003 | Gemini 2.5 Flash-Lite as LLM (provider is a config var) | ✅ Final |
| ADR-004 | Supabase RLS on all tables; leads = service_role only | ✅ Final |
| ADR-005 | n8n self-hosted on Oracle Cloud Free (not Make.com) | ✅ Final |
| ADR-006 | API-first architecture: GEO-Pulse is an API with a UI on top | ✅ Final |

---

## State history
| Date | Change |
|------|--------|
| 2026-03-24 | Phase 0 scaffold gate closed; **Current Phase → Phase 1 — Core Scan Engine**. P0-002…P0-006 ✅; API-001 ✅ (remote matches `002_api_keys.sql`). |
| 2026-03-24 | **Phase 1 — Core Scan Engine** implementation complete; **Current Phase → Phase 2 — Payment + PDF + Email**. P1-001…P1-011 ✅ (see COMPLETION_LOG). |
| 2026-03-24 | **Phase 1→2 manual gate:** live scan + results + email capture reported successful (domain: techehealthservices.com). |
| 2026-03-24 | **Sequencing:** API-as-a-Service (**API-002…007**) deferred until **Phase 2 + Phase 3** complete and **Phase 3→4 gate** satisfied (`ORCHESTRATOR.md`). Operator: $1 payment e2e reported working; Phase 2 task registry + COMPLETION_LOG evidence still to be closed out. |
| 2026-03-24 | **Phase 2 + Phase 3** implementation landed (auth, dashboard, `guest_email` on payments, middleware). **Current Phase → Phase 4 — Launch**. P2-001…P2-007 ✅; P2-008 operator QA; P3-001…P3-004 ✅. Run `supabase db push` for `004_payments_guest_email.sql`. |
| 2026-03-24 | **Phase 4 (partial):** P4-002 OG + P4-005 keep-alive cron **✅ in repo** (`COMPLETION_LOG.md` Phase 4 bundle). P4-001/P4-003/P4-004/P4-006 **pending** operator + Security. API-002…007 deferral clarified → **after Phase 4 launch gate**. |
| 2026-03-24 | **Deep Audit Upgrade** initiative added to Task Registry (**DA-001…DA-005**). Spec: `.cursor/plans/report_depth_and_formats_fa7e556e.plan.md`; companion: [`PLAYBOOK/audit-upgrade.md`](../../PLAYBOOK/audit-upgrade.md). Sequencing vs Phase 4: Orchestrator call (parallel vs after P4 operator work). |
| 2026-03-24 | **PLAYBOOK:** Renamed `audit uprade.md` → [`audit-upgrade.md`](../../PLAYBOOK/audit-upgrade.md); links in this file updated. |
| 2026-03-25 | **Checkout rate limit + DA-001:** Operator note — `POST /api/checkout` capped at **5/IP/hour** (wait or alternate IP). **DA-001** marked **IN_PROGRESS**; parallel with Phase 4 operator work per `agents/ORCHESTRATOR.md`. |
| 2026-03-25 | **DA-001 (implementation):** `scan_runs` / `scan_pages` migration + RLS; paid flow creates `scan_run` and enqueues **v2**; Worker runs capped same-origin crawl, updates `scans` aggregate + PDF multi-page. **Pending:** remote DB migration + Orchestrator acceptance + optional Security note (fetch path unchanged from P1 `fetch-page` until DA-002). |
| 2026-03-25 | **DA-001 — CLOSED (Orchestrator):** Paid deep-audit smoke on **techehealthservices.com** — multi-page PDF (10 URLs, aggregate score, per-page checklist). **DA-002** marked **IN_PROGRESS** (central fetch gate + robots/sitemap + sampling; Security review on fetch/SSRF). |
| 2026-03-25 | **DA-002 — DONE (implementation):** Central fetch gate (`fetchGateText`, stream byte cap), `validateEngineFetchUrl` (http/https for engine only), robots.txt + sitemap.xml discovery, section-aware URL ordering, `scan_pages.section`; free scan still uses HTTPS-only `validateUrl`. **Next:** **DA-003**. |
| 2026-03-25 | **DA-004 (incremental):** `parseRobotsTxt` reads `Crawl-delay` for `*`; `deep-audit-crawl` sleeps between non-seed fetches (cap 10s); `scan_runs.coverage_summary` adds `wall_time_ms`, `pages_errored`, `crawl_delay_ms`; `structuredLog` `deep_audit_crawl_complete`. Full DA-004 (Workflows / 100+ pages) still deferred. |
| 2026-03-25 | **Orchestrator sequencing:** Complete **Phase 4 → Launch** (P4-001, P4-003, P4-004, P4-006 + evidence) **before** resuming **DA-004** remainder / **DA-005** — see `agents/ORCHESTRATOR.md` § *Phase 4 first — defer remaining Deep Audit until Launch gate*. |
| 2026-03-25 | **Phase 4 — P4-001 + P2-008:** Production **`https://geo-pulse.uzzielt.workers.dev`** — live Stripe checkout success return path verified. **P4-003 / P4-004 / P4-006** still need evidence in `COMPLETION_LOG.md` for full Launch gate. |
| 2026-03-25 | **DA-004 (ops):** `DEEP_AUDIT_DEFAULT_PAGE_LIMIT` plaintext var in `wrangler.jsonc` + `getPaymentApiEnv` — default page cap for new paid `scan_runs`; `lib/server/deep-audit-page-limit.ts` single source for max + parsing. |
| 2026-03-26 | **UCD Report Journey Overhaul (5 phases) — COMPLETE.** Phase 1: 4 differentiating checks (AI crawler access, llms.txt, snippet eligibility, E-E-A-T signals) + `CheckContext` extended + weights rebalanced to 100 across 17 checks. Phase 2: PDF report rebuilt — branded cover, exec summary, score breakdown table, priority action plan, per-page breakdown, branded footer. Phase 3: Delivery email rebuilt — branded header, score badge, top 3 priorities w/ severity, CTA button, inline CSS. Phase 4: User-centered language rewrite across all components. Phase 5: Results page restructured — score hero → category health grid (4 pillars) → upgrade strip (dark) → numbered issues w/ severity chips → email gate → rescan link. Evidence: type-check 0 errors, 50/50 tests passed, build success. |
| 2026-03-26 | **Marketing Attribution Microservice (MA-001 → MA-004) — COMPLETE.** Parallel build: `analytics.marketing_events` (007), Zod contract + idempotent ingestion (39 tests), client UTM/session capture (`gp_anon_id` cookie + `gp_utm` sessionStorage), emits from scan/leads/checkout/webhook, first-touch/last-touch views (008), weekly funnel aggregate, Monday cron email (opt-in `MARKETING_REPORT_TO`). `leads.scan_id` now persisted. MA-005 (queue hardening) deferred. Evidence: type-check 0 errors, 89/89 tests, 0 lint errors. |
| 2026-03-26 | **Admin & report eval (ADM-001 … EVAL-004) — implementation in repo.** `/admin/login` password for `ADMIN_EMAIL`, `report_eval_runs` + `reports.markdown_url`, `/dashboard/evals`, `npm run eval:smoke`. See Task Registry **Admin & report quality eval** + `COMPLETION_LOG.md`. **Phase 4** still blocked on P4-003 / P4-006 operator evidence per `ORCHESTRATOR.md`. |
| 2026-03-26 | **P4-004 / P4-006 (tests):** Unit tests for CVE-2025-29927 middleware guard (`lib/server/middleware-cve.ts`, `middleware-cve.test.ts`) — supplements `middleware.ts` belt-and-suspenders check; paste Vitest output into `COMPLETION_LOG` for Security review. |
| 2026-03-26 | **v2 Enhancement Plan — Tier 1 COMPLETE.** T1-1: `CheckStatus` 6-value enum + `CheckCategory` 5-value type in `audit.ts`. T1-2: 17 checks mapped to categories (ai_readiness, extractability, trust). T1-3: `computeCategoryScores` in `scoring.ts` with v2 weighting (BLOCKED/NOT_EVALUATED excluded; LOW_CONFIDENCE = 50%). T1-4: `score-display.tsx` 5-pillar category grid (replaces 4-pillar). T1-5: category breakdown table in Markdown + PDF reports. LLM checks now carry `confidence` + `status: LOW_CONFIDENCE` when confidence is low. Evidence: type-check 0 errors, 96/96 tests pass. |
| 2026-03-26 | **Task ledger reconciliation:** v2 plan pointer in `PROJECT_STATE.md` marked stale (referenced `.cursor/plans/...` file not present in repo). Task statuses corrected for implemented checks/views: **T2-1…T2-6** and **T3-6** set to **DONE** based on shipped code. Added explicit bite-size backlog sections: **Audit Closure — Report Integrity + Product Truth** (**AU-001…AU-010**) and **Retrieval / Evaluation Backlog** (**RE-001…RE-010**) including `promptfoo` and `ragas` as backlog items only, not implemented features. |
| 2026-03-26 | **AU-001 … AU-005 / AU-007 — COMPLETE (implementation).** Deep-audit reports now separate `highlightedIssues` from `allIssues`, render the full sitewide breakdown, preserve v2 statuses in Markdown/PDF, keep `full_results_json` aligned with `categoryScores` + report metadata, expose coverage summary in customer-facing report output, and remove inaccurate checkout copy about exact check count. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **AU-009 / AU-010 — COMPLETE (implementation).** Report eval moved from shallow structural checks to content-integrity assertions, and golden fixtures now cover multi-page output plus blocked / low-confidence / not-evaluated statuses. `scripts/report-eval-smoke.ts` now uses rubric version `integrity-v2`. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-001 / RE-002 — COMPLETE (foundation).** Added retrieval-eval scope doc (`PLAYBOOK/retrieval-eval-foundation.md`) and schema scaffold (`010_retrieval_eval_foundation.sql`) for future retrieval simulation, `promptfoo`, and possible `ragas` adoption. No runtime retrieval harness implemented yet. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **AU-006 / AU-008 — COMPLETE (implementation).** Deep-audit reports now include a structured technical appendix in Markdown/PDF, and the SSRF/security docs were updated to match the implemented protection model and Cloudflare Workers runtime limitations. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-003 — COMPLETE (foundation).** Added deterministic retrieval simulation helpers in `lib/server/retrieval-eval.ts` with passage extraction, lexical ranking, and prompt-level metrics. This is an offline harness only; no `promptfoo`, `ragas`, or live engine integration yet. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-004 — COMPLETE (design / truth pass).** Added a benchmark-percentile design note and removed unsupported percentile claims from the current API contract, PRD report promise, OG-image guidance, and audit-upgrade metrics. Percentile output remains deferred until cohorting, snapshots, and guardrails exist. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-005 — COMPLETE (implementation).** Added a repo-local `promptfoo` skeleton with report and retrieval suites, custom local providers, a repo-scoped runner to avoid user-profile writes, package scripts, and `.promptfoo/` ignore. This is a regression harness only; `ragas` remains pending. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-006 / RE-007 — COMPLETE (implementation / evaluation).** Promptfoo report coverage now checks executive summary extraction, fix specificity, and status preservation. Added a formal `ragas` fit note with a current no-go decision until the retrieval dataset and answer-generation layer are mature enough to justify semantic scoring. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-011 … RE-015 — COMPLETE (implementation).** Eval analytics now carry stable site/framework metadata via `011_eval_run_metadata.sql`; Promptfoo runs can be written into Supabase with `scripts/promptfoo-eval-write.ts`; `/dashboard/evals` is now a site-centric admin analytics view spanning report + retrieval runs with framework filters, trend views, and an explicit RAGAS placeholder lane. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-017 — COMPLETE (implementation).** Added a deterministic retrieval writer over the existing foundation: fixture-driven site pages + prompts are simulated, persisted into `retrieval_eval_runs`, `retrieval_eval_prompts`, `retrieval_eval_passages`, and `retrieval_eval_answers`, and can now support future admin drilldown. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **RE-018 — COMPLETE (implementation).** Added a retrieval-run admin drilldown page from `/dashboard/evals` and made the landing header session-aware so `Dashboard` only appears when a user is logged in. Signed-in users now see `Dashboard` + `Sign out`; signed-out users see only sign-in actions. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **DA-004 (incremental resume):** Added chunk-progress metrics and continuation guardrails to `crawl_pending` / `coverage_summary`, surfaced chunk metrics in Markdown/PDF coverage output, raised the deep-audit cap to **1000** pages, and aligned final coverage reporting with the configured chunk size. |
| 2026-03-26 | **DA-004 — COMPLETE (user-directed reopen).** Added a targeted `deep-audit-crawl` Vitest that exercises partial requeue, `crawl_pending`, continuation, and final coverage metrics for a multi-chunk run. `npm.cmd run type-check` passed and `npx.cmd vitest run workers/scan-engine/deep-audit-crawl.test.ts` passed (7/7). Workflows is now tracked as optional future scale work, not DA-004 remainder. |
| 2026-03-26 | **UX-001 — COMPLETE (implementation).** Added a centralized delayed long-wait overlay with animated step progression and wired it into scan submit, checkout, email save, login, admin login, results load, and report load. Fast-path buttons still use local pending labels; the overlay only appears after a delay for longer waits. `npm.cmd run type-check` and `npm.cmd run build` passed. |
| 2026-03-26 | **P4-006 repo evidence refreshed.** Added targeted Turnstile and Stripe webhook route tests, then re-ran the Phase 4 security Vitest slice (`middleware-cve`, `turnstile`, `ssrf`, `checkout-completed`, webhook route): 5 files / 25 tests passed. `npm.cmd run type-check` and `npm.cmd run build` passed. `P4-006` remains pending until operator DNS evidence (`P4-003`) and final security sign-off are attached. |
| 2026-03-26 | **Phase 4 operator pause recorded.** Founder could not complete domain purchase because of a credit-card issue. `P4-003` is now explicitly operator-blocked, and final `P4-006` sign-off remains pending on that DNS evidence. Repo-side security/test work is current; revisit once domain purchase can be completed. |
| 2026-03-26 | **UX journey clarity stream opened.** User testing showed that the scan → preview → pay / save → report flow is functionally working but not communicating clear incremental steps. Added `UX-002` ... `UX-007` to track canonical journey design, state-driven status, and unified loading/report messaging while Phase 4 operator work is externally blocked. |
| 2026-03-26 | **UX-002 ... UX-006 — COMPLETE (implementation).** Results now present a single guided audit journey: preview first, full audit second, preview-save as the subtle alternative. Query-string-only checkout success messaging was removed in favor of results-page state derived from `hasPaidReport` / `reportStatus`, and the long-wait copy now frames preliminary and full audit generation as one continuous process. `npm.cmd run type-check` passed; `npm.cmd run build` passed outside sandbox after the usual `spawn EPERM` retry. |
| 2026-03-26 | **UX-007 — COMPLETE (verification).** Added a pure state-matrix test for the results journey (`lib/client/results-journey.test.ts`) covering preview-ready, checkout-cancelled, checkout-returned awaiting confirmation, generating, and delivered states. Verification notes recorded that both guest/public and signed-in results paths feed the same `hasPaidReport` / `reportStatus` shape into `ResultsView`, so the UX state model is shared across both flows. `npm.cmd run type-check` passed; targeted Vitest passed outside sandbox (1 file / 5 tests). |
| 2026-03-26 | **T3-7 — COMPLETE (implementation).** `components/report-viewer.tsx` now renders an interactive report shell above the markdown artifact: score/category summary, top-issue snapshot, mobile section chips, and collapsible report sections split from markdown `##` headings. Route ownership remains in `app/results/[id]/report/page.tsx`; existing PDF + raw markdown delivery path is preserved. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **DA-005 — COMPLETE (implementation).** Added an optional Browser Rendering-backed SPA fallback for paid deep audits: Browser Rendering client + heuristics, deep-audit crawl metrics for render attempts/success/failure, queue/run-config wiring, and security/operator docs. Disabled by default; this is not a full `/crawl` orchestration layer. Evidence logged in `COMPLETION_LOG.md`. |
| 2026-03-26 | **Measurement platform initiative opened.** Strategic direction is now explicit: keep the audit/report product as the wedge and build the benchmark/measurement layer as a staged internal platform on top. Added `PLAYBOOK/measurement-platform-roadmap.md` and `BM-001` ... `BM-008` to the task registry so the work is documented before implementation starts. |
| 2026-03-26 | **BM-002 — COMPLETE (design).** Added `PLAYBOOK/measurement-schema-v1.md` defining the first benchmark schema set: canonical benchmark domains, query sets, benchmark run groups, raw query runs, extracted citations, and computed domain metrics. The spec freezes v1 identity rules, service-role-only access, first metric semantics, and clear out-of-scope items before any migration is written. |
| 2026-03-26 | **BM-003 — COMPLETE (design).** Added `PLAYBOOK/litellm-integration-plan.md`, which keeps the existing `LLMProvider` seam intact, introduces a staged LiteLLM-backed provider strategy, and defines provider-factory/config boundaries so audit/report flows remain stable while the future benchmark layer gains multi-model flexibility. |
| 2026-03-26 | **BM-004 — COMPLETE (design).** Added `PLAYBOOK/langfuse-integration-plan.md`, which scopes Langfuse to benchmark-layer traces, datasets, and experiments while keeping Supabase as the system of record and preserving Promptfoo/deterministic evals as primary foundations. |
| 2026-03-26 | **BM-005 — COMPLETE (design).** Added `PLAYBOOK/benchmark-runner-v1.md`, defining the first internal runner shape: one domain, one query set, one model lane, serial execution, conservative citation extraction, query-level failure tolerance, and structured logging around run-group lifecycle. |
| 2026-03-26 | **BM-006 — COMPLETE (design).** Added `PLAYBOOK/citation-and-metrics-v1.md`, which confirms no existing shared citation service conflicts with the new benchmark layer and defines the v1 citation classes, dedupe/confidence rules, and first metric formulas for `citation_rate`, `query_coverage`, and `share_of_voice`. |
| 2026-03-26 | **BM-007 — COMPLETE (design).** Added `PLAYBOOK/benchmark-admin-ui-v1.md`, which defines the first internal benchmark admin surfaces, keeps them aligned with the repo’s existing server-rendered admin pattern, and limits run controls to narrow internal operations before any customer-facing benchmark UI exists. |
| 2026-03-27 | **Benchmark milestone — first live benchmark verified.** Real domain (`techehealthservices.com`) + admin-authored query set executed successfully on `gemini-2.5-flash-lite` with 6 completed queries, 4 citations, and non-zero benchmark metrics. The benchmark foundation is now operational; next step is transient-provider retry handling (`BM-024`) rather than more scaffolding. |
| 2026-03-27 | **BM-024 — COMPLETE (implementation).** Added light bounded retry/backoff for transient Gemini overload (`429` / `503`) in `lib/server/benchmark-execution.ts`. Retries are limited to temporary provider failures only, store retry metadata in `response_metadata`, and do not mask hard failures like invalid keys or unsupported models. |
| 2026-03-27 | **BM-025 — COMPLETE (design).** Added `PLAYBOOK/benchmark-grounding-v2.md` to define the next benchmark-methodology evolution: separate `ungrounded_inference` from `grounded_site` interpretation, treat business-type misclassification as a real internal signal, and keep sequencing anchored to `BM-024` retry/backoff before protocol expansion. |
| 2026-03-27 | **BM-026 — COMPLETE (implementation).** Added the first grounded benchmark foundation without disturbing the live ungrounded path: `runMode` is now a first-class runner/admin/CLI contract, benchmark-domain metadata can optionally provide `grounding_context.evidence` for `grounded_site` runs, the execution adapter now builds mode-aware prompts, and grounded runs fail truthfully when evidence is missing instead of silently drifting. Default behavior remains `ungrounded_inference`, so this does not claim grounded benchmark maturity yet. |
| 2026-03-27 | **BM-027 — COMPLETE (implementation).** Tightened the grounded-site prompt so completed answers should mention the exact target domain when the evidence supports the company, giving the conservative citation extractor a deterministic hook. Added explicit alias-based citation coverage in tests so names like `Teche Consulting` can still map back to the measured domain when configured. |
| 2026-03-27 | **BM-028 — COMPLETE (design).** Added the next explicit roadmap step after the current grounded-mode foundation: move from a loose grounded evidence blob to structured page-level provenance with `page_url`, `page_type`, and `excerpt`, so future grounded answers can be judged against exact source pages instead of domain-level evidence only. |
| 2026-03-27 | **BM-029 — COMPLETE (implementation).** Replaced the grounded-site prompt with the synthesized production version derived from external prompt research: stronger `do not use outside knowledge` scoping, explicit anti-inference behavior, explicit paraphrasing guidance, softer natural domain mention, and a cleaner plain-text answer instruction. This keeps the current grounded mode aligned with the benchmark methodology docs before the next live comparison run. |
| 2026-03-27 | **BM-030 — COMPLETE (implementation).** Grounded benchmark runs now normalize either legacy evidence blobs or structured provenance evidence into one internal shape, persist a safe `grounding_evidence` snapshot in run-group metadata, and expose that snapshot on the benchmark run detail page. This is the first inspectable page-provenance slice only; it does not claim a live grounding-context builder or exact-page citation-quality scoring yet. |
| 2026-03-27 | **BM-031 — COMPLETE (implementation).** Grounded benchmark runs now have a first minimal site-derived grounding builder: when manual evidence is absent, the runner fetches the homepage plus likely about/services pages through the existing validated fetch gate, derives bounded excerpts, and records whether grounding came from metadata or the site builder. This does not claim full crawl coverage, ranked page selection, or exact-page citation scoring yet. |
| 2026-03-28 | **BM-032 — COMPLETE (implementation).** Grounded benchmark citations now preserve the first exact-page provenance slice: each grounded evidence record carries a deterministic `evidence_id`, citation rows store matched grounding page fields when the parsed citation URL exactly matches a grounded evidence page URL, and unresolved domain-only mentions remain explicitly unresolved. The admin run-detail view now shows whether each citation matched a grounded source page. This is intentionally conservative and does not claim excerpt-level matching, best-page inference, or exact-page citation scoring beyond exact URL equivalence. |
| 2026-03-28 | **BM-033 — COMPLETE (design).** Froze the next grounded-provenance implementation order before more code is added: first improve bounded page selection and inspectable grounding metadata, then widen provenance matching, then add excerpt-level evidence checks, then define an exact-page citation-quality metric, and only after that expose grounded-vs-ungrounded comparison UI. The non-goals are now explicit: no large benchmark rewrite, no guessed provenance on weak evidence, and no customer-facing benchmark claims from this sequence. |
| 2026-03-28 | **BM-034 — COMPLETE (implementation).** Replaced the earlier homepage/about/services-only grounding candidate picker with a bounded ranked selector over same-origin links. The builder still stays narrow and heuristic, but it now prefers higher-signal pages like about, services, and product/platform pages while skipping low-signal sections like blog/legal/contact when stronger options exist. This does not claim full crawl coverage, semantic best-page selection, or citation-quality scoring yet. |
| 2026-03-28 | **BM-035 — COMPLETE (implementation).** Grounded evidence snapshots now preserve richer page metadata for later provenance work: `fetch_order`, `selection_reason`, explicit page title, and fetch status are carried through the grounding context, serialized into run metadata, parsed back in the run-detail helper, and shown in the existing grounding evidence cards without changing the route structure. This remains internal inspection metadata only, not a customer-facing score or a new schema surface. |
| 2026-03-28 | **BM-036 — COMPLETE (implementation).** Added a second conservative citation-to-grounding matcher: page URLs can now match grounded evidence by normalized page equivalence, not just raw exact-string equality. The matcher strips weak URL differences such as `www`, trailing slash, fragment, default ports, and tracking query params, but it still refuses to guess across different paths or domain-only mentions. This remains provenance support metadata, not a semantic evidence scorer. |
| 2026-03-28 | **BM-037 — COMPLETE (implementation).** Grounded citation rows now carry an internal claim-to-evidence overlap signal in metadata. For matched grounded citations, the runner selects the best claim sentence from the response, compares its token overlap against the matched evidence excerpt, and records a conservative status such as `supported_overlap`, `weak_overlap`, or `no_overlap`. This is internal inspection metadata only and does not create a customer-facing score or semantic fact-checking claim. |
| 2026-03-28 | **BM-038 — COMPLETE (design + implementation).** Added the first exact-page citation-quality metric slice for grounded runs. The metric counts completed runs where the measured-domain citation both matches a grounded page and has a `supported_overlap` claim/evidence signal, then records that as `exact_page_quality_rate` alongside matched/supported run counts. It is explicitly separate from citation presence and share-of-voice, and remains an internal benchmark-quality signal rather than a customer-facing score. |
| 2026-03-28 | **BM-039 — COMPLETE (implementation).** Added the first grounded-vs-ungrounded comparison view to the benchmark domain history page. The existing admin history surface now pairs the latest `grounded_site` and `ungrounded_inference` runs for the same query set and model, shows metric deltas and grounded exact-page quality, and keeps the change read-only and architecture-light by extending `benchmark-admin-data` instead of introducing a new route or benchmark subsystem. |
| 2026-03-28 | **BM-040 — COMPLETE (implementation).** Added the first recurring internal benchmark scheduling slice on the existing Cloudflare Worker cron path. Scheduled sweeps now use an env-configured query-set/model lane, sweep the current benchmark customer domains in a bounded daily window, stamp each run with deterministic schedule metadata and run scope, and skip duplicates by daily schedule key so benchmark history becomes comparable without a separate benchmark subsystem or customer-trigger path. |
| 2026-03-28 | **BM-041 — COMPLETE (design).** Added `PLAYBOOK/benchmark-competitor-cohort-methodology-v1.md` to freeze the first competitor/cohort benchmark frame rules before broader comparative UI exists. The methodology now makes the comparison frame explicit, constrains cohort size and role labeling, keeps claim language conservative, and requires underlying query lineage inspection before comparative outputs are trusted. |
| 2026-03-28 | **BM-042 — COMPLETE (implementation).** Added the first narrow competitor/cohort storage and admin slice: explicit `benchmark_cohorts` plus `benchmark_cohort_members` storage, and a read-only cohort-frame panel on the existing benchmark domain history page. The slice stays small on purpose: one stored query-set/model/run-mode frame per cohort, explicit measured-customer vs competitor roles, and links back to existing run-detail/domain-history inspection instead of a new benchmark subsystem. |
| 2026-03-28 | **BM-043 — COMPLETE (implementation).** Extended the existing benchmark run-detail page with a first lineage inspection slice: each query can now be read as prompt -> response -> extracted citations -> grounded evidence status / claim-overlap metadata. This reuses the current admin route and stored citation metadata instead of adding a new benchmark subsystem or widening persistence. |
| 2026-03-28 | **BM-044 — COMPLETE (implementation).** Extended the existing benchmark execution seam to support more than one live model lane on the same provider without widening the admin controls. `BENCHMARK_EXECUTION_ENABLED_MODELS` now acts as a narrow comma-separated allowlist, the first enabled model remains the default admin lane, and the Gemini adapter can execute any requested lane in that allowlist while still skipping unknown model ids truthfully. |
| 2026-03-28 | **BM-045 — COMPLETE (implementation).** Hardened the current benchmark schedule path for larger internal sweeps without splitting services: scheduled runs are now bounded by explicit per-sweep launch caps, repeated failures stop the sweep early, and run-level plus summary failure visibility is emitted on the existing structured log path. This keeps operator hardening on the current cron/schedule seam rather than introducing a second benchmark runtime. |
| 2026-03-28 | **BM-046 — COMPLETE (decision).** Froze the post-BM-045 operations decision for the 500 to 1000-site benchmark path. The repo truth is now explicit: do not split into a separate benchmark service yet; keep the current repo/runtime until real operating evidence shows customer-path risk, schedule brittleness, cost-boundary pressure, or observability failure. The 500 to 1000-site path remains planned, not implemented. |
| 2026-03-28 | **Benchmark backlog expansion.** Converted the remaining benchmark-methodology and scale gaps into explicit staged follow-up tasks `BM-033` ... `BM-046` so future work stays architecture-safe: ranked page selection, richer grounding metadata, provenance matching, excerpt-level evidence checks, citation-quality scoring, grounded-vs-ungrounded comparison UI, recurring runs, competitor/cohort methodology, lineage inspection, multi-model lanes, and scale hardening are now documented as separate slices rather than vague future work. |
| 2026-03-27 | **Architecture hardening stream opened.** Added an explicit architecture-hardening backlog covering lean-code discipline, persistent structured logs, admin log visibility, and Playwright readiness. Implemented the first observability slice: structured logs now persist best-effort to `app_logs`, `/dashboard/logs` shows recent internal logs for admins, and Cloudflare scheduled-task failures now flow through the shared structured logging path. |
| 2026-03-27 | **AH-005 — COMPLETE (implementation).** Added a small shared admin runtime helper for repeated admin auth, env loading, and service-role client setup, then moved the main admin pages plus benchmark server actions onto it. This reduces route-level duplication without introducing a broad request-context framework. |
| 2026-03-27 | **AH-006 — COMPLETE (implementation).** Reduced `app/dashboard/benchmarks/[runGroupId]/page.tsx` to a data-loading shell, moved the run-detail rendering into `components/benchmark-run-detail-view.tsx`, and verified the extracted parsing/formatting helpers in `lib/server/benchmark-run-detail.ts`. This is the first narrow large-module decomposition slice only; it does not claim broader benchmark UI or worker refactors yet. |
| 2026-03-27 | **AH-008 — COMPLETE (implementation).** Added the first open-source Playwright foundation: `@playwright/test`, a repo-local config, Chromium smoke specs for the landing page, missing-Turnstile scan-submit guard, a mocked free-scan happy path through submit/redirect/results render, results-page checkout-return messaging before payment confirmation, both sign-in entry points, unauthenticated benchmark-page redirect behavior, the first authenticated admin dashboard case via a non-production-only E2E session seam, and the first authenticated benchmark overview case via a non-production-only E2E admin-data seam, plus output ignores and CI wiring. This is still a narrow browser-smoke lane only; it does not yet cover benchmark actions after login or full live checkout execution. |
| 2026-03-27 | **AH-009 — COMPLETE (implementation).** Reduced `components/report-viewer.tsx` to a fetch/state shell, moved report-viewer helper logic into `lib/client/report-viewer.ts`, moved heavy section rendering into `components/report-viewer-sections.tsx`, and added both targeted helper tests and report-page browser proof. This is a narrow decomposition slice only; it does not claim broader report pipeline or worker refactors. |


| 2026-03-27 | **AH-010 — COMPLETE (implementation).** Reduced `app/dashboard/benchmarks/page.tsx` to a thin admin/data-loading shell, moved benchmark overview rendering into `components/benchmark-overview-view.tsx`, moved pure overview helpers into `lib/server/benchmark-overview.ts`, and kept the existing authenticated benchmark overview Playwright case as the browser proof for this route. The Playwright lane was also tightened to a serialized one-worker configuration because that is the stable dev-server setting for this repo. |

| 2026-03-27 | **AH-011 â€” COMPLETE (implementation).** Extracted shared deep-audit report preparation helpers into `workers/report/deep-audit-report-helpers.ts`, updated both the PDF builder and markdown exporter to reuse the shared narrative/parsing seam where safe, and added targeted helper tests. This is a worker-only decomposition slice; it does not change customer routes or require a new UI smoke test. |

| AH-010 | Add the third large-module decomposition slice by reducing the benchmark overview route to a thin shell and moving overview rendering/helpers into dedicated modules with existing browser proof retained | Backend + Frontend | ✅ DONE | `app/dashboard/benchmarks/page.tsx`, `components/benchmark-overview-view.tsx`, `lib/server/benchmark-overview.ts`; COMPLETION_LOG AH-010 |
| AH-011 | Add the fourth large-module decomposition slice by extracting shared deep-audit report preparation helpers out of the PDF builder without changing report behavior or adding UI churn | Backend | ✅ DONE | `workers/report/deep-audit-report-helpers.ts`, `workers/report/build-deep-audit-pdf.ts`, `workers/report/build-deep-audit-markdown.ts`; COMPLETION_LOG AH-011 |

| 2026-03-27 | **AH-012 — COMPLETE (implementation).** Extracted crawl-pending parsing, config merge, browser-render stat merge, and chunk-window planning into `workers/scan-engine/deep-audit-crawl-state.ts`, then updated `workers/scan-engine/deep-audit-crawl.ts` to consume the shared state helpers. This is a worker-only decomposition slice; it does not change customer routes or require a new UI smoke test. |
| 2026-03-27 | **AH-013 — COMPLETE (implementation).** Extracted shared email-delivery composition helpers into `workers/report/resend-delivery-helpers.ts`, updated `workers/report/resend-delivery.ts` to reuse the shared CTA/formatting/encoding seam while preserving its existing type exports for callers, and added targeted helper tests. This is a worker-only decomposition slice; it does not change customer routes or require a new UI smoke test. |
| 2026-03-28 | **AH-007 — COMPLETE (design).** Reviewed the current founder-stage admin auth path in `lib/server/require-admin.ts` and `app/admin/login/actions.ts`, then documented the next-stage maturity plan in `PLAYBOOK/architecture-hardening-backlog.md`: keep the current env-based single-admin-email gate for internal use, but move to a fail-closed table-driven admin allowlist and small explicit role model before broader operator scale. No runtime auth behavior changed in this slice. |

| 2026-03-28 | **UX-008 — COMPLETE (implementation + verification).** Tightened the customer-facing results-to-report action layer after founder review: the score snapshot card now has a real share action with native-share or copy-link fallback plus OG-image preview, delivered-report status only promises direct access when hosted artifacts actually exist, the report viewer falls back cleanly to PDF download when no web report is available, and results-page load states now distinguish missing / expired / private scans more clearly. Verification: `npm.cmd run type-check`, targeted Vitest for `lib/client/results-journey.test.ts`, and targeted Playwright smoke for checkout-return messaging, share snapshot, interactive report render, and PDF-only fallback all passed outside sandbox. |
| 2026-03-29 | **Paid-path traceability slice — COMPLETE (implementation).** Added the missing runtime breadcrumbs on the post-payment path so live founder testing can distinguish webhook success from queue/report-worker failure. Stripe webhook processing now logs a structured success/failure event, queue enqueue now logs both success and failure with scan/run ids, and the report worker now logs start, email-sent, and completed milestones in addition to existing failure logs. Verification: `npm.cmd run type-check` passed and targeted Vitest for `app/api/webhooks/stripe/route.test.ts` plus `lib/server/stripe/checkout-completed.test.ts` passed outside sandbox. |
| 2026-03-29 | **Paid-report recovery slice — COMPLETE (implementation + verification).** Tightened the successful paid-path recovery story: delivered results now explain how to save the report into the dashboard, login explicitly tells the user to use the Stripe checkout email first, dashboard empty state now explains why a paid report may not appear yet, and report-delivery emails now remind the user of the same recovery rule. Verification: `npm.cmd run type-check` passed and targeted Playwright smoke for delivered-results recovery, customer login, and dashboard entry all passed outside sandbox. |
| 2026-03-29 | **Results chronology slice — COMPLETE (implementation + verification).** Added one explicit top-of-page action band to the customer results route so the next step is obvious in each real state: preview users get direct buy/save actions, generating users get a progress/recovery panel, and delivered users get open/download/dashboard recovery actions without hunting below the score section. Verification: `npm.cmd run type-check` passed and targeted Playwright smoke for checkout-return, generating, and delivered states all passed outside sandbox. |

---

## Known Constraints (do not debate these)
- No Vercel (commercial use prohibited on hobby plan)
- No Make.com (15x over free limit)
- No Puppeteer in Workers (V8 isolate, not Node.js)
- No `@cloudflare/next-on-pages` (deprecated)
- Gemini free tier: 1,000 RPD — architect around this
- Workers free tier CPU: 10ms — PDF gen requires paid $5/mo plan
- Supabase 500MB + 2GB bandwidth — monitor, Pro at $25/mo when needed

| 2026-03-29 | **BM collection start slice - COMPLETE (implementation + design).** Added the first narrow recurring benchmark collection path for category-based seed data. `benchmark_seed.csv` can now be imported into `benchmark_domains` through `scripts/benchmark-seed-domains.ts` as explicit schedule-enabled targets, the recurring scheduler can filter by one benchmark vertical plus selected seed priorities, and schedule idempotency now supports 12-hour windows so a twice-daily lane does not self-dedupe. The first operating lane is frozen in `PLAYBOOK/benchmark-collection-operations-v1.md`: `law_firms` priority `1`, one fixed query set version, one fixed model lane, and both `ungrounded_inference` plus `grounded_site`. |

| 2026-03-29 | **Law-firms query-set seed slice - COMPLETE (implementation + design).** Added a repeatable seed path for the first frozen law_firms recurring collection query set. scripts/benchmark-seed-query-set.ts now seeds eval/fixtures/benchmark-law-firms-p1-query-set.json through the existing repository seam, so the first collection lane no longer depends on manual admin query entry. The frozen first query-set identity is law-firms-p1-core 1 with 8 queries. |

| 2026-03-29 | **Benchmark schedule preview slice - COMPLETE (implementation + verification).** Added a dry-run preview path for the recurring benchmark lane. scripts/benchmark-schedule-preview.ts now reads the current BENCHMARK_SCHEDULE_* env, reuses the existing scheduler selection rules, and prints the exact query set, model lane, schedule window, and selected domains before cron is enabled. |

| 2026-03-29 | **Benchmark schedule run-now slice - COMPLETE (implementation + verification).** Added scripts/benchmark-schedule-run-now.ts and 
pm run benchmark:schedule:run-now so the recurring benchmark lane can be executed immediately through the same scheduler path as cron. This keeps the proof step architecture-light: same env, same selection rules, same run metadata, no separate benchmark orchestration path. |

| 2026-03-29 | **Benchmark scheduled-window summary slice - COMPLETE (implementation + verification).** Added a read-only terminal summary for the current scheduled benchmark frame. scripts/benchmark-schedule-summary.ts now uses the existing run-group records plus stored metrics to pair grounded and ungrounded runs per domain and print the current window review without adding a new admin route. |

| 2026-03-29 | **Benchmark outlier-selection slice - COMPLETE (implementation + verification).** Added scripts/benchmark-schedule-outliers.ts so the current scheduled window can be ranked by grounded-minus-ungrounded citation-rate delta. This keeps the next methodology step focused and small: inspect the strongest grounded winners and losers in the existing lineage view before touching scoring or scale. |

| 2026-03-29 | **Benchmark run-diagnostic slice - COMPLETE (implementation + verification).** Added scripts/benchmark-run-diagnostic.ts so selected outlier run-group ids can be summarized from existing stored lineage data before manual inspection. The diagnostic now reports URL-vs-domain citation depth, provenance matches, overlap support, and a first probable-issue label without adding a new benchmark subsystem. |

| 2026-03-29 | **First live benchmark window interpretation slice - COMPLETE (docs).** Froze the current operator meaning of the first law_firms-p1-v1 live window on gemini-2.5-flash-lite: grounded citation-rate deltas are usable internal directional signal, but exact-page quality should not yet be treated as a gating metric for this lane because targeted outlier diagnostics showed mostly domain-level grounded attribution rather than page-level URLs. |

| 2026-03-29 | **Two-window benchmark decision freeze - COMPLETE (docs).** After two comparable law_firms-p1-v1 windows on gemini-2.5-flash-lite, the lane is now explicitly treated as a domain-level grounded attribution lane: grounded citation-rate deltas are usable internal signal, exact_page_quality_rate stays visible but non-gating, and the repo should not prioritize provenance-matcher work or scale-up for this lane without new evidence. |
| 2026-03-29 | **Law-firms fit analysis freeze - COMPLETE (analysis/docs).** Completed a manual fit read of the current 21-domain `law_firms` priority-1 lane and froze it in `PLAYBOOK/law-firms-p1-fit-analysis-v1.md`. Current truth: the broad lane is a mixed cohort, not one coherent law-firm frame. It combines enterprise/business-counsel firms with immigration, divorce, PI, and employment specialists while the frozen query set mixes multiple legal-service intents. The main signal-quality issue is now clearer: many current domain/query pairs are low-fit by design, so future redesign work should treat cohort split and query-set redesign as coupled rather than reaching first for matcher rewrites or more infrastructure. |
| 2026-03-29 | **Law-firms replacement-target freeze - COMPLETE (analysis/docs).** Froze the first narrow replacement target for the `law_firms` benchmark redesign: `business_counsel / biglaw / enterprise`. Current truth: the next precision lane should start from the largest coherent subgroup already present in the live 21-domain slice, and the next query-set rewrite should serve that subgroup only rather than trying to repair the whole mixed law-firms lane at once. |
| 2026-03-29 | **Business-counsel narrow query-set draft freeze - COMPLETE (analysis/docs).** Added the first frozen replacement query-set draft for the `business_counsel / biglaw / enterprise` subgroup at `eval/fixtures/benchmark-law-firms-business-counsel-v1-query-set.json`. Current truth: the replacement frame now has a concrete draft built around enterprise/business-law buying intent, but it remains a draft methodology artifact only until the exact target-domain list is frozen and the team explicitly decides to seed a narrow live lane. |
| 2026-03-29 | **Business-counsel target-domain freeze - COMPLETE (analysis/docs).** Froze the exact target-domain list for the first narrow replacement lane as `law_firms_business_counsel_v1`: 17 domains taken from the current priority-1 live lane, excluding the consumer/specialist outliers (`forthepeople.com`, `cordellcordell.com`, `fragomen.com`, `seyfarth.com`). Current truth: the next narrow live experiment can now be launched as a separate comparable frame without redefining the target subgroup on the fly. |
| 2026-03-29 | **Business-counsel query-set seed path - COMPLETE (implementation/docs).** Added an explicit package command for seeding the first narrow replacement query-set draft: `npm run benchmark:seed:query-set:business-counsel`. Current truth: the draft query-set can now be created as a separate DB record without touching the broad live lane or its schedule configuration. |
| 2026-03-29 | **Business-counsel query-set seeded - COMPLETE (operator evidence/docs).** Seeded the first narrow replacement query-set draft successfully. Current draft query-set record: `9910b5ac-ade6-42be-9dca-9b85c04e4469` with 6 queries. This record now exists independently of the broad live lane and can be used for preview/config work without changing the current recurring `law_firms-p1-v1` schedule. |
| 2026-03-29 | **Benchmark schedule domain-allowlist slice - COMPLETE (implementation/docs).** Extended the existing schedule filter seam with an optional `BENCHMARK_SCHEDULE_DOMAINS` canonical-domain allowlist. Current truth: the team can now preview and later launch a narrow business-counsel lane against the frozen 17-domain cohort without mutating the current broad 21-domain live lane. |
| 2026-03-29 | **First business-counsel narrow-lane result freeze - COMPLETE (operator evidence/docs).** The first live `law-firms-business-counsel-v1` window completed cleanly across all 17 paired domains on `gemini-2.5-flash-lite`. Current truth: narrowing the cohort/query frame improved signal quality versus the broad mixed lane while `exact_page_quality_rate` remained `0%` and non-gating. The business-counsel lane is now the cleaner internal law-firms signal lane, and the next move should be repeated comparable windows rather than immediate prompt/model/evidence-depth changes. |
| 2026-03-29 | **Primary law-firms lens freeze - COMPLETE (operator evidence/docs).** After three comparable `law-firms-business-counsel-v1` windows, the narrow business-counsel lane is now the primary internal law-firms benchmark frame. Current truth: the broad `law-firms-p1-v1` lane remains useful only as a secondary legacy comparison frame, while continued law-firms methodology decisions should use the narrow lane first. |
| 2026-03-29 | **Schedule run-now window override - COMPLETE (implementation/docs).** Added `--window-date YYYY-MM-DDTHH` support to `npm run benchmark:schedule:run-now`. Current truth: the team can now create the next controlled benchmark window on demand without waiting for the UTC slot to advance, while still using the same scheduler path and run metadata shape as cron. |
| 2026-03-29 | **Benchmark recurrence review slice - COMPLETE (implementation/docs).** Added a small terminal-only recurrence helper for the current scheduled benchmark frame. `scripts/benchmark-schedule-recurrence.ts` and `buildBenchmarkScheduleMultiWindowSummary(...)` now summarize recurring winners and laggards across an explicit chosen set of comparable windows using the existing run-group records, without adding a new admin route or persistent aggregation layer. |
| 2026-03-30 | **Layer One report rewriter contract freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-report-rewriter-contract-v1.md` to freeze the required section order and role of each section for future Layer One report rewrites. Current truth: report-quality hardening should start by separating confirmed findings, likely implications, priority actions, optional advanced GEO ideas, and open questions before changing prompts or retrieval inputs. |
| 2026-03-30 | **Layer One evidence-discipline freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-report-evidence-discipline-v1.md` to freeze the claim-boundary rules for future Layer One report rewrites. Current truth: rewritten reports should not invent market statistics, hard-diagnose weak signals, or collapse optional GEO strategy into audit-backed remediation before the actual prompt/runtime slice is updated. |
| 2026-03-30 | **Layer One tone and verbosity freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-report-tone-and-verbosity-v1.md` to freeze the default presentation rules for future Layer One report rewrites. Current truth: the rewrite layer should use plain operational language, open with site-specific findings rather than industry framing, and compress repeated narrative before the prompt/runtime slice is updated. |
| 2026-03-30 | **Layer One recommendation-format freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-report-recommendation-format-v1.md` to freeze the recommendation shape for future Layer One report rewrites. Current truth: the `Priority actions` section should resolve into explicit issue, why-it-matters, action, priority, and confidence fields before the prompt/runtime slice is updated. |
| 2026-03-30 | **Layer One ambiguous-signal wording freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-report-ambiguous-signal-wording-v1.md` to freeze how future Layer One report rewrites should describe real-but-ambiguous findings. Current truth: uncertain signals like `402/403`, low-confidence extraction, partial schema, stale freshness, and mixed page outcomes should use observed signal, bounded implication, and verification-step wording before the prompt/runtime slice is updated. |
| 2026-03-30 | **Layer One rewrite-prompt implementation slice - COMPLETE (implementation/docs).** Added `lib/server/layer-one-report-rewrite-prompt.ts` plus `scripts/layer-one-report-rewrite-prompt.ts` so an existing markdown report can now be turned into the constrained rewrite prompt text for another LLM. Current truth: report hardening now has a real local execution seam, but the shipped customer report generation path remains deterministic. |
| 2026-03-30 | **Layer One real-world rewrite fixture slice - COMPLETE (operator workflow/docs).** Added `docs/review-inputs/reports/cllc-gemini-report.md` as the first real-world external rewrite example for the constrained Layer One prompt seam. Current truth: the rewrite workflow now has one concrete bad-output fixture to test against, but there is still no dedicated UI for this path. |
| 2026-03-30 | **Layer One gold rewrite fixture slice - COMPLETE (fixture/docs).** Added `eval/fixtures/cllc-layer-one-rewrite-gold.md` as the first gold-standard rewritten Layer One report fixture. Current truth: prompt tuning can now compare candidate rewrites against one concrete target output for a real site instead of relying only on high-level writing rules. |
| 2026-03-30 | **Automatic Layer One report eval writing - COMPLETE (implementation/docs).** Added `lib/server/report-eval-writer.ts` and wired the deep-audit report queue consumer to write a deterministic `report_eval_runs` row automatically after report creation under framework `layer_one_report`. Current truth: Layer One report quality can now be tracked in the existing admin eval analytics over time without introducing Promptfoo or RAGAS into the paid report runtime. |
| 2026-03-30 | **Deep-audit markdown delivery fix - COMPLETE (implementation/docs).** Added `app/api/scans/[id]/report-markdown/route.ts` and switched the report viewer to load markdown through the app instead of browser-fetching the raw storage URL. Current truth: delivered reports now have a same-origin markdown render path plus a direct markdown download endpoint, which avoids public-R2/CORS fragility in the `View report` flow. |
| 2026-03-30 | **Layer One internal rewritten-artifact contract freeze - COMPLETE (design/docs).** Added `PLAYBOOK/layer-one-internal-rewrite-artifact-v1.md` to freeze the next paid-report improvement boundary. Current truth: any rewritten Layer One report should begin as a second internal artifact stored and evaluated alongside the deterministic markdown first, not as an immediate replacement for the customer-facing paid report. |
| 2026-03-30 | **Layer One internal rewritten-artifact implementation - COMPLETE (implementation/docs).** Added `lib/server/layer-one-report-internal-rewrite.ts`, optional `DEEP_AUDIT_INTERNAL_REWRITE_*` env gates, separate rewritten-markdown R2 storage, and a second best-effort `layer_one_report` eval write in `workers/queue/report-queue-consumer.ts`. Current truth: deep-audit generation can now create an internal rewritten markdown artifact and rewritten eval row without changing the default paid report output. |
| 2026-03-30 | **Layer One report internal comparison access - COMPLETE (implementation/docs).** Added `app/dashboard/evals/report/[id]/page.tsx` and linked `layer_one_report` rows from the existing eval analytics table. Current truth: admins can now open one report-eval run, inspect metadata, and compare deterministic vs rewritten sibling report evals side-by-side for the same `scan_id` before any paid-report default is changed. |
| 2026-03-30 | **Layer One operator judgment seam - COMPLETE (implementation/docs).** Added `app/dashboard/evals/actions.ts` and extended `app/dashboard/evals/report/[id]/page.tsx` with `better / worse / unclear` buttons for rewritten report variants. Current truth: rewritten-report review now has a lightweight persistent operator judgment loop stored in `report_eval_runs.metadata`, so repeated internal comparisons can accumulate evidence before any paid default switch. |
| 2026-03-30 | **Report Design teamOwner propagation slice - COMPLETE (implementation/docs).** Added `teamOwner` to normalized `IssueRow` parsing in `workers/report/deep-audit-report-helpers.ts`, backed by the standalone RD-007 map, and switched the markdown builder to use the shared parser. Current truth: report issue rows now carry owner metadata for later Team Action Map / Immediate Wins work, while customer-facing report structure and content remain unchanged. |
| 2026-03-30 | **Report Design canonical payload teamOwner propagation - COMPLETE (implementation/docs).** Updated `workers/report/deep-audit-report-payload.ts` so the canonical deep-audit payload now preserves normalized issue rows, including `teamOwner`, across highlighted issues, all issues, and page-level issue arrays. Current truth: owner metadata now survives through the canonical report payload, but no owner-aware report sections are rendered yet. |
| 2026-03-30 | **Report Design internal Immediate Wins payload seam - COMPLETE (implementation/docs).** Added `workers/report/immediate-wins.ts` and extended the canonical deep-audit payload with an internal-only `immediateWins` block derived from owner-aware issues. Current truth: the report pipeline now derives prefiltered ticket candidates internally, which now also feed the first rendered markdown `Immediate Wins` section. |
| 2026-03-30 | **Report Design deterministic Immediate Wins markdown slice - COMPLETE (implementation/docs).** Updated `workers/report/build-deep-audit-markdown.ts` so the paid markdown report now renders a first deterministic `Immediate Wins` section from the owner-aware payload seam. Current truth: the report now exposes one owner-aware ticket-style section without changing the broader report order, PDF layout, or web report viewer structure yet. |
| 2026-03-30 | **Report Design per-page markdown cleanup slice - COMPLETE (implementation/docs).** Updated `workers/report/build-deep-audit-markdown.ts` so passed checks in the per-page checklist no longer print `Fix:` lines. Current truth: the markdown report now avoids one obvious source of contradictory/noisy copy while leaving findings, scoring, and overall structure unchanged. |
| 2026-03-30 | **Report Design bounded low-confidence wording slice - COMPLETE (implementation/docs).** Added shared customer-facing finding formatting in `workers/report/deep-audit-report-helpers.ts` and reused it in both markdown and PDF report rendering. Current truth: raw low-confidence transport tokens like `http_403` no longer leak directly into customer-facing report copy, while the underlying audit data remains unchanged. |
| 2026-03-30 | **Report Design metadata-guidance cleanup slice - COMPLETE (implementation/docs).** Corrected broken title-length guidance at the audit-check source in `workers/scan-engine/checks/check-title.ts` and added a targeted regression test. Current truth: customer-facing reports now inherit a readable `10-70` title range instead of corrupted `1070`-style output. |
| 2026-03-30 | **Report Design per-page markdown compression slice - COMPLETE (implementation/docs).** Updated `workers/report/build-deep-audit-markdown.ts` so the per-page checklist now shows only non-passing rows. Current truth: the markdown report is materially less repetitive, while findings, scoring, and the rest of the report structure remain unchanged. |
| 2026-03-30 | **Production domain cutover prep slice - COMPLETE (config/docs).** Updated `wrangler.jsonc` so production `NEXT_PUBLIC_APP_URL` now targets `https://getgeopulse.com/`, and added `PLAYBOOK/getgeopulse-domain-cutover-v1.md` to freeze the operator cutover checklist. Current truth: repo config is now aligned to the new domain, but DNS, Turnstile, Supabase Auth, Stripe, Resend, and WAF closure still require operator actions. |
