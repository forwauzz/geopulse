/**
 * Optional slugs to feature in "Editor's choice". Empty = section hidden or shows nothing.
 * Add published slugs here without DB migrations.
 */
export const EDITORS_CHOICE_SLUGS: readonly string[] = [];
